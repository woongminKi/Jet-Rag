# 2026-05-02 W4 Day 4 마감 — W4-Q-15 (b)·(c) 노이즈 룰 + W4-Q-9 sniff

> W4 명세 v0.1 (CONFIRMED) §3.W4-Q-15 + §3.W4-Q-9 sniff ship 완료.
> 단위 테스트 6건 신규 + chunk_filter 가시성 보강 + DOCX/PPTX 라이브러리 평가.

## 0. TL;DR

- `chunk_filter` 에 (c) **empty** 마킹 추가 (b 는 W3 Day 4 ship 완료, 본 Day 는 (c) + 가시성)
- chunk_filter 가시성 보강 (G-2 P3) — `empty=N` 카운트 + 마킹 비율 > 5% WARNING
- 단위 테스트 신규 6건 (총 19/19 PASS) + 회귀 0 (125 → 131)
- 백필 dry-run — 추가 마킹 **0건** (기존 chunks 분포에서 빈/반복 청크 없음, dead branch 확인)
- DOCX/PPTX sniff 평가 리포트 — DOCX 본격 구현 ~1.5d (HwpxParser 패턴 재사용), PPTX ~2d
- DE-66 (노이즈 룰 (b) 임계값) **3회/100자 유지** + DE-67/68 신규 후보 DRAFT (W5 결정)

## 1. 작업 5건 — 시간 순

| # | 마일스톤 | 산출물 |
|---|---|---|
| 1 | Day 3 마감 commit/push 후 명세 §3.W4-Q-15 + §3.W4-Q-9 정독 | 메인 스레드 컨텍스트 |
| 2 | chunk_filter 에 (c) empty 룰 + (b) 가시성 보강 직접 구현 (단순 작업 직접 처리) | `chunk_filter.py` 변경 |
| 3 | 단위 테스트 6건 추가 (empty 4 + header_footer classify 2) | `test_chunk_filter.py` 확장 |
| 4 | 백필 dry-run 재실행 | `2026-05-02 W4-Q-15 백필 dry-run 리포트.md` |
| 5 | DOCX/PPTX 라이브러리 sniff (WebFetch 로 README 검토) | `2026-05-02 W4-Q-9 sniff DOCX·PPTX 라이브러리 평가.md` |

## 2. 코드 변경

### 2.1 변경 파일

| 파일 | 변경 | LOC |
|---|---|---|
| `api/app/ingest/stages/chunk_filter.py` | docstring + (c) empty 마킹 분기 + 가시성 (`empty=N` + 5% WARNING) | +20 / -5 |
| `api/tests/test_chunk_filter.py` | 신규 6건 (`ClassifyChunkEmptyTest` 4 + `HeaderFooterClassifyTest` 2) | +60 |
| `work-log/2026-05-02 W4-Q-15 백필 dry-run 리포트.md` | 신규 | +60 |
| `work-log/2026-05-02 W4-Q-9 sniff DOCX·PPTX 라이브러리 평가.md` | 신규 | +144 |
| `work-log/2026-05-02 W4 Day 4 마감.md` | 본 문서 (신규) | (현재) |

### 2.2 핵심 변경 — (c) empty 마킹 + 우선순위

```python
def _classify_chunk(chunk, header_footer_texts):
    text = chunk.text or ""
    stripped = text.strip()

    # W4-Q-15 (c) — 빈 청크 우선 마킹 (dead branch 보호 인프라)
    if not stripped:
        return "empty"

    # 기존 — header_footer 우선
    if stripped in header_footer_texts:
        return "header_footer"
    ...
```

### 2.3 가시성 보강 — G-2 P3 처리

```python
filter_ratio = (sum(filtered_count.values()) / total) if total else 0.0
logger.info(
    "chunk_filter: doc=%s total=%d table_noise=%d header_footer=%d "
    "empty=%d filter_ratio=%.3f",
    ...
)
if filter_ratio > 0.05:
    logger.warning(
        "chunk_filter: doc=%s 마킹 비율 %.1f%% > 5%% — false positive risk 검토 필요",
        ...
    )
```

## 3. 단위 테스트 (신규 6건)

### 3.1 (c) empty 4건

- `test_strip_empty_text_marked_as_empty` — `"   \n\t  "` → empty
- `test_truly_empty_text_marked_as_empty` — `""` → empty
- `test_non_empty_text_not_marked_as_empty` — `"짧은 텍스트"` → None (다른 룰 위임)
- `test_empty_takes_priority_over_header_footer` — 빈 텍스트는 header_footer set 에 있어도 empty 우선

### 3.2 (b) header_footer classify 2건 (기존 detection 만 있던 영역 보강)

- `test_classify_marks_header_footer_when_text_in_set` — set 내 텍스트 → header_footer 마킹
- `test_classify_strips_text_before_lookup` — strip 후 lookup (공백 padding 안전)

### 3.3 회귀

| 단계 | 결과 |
|---|---|
| chunk_filter 단위 | 19/19 PASS (기존 13 + 신규 6) |
| 전체 회귀 | 131/131 PASS (이전 125 + 신규 6) |

## 4. 백필 dry-run 결과

본문: `work-log/2026-05-02 W4-Q-15 백필 dry-run 리포트.md`

```
- 전체 chunks: 555
- 마킹 대상: 0
- 마킹 비율: 0.0%
```

해석:
- (a) table_noise: W3 Day 4 에서 이미 83건 마킹 완료
- (b) header_footer: 0건 — chunks 분포에 동일 짧은 텍스트 ≥ 3회 부재 (진단 리포트와 일관)
- (c) empty: 0건 — chunk.py 가 빈 단락 skip (dead branch, 예상대로)

**의의**: 인프라 차원 ship 으로 신규 파서 (DOCX/PPTX) 도입 시 자동 적용. 임계값 (3회/100자) 적정성 확인 (false positive 0).

## 5. DOCX/PPTX sniff 결과

본문: `work-log/2026-05-02 W4-Q-9 sniff DOCX·PPTX 라이브러리 평가.md`

| 항목 | DOCX | PPTX |
|---|---|---|
| heading 추출 | ✅ style 기반 (`'Heading' in paragraph.style.name`) | ✅ `slide.shapes.title` |
| 한국어 처리 | ✅ unicode | ✅ unicode |
| 표 처리 | ⚠️ paragraphs 와 분리 (순서 통합 어려움) | ✅ shape.has_table 명시 |
| 이미지 추출 | ⚠️ relationships API | ⚠️ shape type (별도 조사) |
| 본격 구현 비용 | ~1.5d | ~2d |
| W5 우선순위 | **1순위** (HwpxParser 패턴 재사용) | 2순위 (사용자 가치 측정 후) |

신규 결정 후보:
- **DE-67** — DOCX 파서 어댑터 (HwpxParser sticky propagate 패턴 재사용)
- **DE-68** — PPTX 본격 구현 시점 (사용자 자료 + 가치 검증 후)

## 6. 비판적 자가 검토

1. **(b)·(c) 추가 마킹 0건의 가치**: 명세 §3.W4-Q-15 의 ship 권장은 **인프라 사전 준비** 목적. 현재 chunks 에서 효과 0건이지만, 신규 파서 도입 시 자동 활성. CLAUDE.md 의 "임의 추상화 지양" 원칙과 약간 trade-off — 그러나 단순 분기 추가 (LOC < 5) 로 비용 낮음.
2. **WARNING 임계값 5%**: dry-run 에서 모든 doc 의 마킹 비율 < 5% (table_noise 18.6%/22.2% 의 W3 인제스트 결과는 단일 인제스트 batch 내 비율이라 별개). 단위 테스트 `test_marks_table_noise_and_preserves_existing_flags` 가 50% 마킹 → WARNING 트리거 검증.
3. **DOCX sniff 의 한계**: 사용자 보유 DOCX 자료 0건 → README + WebFetch 만으로 평가. 실제 한국어 doc 처리 검증은 W5 본격 구현 시.
4. **PPTX 의 발표자 노트**: README 에 미커버. RAG 가치는 슬라이드 본문보다 노트가 풍부할 가능성 — W5 본격 구현 시 첫 평가 항목.
5. **에이전트 활용 미진**: 본 Day 의 모든 작업 (단순 변경 + dry-run 재실행 + WebFetch 평가) 이 trivial 범위라 직접 수행. CLAUDE.md trivial 한정 직접 허용 부합.

## 7. AC 매트릭스 갱신

| 항목 | AC | 결과 | 충족 |
|---|---|---|---|
| W4-Q-15 (b)·(c) 룰 ship + 단위 테스트 6건 PASS | (b 는 W3 ship 완료) (c) ship + 19/19 PASS | ✅ |
| 백필 dry-run 마킹 비율 < 5% (false positive risk) | 0% | ✅ |
| golden 20건 회귀 top-3 hit 율 | Day 5 통합 시 측정 | ⏳ |
| W4-Q-9 sniff 평가 리포트 작성 | 본 리포트 + 위험 요소 + W5 우선순위 | ✅ |

## 8. DE 결정

| # | 결정 | 채택 | 사유 |
|---|---|---|---|
| **DE-66** | 노이즈 룰 (b) 임계값 — 3회/100자 vs 5회/200자 | (a) **3회/100자 유지** | dry-run 마킹 0건 → 임계값 적정 (false positive 0). 사용자 자산 누적 후 (W6+) 재calibration. |
| **DE-67 (NEW)** | DOCX 파서 어댑터 (W5) | (a) HwpxParser sticky propagate 패턴 재사용 | python-docx style + paragraph 패턴이 HwpxParser styleIDRef 패턴과 유사. 표/이미지는 chunk_filter + ImageParser 와 자연 통합. |
| **DE-68 (NEW)** | PPTX 본격 구현 시점 (W5) | (a) 사용자 PPT 자료 + 가치 검증 후 W5 Day 3 결정 | 슬라이드 + 노트 통합 정책 결정 + shape 다양성 비용 ↑. 가치 측정 부재 → 사전 검증 게이트. |

## 9. commit + push

| Hash | Commit |
|---|---|
| (이번 commit) | `feat(ingest)`: chunk_filter (c) empty 마킹 + 가시성 5% WARNING + DOCX/PPTX sniff (W4-Q-15·9) |

## 10. 다음 단계 — W4 Day 5

명세 §7 일정대로 Day 5 = 단위 테스트 + smoke + DoD + 종합 정리.

핵심 작업:
1. 본 적용 (재인제스트, DE-65) — chunks 555건 재인제스트 + section_title 채움 비율 + 청크 수 변화 측정
2. golden 20건 batch 회귀 — top-3 hit 율 W3 baseline (5/5) vs W4 후 비교
3. W4 종합 정리 work-log + W5 진입 핸드오프
4. P3 backlog 정리 (W3 잔존 5건 trivial 처리)

## 11. 한 문장 요약

W4 Day 4 — chunk_filter (c) empty 마킹 + 가시성 5% WARNING ship, 단위 테스트 19/19 PASS, 백필 추가 마킹 0건 (인프라 사전 준비), DOCX/PPTX sniff 평가 완료 (DOCX 1.5d / PPTX 2d, **DE-67·68 W5 결정 후보**), **DE-66 (a) 3회/100자 유지 CONFIRMED**.
