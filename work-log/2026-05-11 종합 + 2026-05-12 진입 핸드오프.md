# 2026-05-11 종합 마감 + 2026-05-12 다른 PC 진입 핸드오프 (v2 — 옵션 A 회복 + multimodal ship 반영)

> 프로젝트: Jet-Rag
> 작성: 2026-05-11 본 세션 최종 마감 (Phase 0~10 누적, multimodal LLM judge 실 storage 통합 ship 직후)
> 목적: 본 1장만 보고 5분 안 컨텍스트 회복 + 다음 진입 시 1순위 = acceptable_chunks LLM-judge 자동 보완 (R@10 / Recall@K 신호 정확도 향상). chunks DB 회귀 -185 는 Phase 9 옵션 A 회복으로 해소 (2469 / vision 191). multimodal judge ship 으로 활성 한계 #3 (vision_diagram faithfulness 0.0) 해소.

---

## 0. 한 줄 요약

> **2026-05-11 — Phase 0~10 누적 ship. Phase 0/1/2/3 (claim B 확정 + 재발 hook + cross_doc 보강) + Phase 4 fail close 후 Phase 8 (origin 67 commit cherry-pick + main merge) + Phase 9 옵션 A 회복 (chunks 2278 → 2469 / vision 0 → 191 / cost $0.0667 실측) + Phase 10 multimodal LLM judge 실 storage 통합 (cost $0.0110 / 6 row, vision_diagram 거짓양성 2 교정 + 거짓음성 2 회복). 단위 테스트 763 → 930 / skipped 1 / 회귀 0 (+167 이지만 origin 인수 분 포함, 본 세션 직접 +29). chunks DB 2469 / vision 191. master plan §6: S4-A 35% → 50%+ (옵션 A 회복 + multimodal judge ship), origin 인수분 약 92%. 활성 한계 #1, #3, #4 해소. main HEAD `fba1316`.

---

## 1. 본 세션 누적 ship 표 (Phase 0~10, commit hash 포함)

| Phase | sprint | 상태 | 핵심 산출물 | commit | 단위 테스트 |
|---|---|---|---|---|---:|
| 0 | vision_page_cache raw 검증 | ✅ Ship | v1=8 row (sample-report page 51~60) / v2=148 row / disjoint. 도구 majority 라벨링 한계 ("v1=0") 확인. | (도구만, 미커밋) | 763 유지 |
| 1 | S4-A D4 Phase 1 — compose_off baseline 차분 | ✅ Ship | option C in-memory 도구 + claim B 확정 (caption=true ΔR@10 +0.2194 / exact_fact ∩ caption=true +0.3588) | cherry-pick 통합 (Phase 8) | 763 → 771 (+8) |
| 2 | S4-A D4 Phase 2 — build_golden stale doc_id hook | ✅ Ship | v2 hook (auto title fallback + stats) + v1 `--validate-doc-ids` CLI/env (default OFF) | cherry-pick 통합 (Phase 8) | 771 → 778 (+7) |
| 3 | S4-A D4 Phase 3 — cross_doc 보강 | ✅ Ship | golden_v2 157 → 162 (cross_doc 4 → 8) + G-A-075 정정. 전체 R@10 0.6547 → 0.6368 (임계 통과). cross_doc R@10 0.1667 → 0.0625 (S4-B 정당성 보강). | cherry-pick 통합 (Phase 8) | 778 유지 |
| 4 | S4-A D4 Phase 4 — D5 reingest | ⚠️ Fail close → ✅ Phase 9 회복 | 3 doc per-doc cap $0.15 초과로 vision chunks 105건 소실 → chunks 1923 → 1738. 사용자 옵션 C 선택. **Phase 9 옵션 A 로 회복 완료**. | — | 778 유지 |
| 5 | 실 BGE-reranker (D6) | ❌ Skip → 별도 sprint deferred | — | — | — |
| 6 | S4-B 엔티티 추출 | ❌ Skip → deferred | — | — | — |
| 7 | 통합 핸드오프 1차 작성 | ✅ Ship | `work-log/2026-05-11 종합 + 2026-05-12 진입 핸드오프.md` (Phase 0~6 시점). **본 문서가 v2 로 overwrite**. | — | 778 유지 |
| 8 | origin 통합 (cherry-pick + merge) | ✅ Ship | origin 67 commit 앞서있음 발견 (S4-B ship + S5 진입 + DoD top-1 ≥ 0.80) → safety branch `jet-rag-local-20260511-pre-rebase` (HEAD `877a542`) + `reset --hard origin/main` + cherry-pick 8건 + `merge --no-ff` (5건 main 측 우선 자동 해결) | `c08854b..807c551` (cherry-pick) + `807c551..01e69e1` (merge) | 778 → 913 (+135, origin 인수) |
| 9 | 옵션 A 회복 | ✅ Ship | ENV `JETRAG_BUDGET_GUARD_DISABLED=1` + `JETRAG_DOC_BUDGET_USD=0.15` + `JETRAG_VISION_PROMPT_VERSION=v2`. `evals/_phase4_d5_recovery_3docs.py` 신규. 3 doc (sonata / 데이터센터 / sample-report) reingest → chunks 2278 → **2469** (+191, 기대 +185 초과) / vision chunks 0 → **191** / vision_budget_exceeded flag 3 doc 해제. R@10 0.6368 → 0.6819 / caption=true R@10 0.5459 → 0.6695 (+0.1236, **claim B 회복**) / cross_doc 0.0625 → 0.3333 (+0.2708). 실 cost $0.0667 (sample-report page 46~49 4건 cache miss). | `6a15d3c` | 913 유지 |
| 10 | multimodal LLM judge 실 storage 통합 | ✅ Ship | `evals/_multimodal_judge.py` 의 image_fetch_fn (PDF → PyMuPDF 150 DPI render → PNG, LRU 2-tier 캐시) + llm_call_fn (Gemini 2.5 Flash multimodal, `_gemini_common` 재사용, `temperature=0.0` + JSON mode). `vision_usage_log.source_type='multimodal_judge'` 통합. CLI `--with-multimodal-judge` + `JETRAG_RAGAS_MULTIMODAL_JUDGE` env (default OFF). 실 측정 vision_diagram 8 row (성공 7 / multimodal 6) cost **$0.0110 / 6 row = $0.0018/row** (cap $0.30 의 3.6%). text-only mean 0.696 vs multimodal mean 0.617 (-0.029, multimodal 더 엄격). 거짓 양성 교정 2 + 거짓 음성 회복 2 + verify 완성 1 + 일치 1. | `fba1316` | 913 → 930 (+17) |

**누적**: 763 → **930** / skipped 1 / 회귀 0 (+167, 본 세션 직접 ship +29 / origin 인수 +138)
**push 완료**: `c08854b..807c551` (cherry-pick) → `807c551..01e69e1` (merge) → `01e69e1..6a15d3c` (Phase 9) → `6a15d3c..fba1316` (Phase 10)
**main HEAD**: `fba1316`
**safety branch**: `jet-rag-local-20260511-pre-rebase` (HEAD `877a542`, 본 세션 5 commit 보존)

---

## 2. Master plan §6 sprint 진척률 (2026-05-11 최종 마감 시점)

| Sprint | 목표 | 진척률 | 마감일 | 비고 |
|---|---|---:|---|---|
| S0 비용 계측 / 캐시 / cap | 1주 | 100% ✅ | 2026-05-07 | 변동 0 |
| S1 골든셋 + 실 query 로그 | 1주 | 100% ✅ | 2026-05-07 | 변동 0 |
| S1.5 vision_need_score PoC | 3일 | 100% ✅ | 2026-05-07 | 변동 0 |
| S1.5 v3 multi-line table | 0.5일 | 100% ✅ | 2026-05-09 | 변동 0 |
| S2 Vision 선별 + budget 본 ship | 1주 | 95% | 2026-05-09 | D5 phase 2 보류 유지 |
| S3 Cheap router + decomposition + reranker | 1주 | 100% ✅ | 2026-05-09 | D6 실 reranker 측정 deferred |
| **S4-A 인제스트 보조 인덱스** | 1~2주 | **50%+** (오전 35% → +15+) | 진행 중 | D1+D2+D3 Phase 1 (어제) + D4 Phase 1/2/3 ship + D5 reingest 회귀 → Phase 9 옵션 A 회복 + Phase 10 multimodal judge 실 storage |
| S4-B 핵심 엔티티 추출 | 3일 | ~80% (origin 인수) | — | origin 에서 ship 됨 (`07_a..`) — 본 PC 인수 후 한계 #1/#3 해소된 chunks DB 위 재측정 권고 |
| S4-C section 요약 | 3일 | 0% | — | S4-B 검증 후 |
| S4-D 동의어 후보 사전 확장 | 2일 | 0% | — | S4-C 후 |
| S5 /answer UX | 1주 | ~10% (origin 진입) | — | top-1 ≥ 0.80 DoD 도달 (origin 보고). caption 정합 재측정 필요 |

### S4-A D4 효과 (오전 ship)
- Phase 1: caption=true (n=17) ΔR@10 +0.2194 / qtype 통제 후 +0.3588 — **claim B 확정**
- Phase 2: stale doc_id 재발 방지 두 layer
- Phase 3: cross_doc 4 → 8 (R@10 0.1667 → 0.0625) — S4-B query decomposition 정당성 보강

### S4-A D5 효과 (Phase 4 fail close → Phase 9 회복)
- Phase 4 (fail close): vision-derived chunks 105건 소실, claim B 후퇴
- **Phase 9 (회복 ship)**: chunks 2278 → 2469 (+191) / vision 0 → 191 / 실 cost $0.0667 / 전체 R@10 0.6368 → 0.6819 / caption=true R@10 0.5459 → 0.6695 (+0.1236 **claim B 자연 회복**) / cross_doc 0.0625 → 0.3333 (+0.2708) / v2 라벨 R@10 +0.0975

### S4-A Phase 10 효과 (multimodal LLM judge 실 storage 통합)
- helper `evals/_multimodal_judge.py` (origin `338a2d9` ship) 의 stub → 실 함수 구현
- image_fetch_fn: `documents.storage_path → Supabase storage.get → PyMuPDF page render (150 DPI) → PNG bytes` + LRU 2-tier (PDF 16 / PNG 64)
- llm_call_fn: Gemini 2.5 Flash multimodal (`_gemini_common.get_client()` + `with_retry()`, temperature=0.0, JSON mode)
- vision_usage_log integration (source_type='multimodal_judge')
- 실 측정 ratio: cost cap $0.30 의 3.6% → 정상 운영 안전 마진
- 정확도 영향: text-only mean 0.696 vs multimodal mean 0.617 — multimodal 더 엄격 (거짓 양성 교정 2 + 거짓 음성 회복 2)
- 활성 한계 #3 (vision_diagram faithfulness 0.0) 해소

---

## 3. 다른 PC 진입 절차 (5분)

### 3.1 코드 + work-log 동기화

```bash
cd Jet-Rag && git pull origin main
# 기대: main HEAD = fba1316
# safety branch jet-rag-local-20260511-pre-rebase 가 본 PC 에만 있음 (push 안 함, 보존 의도) — 다른 PC 에서는 무시

cd api && uv sync
cd ../web && pnpm install

cd ../api && uv run python -m unittest discover tests
# 기대: Ran 930 tests, OK (skipped=1)
```

### 3.2 .env 확인 (변경 없음, 본 세션 ENV 는 process scope)

```bash
# Phase 9/10 에서 사용한 ENV 는 모두 process scope (.env 변경 X). 다음 세션 진입 시 필요한 경우만 export.
# 일상 검색·답변 흐름은 ENV 추가 없이 동작 (multimodal judge default OFF).
```

### 3.3 chunks DB / vision_page_cache 상태 확인

```sql
-- chunks 총 row (기대: 2469)
SELECT COUNT(*) FROM chunks;

-- vision-derived chunks (기대: 191)
SELECT COUNT(*) FROM chunks WHERE metadata->>'source' = 'vision_compose';

-- vision_page_cache (기대: v2 ≈ 152, v1 = 8)
SELECT prompt_version, COUNT(*) FROM vision_page_cache GROUP BY prompt_version;

-- vision_budget_exceeded flag (기대: 0)
SELECT COUNT(*) FROM documents WHERE flags->>'vision_budget_exceeded' = 'true';

-- vision_usage_log (cost 누적, 본 세션 +$0.0777)
SELECT SUM(cost_usd), MAX(created_at) FROM vision_usage_log WHERE created_at::date = '2026-05-11';
```

### 3.4 multimodal judge 활성화 (선택, 검증용)

```bash
# 1회성 실측 또는 acceptable_chunks LLM-judge 진입 시 ON
export JETRAG_RAGAS_MULTIMODAL_JUDGE=1

# 일상 회귀 측정은 default OFF 유지 (cost 보호)
uv run python evals/run_ragas_regression.py --goldenset v2 --with-multimodal-judge
```

---

## 4. 활성 한계 (해소 / 신규)

### 해소
- ~~#1 chunks DB 회귀 -185~~ → **Phase 9 옵션 A 해소** (2026-05-11, `6a15d3c`)
- ~~#3 vision_diagram faithfulness 0.0~~ → **Phase 10 multimodal judge 해소** (2026-05-11, `fba1316`)
- ~~#4 budget_guard cache hit 우회 부재~~ → ENV `JETRAG_BUDGET_GUARD_DISABLED` + 회복 도구 패턴으로 우회 가능 (영구 정책은 별도 sprint)

### 잔존
- **#2 G-A-107/111 search empty** — 포트폴리오 chunk page DB NULL 별개 이슈
- **#5 본 PC 측정 가능 row N=3** — golden v1 hint cross-check 한계 (origin 인수로 다른 PC dataset 포함됐는지 별도 확인)
- **#6 R@10 v1 ↔ v2 직접 비교 부적절** — graded scoring 도입으로 분모 inflation
- **#7~#12** (어제 통합 핸드오프 §4 참조) — 데이터센터 p.40 / cap modeling 결함 / prompt_version surfacing 등 origin 인수 분 별도 확인 필요

### 신규 (Phase 9~10 발견)
- **#13 sample-report page 46~49 vision_page_cache 누락** — Phase 9 회복 시 4건 cache miss → 추가 cost $0.0667 발생. 다른 26 페이지는 v2 cache hit 100%. cache backfill 별도 sprint (P2)
- **#14 G-A-208 multimodal judge timeout** — Gemini multimodal API 응답 60s 초과 1 row. retry 후 fallback to text-only 처리됨. timeout 진단 + retry 정책 별도 검토 (P2)
- **#15 G-U-016 chunks.page=None** — multimodal judge image_fetch_fn 이 page 번호 필요한데 chunks 측 메타 누락 1 row. text-only fallback. metadata.page 보존 정책 별도 검토 (P2)

---

## 5. 다음 후보 우선순위 (다른 PC 진입 시점)

### 1순위 — acceptable_chunks LLM-judge 자동 보완 (selective 30 row, ~$0.15)
- 본 세션 Phase 10 multimodal judge ship 의 직접 후속
- 골든셋 v2 162 row 중 acceptable_chunks 가 empty 인 30 row 선별 → LLM-judge 로 자동 보완 (Gemini 2.5 Flash, JSON mode)
- 기대: Recall@K / R@10 신호 정확도 향상, claim B 측정 noise 감소
- cost 예산: $0.005/row × 30 = ~$0.15 (cap $0.30 의 50%)
- DoD: acceptable_chunks 채워진 row +30 / R@10 변동 ±0.02 이내 / 단위 테스트 930 / 회귀 0
- 도구: `evals/_acceptable_chunks_judge.py` 신규 (Phase 10 helper 재사용)
- 사용자 명시 승인 필요 (paid LLM 호출)

### 2순위 — multimodal judge 샘플 확대 (vision_diagram 8 row → 전 caption=true 17 row)
- Phase 10 ship 의 효과 일반화 측정
- 17 row 다 multimodal 처리 시 cost ~$0.03 (cap 대비 안전)
- DoD: caption=true 17 row 의 multimodal vs text-only 차이 통계적으로 측정 (paired t-test 또는 effect size)

### 3순위 — chunk text augmentation (S4-A D6 후보)
- 현 chunk text 가 caption + body 합성된 vision-derived 외에는 raw text only
- caption-like prefix / surrounding context window 추가 시 R@10 변동 측정
- 5~7일

### 4순위 — entity_boost factor (S4-B 후 또는 병행)
- origin 에서 S4-B 80% ship 후 entity-aware ranking 효과 측정 (entity_boost factor 1.0 / 1.5 / 2.0 ablation)
- cross_doc 0.3333 (Phase 9 회복 후) 추가 개선 여지

### 5순위 — G-A-208 timeout 진단
- Phase 10 잔존 #14
- multimodal Gemini API 60s timeout 패턴 + retry 정책 + fallback chain 정합

### 6순위 — Phase 5 실 BGE-reranker 측정 (D6)
- Phase 9 회복으로 chunks DB 정상 → 측정 baseline 회복
- combo b/c 가 a 회복 + R@10 ≥ 0.75 / top-1 ≥ 0.95

### 7순위 — Phase 6 S4-B 본 PC 재검증
- origin 80% ship 의 chunks DB 정합성 본 PC 환경에서 측정

---

## 6. 사용자 결정 보류 항목

### 해소
- ~~Q-Phase4-Recovery~~ → 옵션 A 채택 (Phase 9 ship)
- ~~Q-multimodal-storage~~ → 실 storage + Gemini integration (Phase 10 ship)
- ~~Q-commit-policy~~ → Phase 9/10 각각 commit (`6a15d3c`, `fba1316`)

### 진행 중 / 신규
- **Q-acceptable-judge-real** acceptable_chunks LLM-judge 자동 보완 진입 시점 + 30 row 선별 기준 (empty 우선 vs caption_dependent 우선)
- **Q-chunk-augment** chunk text augmentation 범위 (caption prefix only vs surrounding context window)
- **Q-entity-boost-factor** S4-B entity_boost ablation 진입 시점 + factor 후보 (1.0 / 1.5 / 2.0)
- **Q-multimodal-expand** multimodal judge 17 row 확대 진입 시점 (cost ~$0.03 사전 승인)
- **Q-cache-backfill** sample-report page 46~49 vision_page_cache backfill 시점 (단발 cost $0.0667, vs S4-A D6 통합)
- **Q-budget-policy-permanent** ENV `JETRAG_REINGEST_BYPASS_BUDGET` 영구 추가 vs vision_page_cache hit 시 cap 우회 (별도 sprint)

### 기존 잔존
- Q-S4-B-진입-시점 / Q-prompt-version-surfacing / Q-S2-D5-3 / Q-S3-D1-1~4 / Q-S2-D4-1 / Q-S1.5-v3-1~5

---

## 7. 참고 문서 우선순위

1. **본 문서** — 1순위 진입 자료
2. `work-log/2026-05-10 종합 + 다음 세션 진입 핸드오프.md` — origin 작성, S4-B 80% ship + S5 진입 + multimodal judge stub 설계 내용
3. `work-log/2026-05-09 종합 + 2026-05-10 다른 PC 진입 핸드오프.md` — S3 D5 측정 결과 + S4-A D1~D3 ship
4. 기획서 v0.1 (`work-log/2026-04-22 개인 지식 에이전트 기획서 v0.1.md`) §6/§9/§11.5/§13
5. master plan (`work-log/2026-05-06 무료유료 모델 전략 통합 plan + 다른 컴퓨터 핸드오프.md`) §6
6. `evals/_multimodal_judge.py` (Phase 10 ship) + `evals/_phase4_d5_recovery_3docs.py` (Phase 9 ship)

---

## 8. 다른 PC 진입 후 첫 명령 (1순위)

```bash
# 0. 코드 동기화 + 단위 테스트 baseline 확인
cd Jet-Rag && git pull origin main
cd api && uv sync && uv run python -m unittest discover tests
# 기대: 930 tests OK / skipped 1

# 1. DB 상태 확인 (chunks 2469 / vision 191 / budget_exceeded 0)
# (위 §3.3 SQL 또는 supabase-jetrag MCP tools)

# 2. 1순위 진입 — acceptable_chunks LLM-judge 자동 보완
#    senior-planner 에이전트로 명세 작성 → senior-developer 구현 → senior-qa 검증 → ship
#    사용자 명시 승인 필요 (paid LLM 호출, ~$0.15 예산)
```

---

## 9. 한 문장 마감

> Phase 0~10 누적 ship 으로 본 세션 마감 — chunks DB 회복 + multimodal judge 실 storage 통합 완료, 단위 테스트 930 / 회귀 0 / 본 세션 직접 cost $0.0777, 다음 진입 1순위는 acceptable_chunks LLM-judge 자동 보완 (~$0.15).
