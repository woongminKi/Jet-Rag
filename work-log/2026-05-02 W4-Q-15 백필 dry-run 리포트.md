# 2026-05-02 W4-Q-15 백필 dry-run 리포트 — (b) header_footer + (c) empty 룰

> chunk_filter 에 (b) header_footer + (c) empty 룰 추가 후 기존 chunks 555건 백필 시뮬.
> DB 변경 0 — 마킹 대상 비율만 측정.

## 0. TL;DR

- 추가 마킹 **0건** — 기존 chunks 555건은 이미 W3 Day 4 backfill 로 (a) table_noise 83건 마킹 완료
- (b) header_footer: 0건 — chunks 진단 리포트 (2026-04-30) 와 일관 (헤더/푸터 0%)
- (c) empty: 0건 — chunk.py 가 빈 단락 skip (dead branch, 예상대로)
- **결론**: (b)·(c) 룰은 인프라 차원 ship — 향후 DOCX/PPTX 등 신규 파서에서 빈/반복 청크 발생 시 자동 적용

## 1. dry-run 실행 결과

```
$ cd api && uv run python scripts/backfill_chunk_flags.py
# fetching chunks for user_id=00000000-0000-0000-0000-000000000001
# fetched 555 chunks
# G(3) chunk flags backfill — dry-run 결과

- 전체 chunks: 555
- 마킹 대상: 0
- 마킹 비율: 0.0%

(마킹 대상 없음)
```

## 2. 분석

### 2.1 (b) header_footer 0건의 원인

chunks 진단 리포트 (`2026-04-30 chunk 품질 진단 리포트.md`) 의 헤더/푸터 후보 0% 결과와 일관:
- 임계값 (≥ 3회 + len < 100자) 이 chunks 555건의 분포에서 매칭되는 텍스트 없음
- 가능한 원인:
  - 파서 (PyMuPDFParser, HwpxParser) 가 페이지 단위 반복 헤더 텍스트를 청크 1개로 통합 (덜 분리)
  - 청크 단위가 페이지 헤더와 본문을 같이 묶어 "동일 짧은 텍스트" 가 발생 안 함
  - PDF/HWPX 페이지 번호는 raw_text 추출 시 자동 제외되거나 본문과 합쳐짐

**의의**: 임계값 적정성 측정 — 현재 임계값이 false positive 차단 충분 (오히려 너무 strict 가능). DOCX/PPTX 등 새 파서 도입 시 마킹 비율 모니터링 후 임계값 calibration 가능.

### 2.2 (c) empty 0건의 원인

`chunk.py` 가 빈 단락 skip (dead branch). 명세 §3.W4-Q-15 "(c) 룰의 임계값 사용자 confirm 게이트" 는 dry-run 마킹 0건 → 자동 통과.

**의의**: 신규 파서 (DOCX/PPTX, W5+) 가 빈 슬라이드/빈 단락 청크 생성 시 자동 마킹 → 검색 노이즈 사전 차단.

### 2.3 가시성 보강 (G-2 P3 처리)

W4 Day 4 ship — `run_chunk_filter_stage` 의 stage 로그에 추가:
- `empty=N` 카운트 노출 (기존 table_noise · header_footer 와 동일 형식)
- 마킹 비율 > 5% 시 WARNING 로그 — false positive risk early signal

코드 검증 (단위 테스트 19건 PASS):
```
chunk_filter: doc=d1 마킹 비율 50.0% > 5% — false positive risk 검토 필요
```

## 3. R-W3-4 정성 검토 게이트

**Skip** — 마킹 대상 0건이므로 정성 검토 불요. 본 적용 (--execute) 도 변화 없음.

## 4. AC 매트릭스

| AC | 결과 | 충족 |
|---|---|---|
| (b)·(c) 룰 ship + 단위 테스트 6건 PASS | 19건 PASS (기존 13 + 신규 6) | ✅ |
| 백필 dry-run 리포트 (doc 별 breakdown) | 본 문서 + 마킹 0건 | ✅ |
| R-W3-4 정성 검토 게이트 | Skip — 마킹 0건 | ✅ |
| golden 20건 회귀 top-3 hit 율 ≥ baseline | Day 5 통합 시 측정 | ⏳ |
| 백필 마킹 비율 < 5% | 0% < 5% | ✅ |

## 5. DE-66 결정

| 결정 | 채택 | 사유 |
|---|---|---|
| **DE-66 — 노이즈 룰 (b) 임계값** | (a) **3회 / 100자 유지** | dry-run 마킹 0건 → 임계값 적정 (false positive 0). 사용자 자산 누적 후 (W6+) 재calibration. |

## 6. 한 문장 요약

W4-Q-15 (b)·(c) 룰 백필 dry-run — 추가 마킹 **0건** (기존 chunks 분포에서 헤더/푸터/빈 청크 없음), 인프라 차원 ship 으로 향후 DOCX/PPTX 등 신규 파서 도입 시 자동 적용 + chunk_filter 가시성 보강 (empty 카운트 + 5% WARNING).
