# 2026-05-20 세션 종합 — D1 멀티유저 Auth 구현 + QA 통과 + P1#1 차단

> 본 세션 = D1(Auth 인프라) sprint 완수. plan v0.1 작성 → 4건 설계 결정 → Phase A 백엔드 → 아키텍처 B 정밀 명세 → Phase B 프론트+쿠키 → senior-qa 검증 → P1#1 IDOR 차단.
> 시작 HEAD `c754204` → 마감 시점 HEAD `c754204` (commit 0, **전부 작업 트리 미커밋**, 사용자 명시 요청 시 commit).
> 단위 테스트 1229 → **1298 OK** (+69, failures 7 = 환경 flaky 동일).
> 마스터 plan: `work-log/2026-05-20 D1 멀티유저 Auth plan.md` v1.0 (§1~§12).

---

## 0. 핵심 요약

본 세션은 멀티유저 D1 (Auth 인프라) sprint 를 단일 세션에 완수했다. 이전 세션(2026-05-19) 핸드오프 §5.4 D1 사전 의사결정 위에서 senior-planner 가 plan v0.1 을 작성, 사용자가 설계 결정 4건(JWT 검증·토큰 저장·초대 게이트·email 인증)을 응답, senior-developer 가 Phase A(백엔드) 와 Phase B(프론트+쿠키 보정) 를 순차 ship, senior-qa 가 검증(P0=0건) 후 발견한 P1#1 per-doc IDOR 을 senior-developer 가 추가로 닫았다.

**무중단 절대 준수** — `JETRAG_AUTH_ENABLED=false` default 라 production live(https://jetrag.woong-s.com) 무영향. 프론트도 Supabase ENV 미설정 시 graceful 통과. 본인 가입 → 018 이관 → ENV `true` 전환 단계마다 독립 rollback.

총 6 sprint(plan / Phase A / 아키텍처 B 결정 / Phase B / QA / P1#1 차단), 단위 테스트 +69(35+23+11), 신규 파일 백엔드 ~10·프론트 ~13. **커밋 0 (사용자 요청 후 일괄 commit 예정)**.

---

## 1. 진행 흐름 (시간 순)

### 1.1 plan v0.1 작성 (senior-planner)
- 입력: 이전 세션 §5.4 사전 의사결정 5건 + 현 코드 직접 탐색(`api/app/`·`web/src/`·마이그 016 → 다음 017).
- 핵심 통찰 = **D1 은 생각보다 가볍다**. `user_id` 컬럼이 이미 documents/answer_feedback/answer_ragas_evals 에 존재(F2), RPC 가 `user_id_arg` 격리 이미 지원(F3), 백엔드가 `default_user_id` 단일 지점 사용(F1) → "하드코딩 → JWT current_user 치환" + 초대 코드 게이트 + 프론트 로그인 UI 가 작업 전부.
- 산출: 10건 미결 결정 표(§9) + 작업량 6~10h 추정.

### 1.2 설계 결정 4건 (사용자)
- D1-Q1/Q2 = **PyJWT 추가 + 로컬 HS256** (P95 게이트 보호)
- D1-Q4 = **httpOnly 쿠키** (@supabase/ssr)
- D1-Q5 = **가입 직후 검증** (signUp → redeem-invite)
- D1-Q6 = **Email confirm OFF** (즉시 가입)
- 나머지 5건 = planner 추천값 자동 채택(auth_enabled=false default / admin OWNER ENV / chunks.user_id 미추가 / 30개 SQL seed / RLS 정책 D2).

### 1.3 Phase A 백엔드 (senior-developer)
- 신규: `api/app/auth/`(jwt_verify, dependencies, `CurrentUserDep` 패턴), `api/app/routers/auth.py`(POST /auth/redeem-invite), 마이그 017·018, 테스트 +35.
- 회귀 보존 핵심 = `Annotated[CurrentUser, Depends(get_current_user)] + = LEGACY_DEFAULT_USER` — FastAPI 경유 시 dep 실행, 직접 호출 시 default → **기존 1229 테스트 0줄 수정**.
- 정정 2건: ① work-log 의 failures=3 은 환경 차이, 실제 baseline=**7** / ② plan §6 의 vision_usage_log·search_metrics_log 는 **user_id 컬럼 없음**(005 주석), 018 대상은 documents/answer_feedback/answer_ragas_evals 3개.
- 결과: 1229 → 1264 OK (+35).

### 1.4 아키텍처 B 정밀 명세 (httpOnly 함정 발견 → 사용자 재결정)
- 프론트 확인 결과 = Next 16.2.4·React 19, @supabase/ssr 미설치, middleware/route handler 없음. API 호출이 RSC(`page.tsx await`) + client 양쪽에서 `jetrag-api.woong-s.com` 직접 fetch.
- 발견 = **분리 서브도메인 + 표준 @supabase/ssr browser client 패턴은 쿠키 JS-readable → httpOnly 무효화**. 3 옵션(A BFF 프록시 / B 크로스 서브도메인 쿠키 / C Bearer) 비교.
- 사용자 결정 = **B (크로스 서브도메인 `.woong-s.com` 쿠키 + credentials)**. P95 홉 추가 없이 실질 httpOnly.
- 정밀 명세: 세션 서버 관리(server action/route handler) + 데이터 콜은 credentials:'include' 직접 호출 + 백엔드는 Authorization 헤더 + 쿠키 양쪽 추출.

### 1.5 Phase B 프론트 + 백엔드 쿠키 보정 (senior-developer)
- senior-developer 가 Next 16 정식 컨벤션 발견 = `middleware.ts` deprecated → **`proxy.ts`**. node_modules/next/dist/docs 직접 확인.
- 신규(프론트): `proxy.ts`, `web/src/lib/supabase/{server,middleware-client}.ts`, `web/src/lib/auth/{actions,session,auth-context}.{ts,tsx}`, `web/src/lib/api/server-token.ts`, `web/src/app/{login,signup}/*`, `web/src/app/auth/{callback,auth-error}/*`. 수정: `api/client.ts`(server: Bearer forward / client: credentials), `index.ts` 4 fetch 통합, `layout.tsx`, `header.tsx`.
- 신규(백엔드): `api/app/auth/cookie_token.py`(@supabase/ssr 0.10.3 형식 정확 파싱: project-ref + base64-prefix + 청크 결합), `api/app/routers/auth.py:GET /auth/me`(복귀 유저 판별).
- 수정: `main.py` CORS `allow_credentials=True` + 명시 origin / `dependencies.py` 헤더→쿠키 fallback.
- 함정 회피 = Turbopack 정적 트레이싱이 동적 import 도 client 번들로 끌어 빌드 실패 → **resolver 주입 패턴**(server.ts 로드 시 setServerTokenResolver 1회) 으로 server-only 의존성 0 + 동작 동일.
- OAuth 초대 게이트 = pending-invite httpOnly 쿠키 보관 → 콜백에서 redeem(신규) / 쿠키 없으면 /auth/me authorized 판정(복귀 유저 통과·신규 코드없음 차단).
- 결과: 1264 → 1287 OK (+23). 프론트 lint·build PASS.

### 1.6 senior-qa 검증 (조건부 PASS)
- 회귀 / 무중단 / 인증 정합 / 초대 코드 / 데이터 이관 경계 / 보안 / KPI #10 전부 PASS.
- 발견 = P1#1 per-doc IDOR (6 핸들러 + reingest 데이터 파괴), P1#2 stats 전역 count, P2#1 테스트 경로 표기, P2#2 미승인 세션 찰나 잔류.
- ship 권고 = D1 인프라 즉시 commit 가능, 단 D2 RLS 완료 전 `auth_enabled=true` 베타 공개 금지.

### 1.7 P1#1 차단 (사용자 결정 = 지금 닫기)
- 사용자가 "D1 격리 원칙 완결" 선택. senior-developer 가 6 핸들러(reingest/reingest_missing_vision/get_document/get_document_status/list_active_documents/batch_status) 에 app-layer ownership 검증 추가.
- 패턴: 단일 query `.eq("user_id", caller)` 또는 fetch 후 비교 → 404(존재 위장) / batch_status 는 부분 응답(미소유 제외).
- 프론트 영향 확인 = `use-docs-batch-polling.ts:78-79` Record 매핑이라 누락 doc_id 자연 처리.
- 결과: 1287 → 1298 OK (+11). failures 7 동일. OpenAPI shape 무변경. 직격 회귀 게이트 28건 OK.

---

## 2. 누적 변경 (전부 작업 트리, 커밋 0)

### 백엔드 (신규)
- `api/app/auth/__init__.py`, `jwt_verify.py`, `dependencies.py`, `cookie_token.py`
- `api/app/routers/auth.py` (POST /auth/redeem-invite + GET /auth/me)
- `api/migrations/017_invite_codes.sql`, `018_migrate_default_user.sql`
- 테스트: `test_auth_jwt.py`, `test_auth_protected_routes.py`, `test_invite_codes.py`, `test_admin_gate.py`, `test_cookie_token.py`, `test_documents_per_doc_isolation.py`

### 백엔드 (수정)
- `api/app/config.py` (auth_enabled / supabase_jwt_secret / supabase_jwt_algorithm / owner_user_id)
- `api/app/main.py` (CORS allow_credentials=True + auth_router include)
- `api/app/routers/{documents,search,answer,stats,admin}.py` (보호 적용 + current_user 주입 + per-doc ownership[P1#1])
- `api/pyproject.toml` + `uv.lock` (PyJWT 2.12.1)
- `.env.example`, `api/migrations/README.md`

### 프론트 (신규)
- `web/src/proxy.ts` (Next 16 정식)
- `web/src/lib/supabase/{server,middleware-client}.ts`
- `web/src/lib/auth/{actions.ts, session.ts, auth-context.tsx}`
- `web/src/lib/api/server-token.ts`
- `web/src/app/login/{page,login-form}.tsx`
- `web/src/app/signup/{page,signup-form}.tsx`
- `web/src/app/auth/callback/route.ts`, `auth/auth-error/page.tsx`

### 프론트 (수정)
- `web/src/lib/api/{client,index}.ts` (server: Bearer / client: credentials)
- `web/src/app/layout.tsx` (async + AuthProvider)
- `web/src/components/jet-rag/header.tsx` (로그인 상태/로그아웃)
- `web/package.json` + `pnpm-lock.yaml` (@supabase/ssr 0.10.3)
- `web/.env.example` (NEXT_PUBLIC_SITE_URL, NEXT_PUBLIC_COOKIE_DOMAIN)

---

## 3. 테스트·검증 진전

| 시점 | 백엔드 단위 테스트 | failures(환경 flaky) | 비고 |
|---|---:|---:|---|
| 본 세션 시작 (HEAD `c754204`) | 1229 | 7 | (work-log 의 3건 기록은 환경 차이) |
| Phase A 종료 | 1264 | 7 | +35 신규, 회귀 0 |
| Phase B 종료 | 1287 | 7 | +23 신규(cookie 12 + jwt 보강 5 + me 4 등) |
| P1#1 차단 종료 | **1298** | 7 | +11 신규(per-doc isolation) |
| failures 7 환경 셋 | `test_embed_cache`×4 + `test_synonym_inject`×1 + `test_vision_caption_propagation`×2 | — | 본 세션 unchanged |

프론트: lint 0 error / build PASS / 14 route + Proxy 인식.

---

## 4. ship 순서 (사용자 작업 — 무중단)

plan §9 + senior-qa 권고 종합:

1. **commit + push** (사용자 명시 요청 시) — 작업 트리 → main 또는 feature 브랜치.
2. **Vercel ENV 추가**: `NEXT_PUBLIC_SITE_URL=https://jetrag.woong-s.com`, `NEXT_PUBLIC_COOKIE_DOMAIN=.woong-s.com` (선행 점 필수).
3. **Supabase dashboard**: Email confirm **OFF** / Google provider 활성화(Google Cloud OAuth Client) / Redirect URLs 에 `https://jetrag.woong-s.com/auth/callback` + `http://localhost:3001/auth/callback`.
4. **Railway ENV**: `SUPABASE_JWT_SECRET`(dashboard 에서 복사), `JETRAG_CORS_ORIGINS=https://jetrag.woong-s.com,https://jetrag.vercel.app` (기존 유지). `OWNER_USER_ID`·`JETRAG_AUTH_ENABLED`는 단계 후행.
5. **프론트·백엔드 ship** — `auth_enabled=false` 라 무인증 통과(현 동작 보존).
6. **본인 가입** (https://jetrag.woong-s.com/signup, 초대 코드 1개를 미리 마이그 017 seed 로 활성화) → Supabase auth.users 의 본인 UUID 확인.
7. **018 이관 실행** (Supabase SQL Editor): `:owner` 본인 UUID·`:legacy` `00000000-0000-0000-0000-000000000001` 치환, documents/answer_feedback/answer_ragas_evals 3 테이블 UPDATE.
8. **Railway ENV**: `OWNER_USER_ID=본인 UUID` 등록 + `JETRAG_AUTH_ENABLED=true` 전환 → redeploy.
9. **smoke**: 본인 토큰 로그인 → /search·/answer 정상 / 무토큰 → 401 / curl http://… `/auth/redeem-invite`(이미 소진) → 409.
10. **베타 30명 초대 코드 발행**: 마이그 017 seed BETA-01~30 placeholder 를 추측 불가 랜덤 문자열로 교체 후 INSERT.

> **⚠️ P1#1 닫혔지만 D2 RLS 미완 = `auth_enabled=true` 다중유저 베타 공개는 D2 ship 후 진행 권장**. P1#2(stats 전역 count) 가 D2 의존(chunks user_id 없음). 본인 단독 운영은 안전.

---

## 5. 다음 sprint 후보 (멀티유저 D2~D7)

| Sprint | 내용 | 작업량 | 트리거 |
|---|---|---|---|
| **D2** RLS 정책 | documents/chunks(doc_id 서브쿼리)/answer_feedback/jobs 등 RLS auth.uid() 정책 + Storage prefix per-user | 3~5h | D1 ship 후 |
| **D3** per-user rate limit + 비용 cap | 글로벌 cap → per-user 분리, 일일 query 카운트 | 4~6h | D2 후 |
| **D4** 법적/약관 | Privacy/ToS 페이지, 데이터 삭제 요청, GDPR-ish | 2~3h | D3 후 |
| **D5** 결제 (Q8 deferred) | 베타 5~10명 1주 안정화 후 결정 (무료 / cap / 구독) | — | Q8 답변 시 |
| **D6** Vercel Pro 전환 (Q11) | 라이선스 정합 (수익 발생 시) | — | Q8 의존 |
| **D7** Supabase Pro 전환 | 비용 cap·DB connection 한계 도달 시 | — | 베타 확장 시 |

병행 후보:
- woong-s.com root **포트폴리오 landing 페이지** (이직 어필 ★★★★, 4~8h)
- 데모 GIF / 검색 결과 스크린샷 (README 10/10 도달, 1~2h)
- M3 sprint (BIAS-1/UX-1 — 2026-05-19 §5.7 트래킹)

---

## 6. 미응답 의사결정 (이전 세션 인계 — 변동 없음)

| Q | 항목 | 상태 |
|---|---|---|
| Q1 | 도메인 | ✅ 해소 (woong-s.com) |
| Q8 | 결제 모델 | ⏸ deferred (베타 후) |
| Q9 | 공개 범위 + Auth provider | ✅ 해소 (초대 코드 + Email/Google) |
| Q10 | 공개 시점 | ⏸ Q8 의존 |
| Q11 | Vercel 라이선스 | ⏸ Q8 의존 |

본 세션 D1-Q1~Q10 (plan §9) 전부 해소.

---

## 7. 다른 컴퓨터 / 새 세션 진입

```bash
git clone https://github.com/woongminKi/Jet-Rag.git
cd Jet-Rag
git log --oneline -1
# 기대: c754204 (커밋 전 — 본 세션 변경분은 작업 트리에만)
```

⚠️ **본 세션 변경분이 작업 트리에만 있음** — 다른 컴퓨터에서 이어가려면 사용자가 먼저 본 세션 끝에 commit + push 요청해야 함.

작업 트리 확인:
```bash
git status   # 신규 파일 ~13 백엔드 + ~13 프론트, 수정 다수
```

---

## 8. 회고

### 8.1 ROI 가장 높은 결정
- **F1~F4 사실 발견 (plan §2)** = D1 작업량 추정 시 "이미 user_id 컬럼 있음 + RPC 격리 있음 + default_user_id 단일 지점" 을 코드 탐색으로 잡아 작업 범위를 정확히 가두었다. 추측이었으면 chunks user_id 추가 + RPC 수정 + 인제스트 코드 수정 같은 큰 작업이 D1 에 끌려들어왔을 것.
- **`Annotated + LEGACY_DEFAULT_USER` 패턴** = 기존 1229 테스트 0줄 수정. 회귀 보존을 production 코드의 방어 분기로 해결하지 않고 dependency 계약 자체로 처리.

### 8.2 미리 못 본 함정 → 학습
- **@supabase/ssr 표준 패턴 ≠ httpOnly** = 사용자가 "httpOnly 쿠키" 를 단순 결정(D1-Q4) 으로 답했지만, 분리 서브도메인 + browser client 패턴이 사실 JS-readable 임을 프론트 코드 탐색 후 발견. 즉시 3 옵션 비교(A BFF / B 크로스 서브도메인 / C Bearer) 로 재결정. **결정 → 구현 → 함정 발견 → 재결정 → 정밀 명세** 의 사이클이 plan 후행 단계에서 발생했다. 향후 plan 작성 시 라이브러리의 "표준 패턴" 의 실제 의미를 한 단계 더 검증.
- **Turbopack 정적 트레이싱 = 동적 import 도 client 번들로 끌고 옴** = Phase B 1차 시도에서 빌드 실패. resolver 주입 패턴으로 우회. Next 16 + Turbopack 의 함정.
- **Next 16 = `middleware.ts` deprecated → `proxy.ts`** = AGENTS.md "this is NOT the Next.js you know" 가 정확. node_modules/next/dist/docs 직접 확인이 답.

### 8.3 작업 분담
- plan: senior-planner (1 dispatch)
- 구현: senior-developer (3 dispatch — Phase A / Phase B / P1#1)
- 검증: senior-qa (1 dispatch)
- 메타·문서: Claude (plan 저장·수정, work-log)
- dashboard·결제·DNS: 사용자

각 dispatch 가 단일 책임 + 검증 가능한 산출물(테스트 실측 출력 인용) 을 가졌고, 본인은 dispatch 결과를 검토·통합·다음 dispatch 의 브리프 작성에 집중.

---

## 9. 참조

- 마스터 plan: `work-log/2026-05-20 D1 멀티유저 Auth plan.md` v1.0 (§1~§12)
- 이전 세션: `work-log/2026-05-19 세션 종합 — v1.5 W-1+W-2 + 도메인 + README 갱신.md`
- 멀티유저 sprint 전체 그림: `MEMORY.md` `project_remaining_sprints.md` 항목

## 10. production URLs (live, D1 default OFF — 변동 없음)

- https://jetrag.woong-s.com — frontend (현 무인증 통과, ENV true 전환 후 인증 필수)
- https://jetrag-api.woong-s.com — backend (DeepInfra embedding, P1#1 닫힘)
- https://jetrag.vercel.app — frontend fallback
- https://jet-rag-production.up.railway.app — backend fallback
