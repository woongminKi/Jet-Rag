# 2026-04-29 W3 Day 5 진입 핸드오프 — 종합 정리 + 다음 컴퓨터·내일 이어가기

> 오늘 (2026-04-29) Phase 2~4 + 마이그레이션 004 적용까지 마감 후 9 commit push 완료.
> 본 문서만 읽으면 W3 Day 5 (HWPX heading + golden 평가셋 + DoD) 진입 가능.
>
> **자매 문서**:
> - `2026-04-28 W3 진입 핸드오프.md` — Day 1 ship 후 핸드오프 (어제)
> - `2026-04-29 W3 스프린트 명세 v0.5.md` — Option Z CONFIRMED (오늘 채택)
> - `2026-04-29 W3 Day 2 마감 — Phase 2~4 ship.md` — Phase 별 결과 인덱스
> - `2026-04-29 청킹 정책 검토.md` — W4-Q-14 입력
> - `2026-04-30 chunk 품질 진단 리포트.md` — G(1) 결과
> - `2026-04-29 W3 Day 2 — search 라우터 재작성.md` — Day 2 #1 (Phase 1) 결과

---

## 0. TL;DR

오늘 하루에 **W3 Day 2 #1 + Phase 2 + Phase 3 + Phase 4 + Day 4 까지 모두 ship + push**. v0.5 (Option Z) 채택, 결함 3건 인정·redirect 후 명세 일정 1d 단축. **Day 5 (HWPX heading + golden 자료 + DoD) 진입 직전**.

**최종 상태 (2026-04-29 EOD)**:
- v0.5 CONFIRMED (Option Z) — DE-60 (PGroonga) · DE-61 (KPI 정성화) · DE-62 (chunk 품질 G) **모두 RESOLVED**
- 마이그레이션 004 (PGroonga + chunks.flags) Studio 적용 완료
- DE-60 회복 라이브 검증 통과 — `q="판결"` sparse=21, `q="대법원 판결"` sparse=9 (직전 simple FTS 의 sparse=0 회복)
- chunk 진단 (G(1)) + 자동 필터링 룰 (G(3)) 적용 — 465 chunks 중 83건 (17.8%) `flags.filtered_reason='table_noise'` 마킹
- **57/57 unittest PASS** (기존 + 신규 33 케이스: bgem3_singleton 11 + search_user_isolation 4 + search_metrics 4 + search_503_retry_after 4 + chunk_filter 12 + url_gate 14 + pgroonga_migration 8)
- working tree clean, **9 commit push 완료** (origin/main = `dfa056a`)
- **새 발견**: PDF section_title 0% (KPI 미달) → W4-Q-17 (NEW) 이월

---

## 1. 코드 셋업 — 다른 컴퓨터에서 5분 안에 이어가기

### 1.1 저장소·경로

```bash
git clone https://github.com/woongminKi/Jet-Rag.git
cd Jet-Rag
git pull origin main  # 최신 dfa056a 까지 받음
```

### 1.2 환경변수

`api/.env`:
```
SUPABASE_URL=https://mpmtydudhojpukuuadrd.supabase.co
SUPABASE_KEY=...                  # anon key
SUPABASE_SERVICE_ROLE_KEY=...     # 백엔드는 service_role
SUPABASE_STORAGE_BUCKET=documents
GEMINI_API_KEY=...                # gemini-2.5-flash 무료
HF_API_TOKEN=...                  # BGE-M3 HF Inference
DEFAULT_USER_ID=00000000-0000-0000-0000-000000000001
```

`web/.env.local`:
```
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
```

### 1.3 의존성 동기화

```bash
cd api && uv sync
# W2~W4 누적 의존성 6종 (W3 v0.5 전체 추가 0):
#   trafilatura · python-hwpx · pyhwp · filetype · pillow · olefile
# PGroonga 는 Postgres extension (Python deps 정책 별개)

cd ../web && pnpm install
```

### 1.4 마이그레이션 상태

| # | 파일 | 적용 |
|---|---|---|
| 001 | `api/migrations/001_init.sql` | ✅ 초기 |
| 002 | `api/migrations/002_documents_received_ms.sql` | ✅ W2 Day 2 |
| 003 | `api/migrations/003_hybrid_search.sql` | ✅ 2026-04-28 — HNSW + simple fts + pg_trgm + RPC |
| **004** | `api/migrations/004_pgroonga_korean_fts.sql` | ✅ **2026-04-29** — PGroonga + chunks.flags + RPC 재작성 + search_sparse_only_pgroonga 신설 |
| 004-rollback | `api/migrations/004_rollback.sql` | (옵션) 적용 X — PGroonga 부작용 시 안전망 |

검증 SQL (적용 후):
```sql
SELECT 
  (SELECT extname FROM pg_extension WHERE extname = 'pgroonga') AS pgroonga_ext,
  (SELECT column_name FROM information_schema.columns WHERE table_name='chunks' AND column_name='flags') AS chunks_flags,
  (SELECT indexname FROM pg_indexes WHERE indexname='idx_chunks_text_pgroonga') AS pgroonga_idx,
  (SELECT proname FROM pg_proc WHERE proname IN ('search_hybrid_rrf', 'search_sparse_only_pgroonga'));
-- 모두 NULL 아님 = 적용 OK
```

새 컴퓨터에서 미적용 시:
- `api/migrations/004_pgroonga_korean_fts.sql` 본문을 Studio SQL Editor 에 붙여넣고 Run (MCP 가 read-only 라 DDL 차단)

### 1.5 서버 실행 + 동작 검증

```bash
cd api && uv run uvicorn app.main:app --reload   # port 8000
cd web && pnpm dev                                 # port 3001
```

검증:
```bash
# DE-60 회복 라이브 검증
curl -sG 'http://localhost:8000/search' --data-urlencode 'q=대법원 판결' --data-urlencode 'limit=3' | python3 -c "
import sys, json
d = json.load(sys.stdin)
print(f'total={d[\"total\"]} parsed={d[\"query_parsed\"]}')"
# 기대: total≥1, sparse_hits>0 (직전 simple FTS 시대엔 0)

# 단위 테스트 회귀 0
cd api && uv run python -m unittest discover tests
# 기대: 57 ran, OK
```

### 1.6 G(3) backfill 상태

이미 적용 완료 (chunks 465건 중 83건 마킹). 추가 인제스트 시 자동 적용 (chunk_filter 스테이지). 백필 재실행 안 해도 됨.

만약 마킹 되돌리고 싶으면:
```sql
-- 전체 마킹 삭제
UPDATE chunks SET flags = flags - 'filtered_reason'
WHERE flags ? 'filtered_reason';
```

### 1.7 Claude Code MCP

`.mcp.json` 의 `SUPABASE_ACCESS_TOKEN` (PAT) 세팅. **read-only 모드라 DDL (CREATE EXTENSION, ALTER TABLE 등) 은 차단** — Studio 직접 적용 패턴 유지.

---

## 2. 오늘 (2026-04-29) 작업 종합 — 시간 순

| 시각 | 마일스톤 |
|---|---|
| 오전 | W3 Day 2 #1 (Phase 1) — `/search` 라우터 재작성 (ilike → 하이브리드 RRF + 메타필터 + query_parsed) |
| 오전 | senior-qa 회귀 점검 — 결함 3건 발견 (정량 KPI 측정 불가 / simple FTS 한국어 sparse=0 / chunk 품질 미반영) |
| 오후 | 비판적 재검토 + 경로 재정의 — 통계적 power 부족 인정, PGroonga 가능성 검증 (Phase 0) |
| 오후 | senior-planner v0.5 (Option Z) 작성 + CONFIRMED — DE-60·61·62 신규 채택 |
| 오후 | Phase 2 ship — 5-1 (silent degradation 차단) + 8-7 (httpx 싱글톤) + 1-1 (격리 회귀 테스트) |
| 오후 | senior-qa Phase 2 재검증 — P0=0, P1 5건 (Phase 3 통합 가능) |
| 저녁 | Phase 3 ship — search_metrics ring buffer + /stats.search_slo + Phase 2 잔존 P1 통합 + D-1/D-2 |
| 저녁 | Phase 4a ship (코드) — 마이그레이션 004 SQL + RPC PGroonga 재작성 + sparse fallback 옵션 B + chunk 진단 도구 |
| 저녁 | Phase 4b — 사용자 직접 Studio 에서 마이그레이션 004 적용 |
| 저녁 | Phase 4c 라이브 검증 + chunk 진단 도구 실행 — DE-60 회복 통과 + section_title 0% 발견 |
| 저녁 | Day 4 ship — A-5 rollback SQL + C dedup (변경 없음 확인) + E DNS rebinding 강화 + G(3) chunk_filter + 백필 적용 |
| 밤 | senior-qa Phase 3+4+Day4 검증 — 모두 P0=0 |
| 밤 | 9 commit push 완료 (origin/main = `dfa056a`) |

---

## 3. v0.5 명세 핵심 — Option Z 채택 항목

### 3.1 결함 3건 인정

| # | 결함 | 대응 |
|---|---|---|
| A | 6~15 doc 환경에서 `Recall@10 ≥ 0.85` / `+5pp` 정량 KPI 통계적 측정 불가 (degenerate metric) | DE-61 — KPI 정성화 + future-proof 측정 인프라 (`search_metrics` ring buffer) |
| B | `simple` FTS 의 한국어 어절 AND 결합 → 자연어 쿼리 sparse=0 (Day 2 smoke 실증) | DE-60 — PGroonga 채택 (Phase 0 가용성 검증, Supabase 매니지드 3.2.5) |
| C | chunk 입력 품질 (PDF 표 노이즈 등) 이 검색 출력 품질의 진짜 bottleneck — 명세 미반영 | DE-62 — 항목 G 신규 (진단 도구 + 자동 필터링 룰 + 청킹 정책 검토) |

### 3.2 DE 매트릭스 (Day 4 ship 후)

| DE | 결정 | 상태 |
|---|---|---|
| DE-56 | pgvector HNSW | ✅ DONE (Day 1) |
| ~~DE-57 v0.4~~ | ~~simple FTS~~ | CLOSED — 결함 B |
| DE-58 | RRF k=60, w=1/1 | ✅ DONE (Day 1) |
| **DE-60 (NEW)** | PGroonga 채택 | ✅ **RESOLVED** — 마이그레이션 004 적용 + 라이브 검증 통과 |
| **DE-61 (NEW)** | KPI 정량 → 정성 + 측정 인프라 (W6+ 정량) | ✅ **RESOLVED** — search_metrics ring buffer ship |
| **DE-62 (NEW)** | chunk 품질 G | ✅ **RESOLVED** — G(1) 진단 + G(3) 자동 필터링 + 백필 apply |

### 3.3 §3.A AC 매트릭스 (Phase 4 senior-qa 평가)

| # | AC | 상태 |
|---|---|---|
| 1 | 마이그레이션 004 적용 + RPC 재작성 ship | ✅ RESOLVED |
| 2 | ~~`q="공사대금 합의해지"` sparse_hits > 0~~ | ⚠️ **REVISED** — chunks 데이터 부재로 AC 부적절. 데이터 존재 키워드 (`대법원 판결` sparse=9, `판결` sparse=21) 로 회복 검증 통과 |
| 3 | golden 20건 자연어 한국어 10건 sparse_hits > 0 평균 | ⏳ DEFERRED — Day 5 작업 |
| 4 | P95 < 500ms | ⚠️ PARTIAL — warm 480ms / cold start 1~2초 (R-W3-5 embedding cache W4) |
| 5 | 메타 필터 4종 회귀 0 | ✅ RESOLVED (코드 변경 0, 라우터 호출부 무변경) |
| 6 | `query_parsed` 응답 호환성 | ✅ RESOLVED (라이브 smoke 통과) |

### 3.4 사용자 결정 사항 (오늘 합의)

- **TokenBigram 유지** (TokenMecab 은 W4-Q-2 통합 검토) — chunks 측정 데이터 부족 + Mecab 활성화 비용 대비 효과 불확실
- **PGroonga `&@~` AND 시맨틱 유지** — 자연어 검색 정확도 우선
- **G(3) 임계값 강화 (≥ 0.9 / ≥ 0.7)** — false positive 차단, 직전 G(1) 의 0.7/0.5 보다 strict
- **section_title 0% → W4-Q-17 이월** — PDF heading 추출 강화는 별도 작업 (PyMuPDFParser 확장)
- **Day 6 (NEW)**: 사용자 비동기 정성 검토 — Day 5 ship 후 다음 날 본인 페이스로 검토 → confirm

---

## 4. 9 Commit 그래프 (오늘 push 분)

| # | Hash | Commit |
|---|---|---|
| 1 | `678b372` | `chore(adapters)`: BGE-M3 싱글톤 + transient/permanent 분류 helper |
| 2 | `10eac42` | `feat(api)`: search_metrics ring buffer (Day 2 #2 측정 인프라) |
| 3 | `38b44b3` | `feat(api)`: /search 하이브리드 RRF + query_parsed + 503 정합 + 메트릭 hook |
| 4 | `9cd473a` | `feat(web)`: search/error.tsx — Next.js 16 Server Component 503 처리 |
| 5 | `da96744` | `feat(db)`: 004 PGroonga 한국어 FTS + chunks.flags + rollback 안전망 |
| 6 | `1209419` | `feat(ingest)`: G(3) chunk 노이즈 자동 필터링 룰 + ChunkRecord.flags + 백필 |
| 7 | `ce39b32` | `feat(api)`: _url_gate DNS rebinding 강화 + multi-IP round-robin 차단 |
| 8 | `918659b` | `chore(scripts)`: chunk 품질 진단 도구 (G(1)) |
| 9 | `dfa056a` | `docs(work-log)`: W3 Day 2~4 종합 + v0.5 명세 + 청킹 정책 검토 + chunk 진단 |

---

## 5. 잔존 이슈 (Phase 4 + Day 4 senior-qa 권고)

### 5.1 Day 5 진입 게이트 (라이브 smoke 의무 측정)

| ID | 항목 | Day 5 측정 |
|---|---|---|
| **B-5** | chunk_filter e2e — 신규 PDF 인제스트 시 chunks.flags 마킹 적용 검증 | PDF 1건 추가 업로드 → `SELECT flags FROM chunks WHERE doc_id=...` 확인 |
| **D-2** | DNS rebinding 엄격 정책의 CDN false positive | CDN URL 5종 (`cdn.jsdelivr.net`, `cloudflare.com` 등) 업로드 시 차단 여부 — 차단 시 임계값 완화 결정 |
| **G-4** | §3.F AC 평가셋 부재 — HWPX 자산 조달 | 합성 HWPX 1~2건 인제스트 또는 사용자 보유 자료 — 사용자 결정 필요 |

### 5.2 P2/P3 — W3 마감 전 또는 W4 이월

- **A-3** (P3): `004_rollback.sql` docstring 에 trouble-shooting 한 줄 추가 (DROP EXTENSION 의존 객체 fail 시)
- **A-3 임시 호환** (P3): `search_sparse_only_pgroonga` 가 simple FTS 사용하는 rollback 호환 RPC 의 이름 misleading — TODO 주석
- **B-2** (P3): `chunk_filter._classify_chunk` 가 빈 청크 (`text.strip() == ""`) → `filtered_reason='empty'` 마킹 (현재 None) — dead branch 라 우선순위 낮음
- **C-1** (P2): backfill dry-run 에 도큐별 breakdown 추가
- **C-2** (P2): 다른 자연어 키워드 (`쏘나타` `법률` `경제`) 의 검색 영향 측정 — Day 5 라이브 smoke 시 추가
- **C-4** (P2): backfill 마킹 되돌리기 SQL 을 docstring 에 명시 (UPDATE chunks SET flags = flags - 'filtered_reason')
- **D-4** (P3): IPv6 loopback (`::1`) 명시 unit test 추가
- **F-4** (P3): `search_metrics` ring buffer 의 003/004 sample 혼재 우려 (uvicorn 재시작 시 자연 reset)
- **G-1** (P2): `migrations/README.md` 에 `004_rollback` 행 추가
- **G-2** (P2): chunk_filter 의 metric/logging 부재 (search_metrics 수준의 가시성) — W4 작업
- **G-3** (P2): rollback 적용 시 검색 회귀의 운영자 결정 게이트 — docstring 강화

### 5.3 W4 이월 항목 (v0.5 §8)

| # | 항목 | 사유 |
|---|---|---|
| W4-Q-1 | RRF dense 우세 가중치 | AC 폐기로 보류 → W6+ |
| W4-Q-2 확장 | PGroonga 사용자 사전 + **TokenMecab 활성화 통합 검토** | Phase 4 결과 default tokenizer = TokenBigram, Mecab 효과는 사용자 자산 누적 (W6+) 후 측정 가능 |
| W4-Q-3 | embedding cache | cold start 1~2초 P95 위협 — 우선순위 ↑ |
| W4-Q-9 | DOCX/PPTX 파서 | v0.4 Option Y 의 W4 이월 |
| W4-Q-11 | BGE-M3 self-hosted sparse | PGroonga 도입으로 우선순위 하락 |
| W4-Q-12 | CrossEncoder rerank | W6+ 정량 측정 후 |
| **W4-Q-14** | 청킹 정책 본격 변경 | `2026-04-29 청킹 정책 검토.md` 의 약점 11건 → W4 5h 묶음 권장 |
| **W4-Q-15** | 노이즈 필터링 룰 (b)·(c) 추가 | Day 4 의 룰 (a) 후 사용자 정성 검토 결과 + W6+ 데이터 |
| **W4-Q-16** | `search_metrics` ring buffer → DB 영속화 | W6+ 사용자 자산 누적 시점 |
| **W4-Q-17 (NEW)** | **PDF heading 추출 강화** (PyMuPDFParser 의 section_title 채움) | Phase 4 진단 결과 PDF doc 4건 모두 section_title 0% — KPI §13.1 의 30% 절대 미달 |

---

## 6. Day 5 진입 즉시 시작 — 2026-04-30 (또는 다음 세션)

### 6.1 목표

(1) F HWPX/HWPML heading 분석 ship — `section_title ≥ 30%` KPI 충족 인프라.
(2) golden 20건 평가셋 자료 작성 — 사용자 Day 6 비동기 정성 검토 input.
(3) DoD 코드 측면 게이트 통과.
(4) W3 종합 정리 work-log.

### 6.2 작업 순서

| # | 액션 | 예상 |
|---|---|---|
| 1 | F HWPX/HWPML heading propagate — `adapters/impl/hwpx_parser.py`, `hwpml_parser.py` 의 paragraph_property → style → section_title | 1.5h |
| 2 | 합성 HWPX 1~2건 조달 + 인제스트 (사용자 협조 또는 GitHub 한국어 공공 HWPX 샘플) | 1h |
| 3 | section_title 채움 비율 측정 (재인제스트 후 chunks.flags 확인) | 30m |
| 4 | golden 20건 평가셋 마크다운 — 자연어 한국어 10 + 키워드 5 + 메타 혼합 5 의 검색 결과 top-3 + expected_doc 매칭 패턴 | 2h |
| 5 | Day 5 진입 전 게이트 5건 (B-5 chunk_filter e2e + D-2 CDN false positive + 5.2 의 P2 docstring 보강) | 1h |
| 6 | DoD 코드 측면 게이트 (`work-log/2026-05-02 W3 종합 정리.md`) + 9 commit push | 1h |

총 ~7h. 하루 작업 + Day 6 사용자 비동기 검토 분리.

### 6.3 핵심 결정점 (Day 5 진입 시 사용자 협조 필요)

1. **합성 HWPX 자료 조달** — 옵션:
   - A. 사용자가 보유한 HWPX 1~2건 직접 업로드
   - B. GitHub 의 한국어 공공 HWPX 샘플 (예: 정부 보도자료) 다운로드 후 인제스트
   - C. 합성 HWPX 생성 스크립트 작성 (pyhwpx 활용)

2. **golden 20건 쿼리 수집 방법** — 옵션:
   - A. 사용자가 페르소나 A 시나리오 따라 직접 작성 (가장 자연스러움)
   - B. 메인 스레드가 chunks 텍스트 분석해 합성 (Day 5 자동, 정성 측면 약화)
   - C. 어제 W3 명세 §5.4 의 예시 ("지난주 받은 사업계획서", "공사대금 합의해지", "PDF 화장품법") 변형

### 6.4 Day 5 마감 시 검증

```bash
# F 효과 측정
cd api && uv run python scripts/diagnose_chunk_quality.py | grep "section_title"
# 기대: section_title 채움 비율 ≥ 30% (HWPX 인제스트 후)

# golden 평가 자료 작성 확인
ls work-log/ | grep "golden"
# 기대: 2026-05-02 golden 평가 자료.md

# 단위 테스트 회귀
uv run python -m unittest discover tests
# 기대: 57+ ran, OK

# 라이브 smoke (P95 측정)
for q in "판결" "쏘나타" "법률" "경제" "GDP"; do
  curl -sG 'http://localhost:8000/search' --data-urlencode "q=$q" --data-urlencode 'limit=3' \
    | python3 -c "import sys,json;d=json.load(sys.stdin);print(f'{d[\"took_ms\"]}ms {d[\"query_parsed\"]}')"
done
curl -s 'http://localhost:8000/stats' | python3 -c "import sys,json;d=json.load(sys.stdin);print(d['search_slo'])"
# 기대: search_slo 의 p95_ms < 500ms (warm), fallback_breakdown 정상
```

---

## 7. 운영 정책 — 오늘 추가/변경분

### 7.1 에이전트 사용 의무 (CLAUDE.md `image2/CLAUDE.md` 신규 §)

오늘 추가됨 (사용자 강한 지적 후) — `image2/CLAUDE.md` 의 "공통 금지 사항" 다음에 신규 섹션:

> **모든 비-trivial 작업은 단계별로 적합한 서브에이전트를 거칠 것 — 직접 구현 금지**
> - 기획·명세 → `senior-planner`
> - 개발·구현 → `senior-developer`
> - 검증·QA → `senior-qa` (직접 수정 X, 리포트만)
> - 코드베이스 탐색 3 쿼리 이상 → `Explore`
> - 다단계 계획 → `Plan`
> - 중요 결정 → 다중 에이전트 교차검증
> - ship 전 검증 (`senior-qa`) 누락 금지
> - trivial 한정 직접 허용 (1~2줄·오타·메모리)

자동 메모리에도 저장됨 (`feedback_agents_mandatory.md`).

### 7.2 의존성 정책 정정

기존 "pure-Python 우선" 정책은 **Python 패키지 한정**. **Postgres extension (PGroonga 등) 은 별개**. Phase 0 검증 (Supabase 매니지드 3.2.5 가용) 후 명세 v0.5 의 DE-60 채택.

### 7.3 마이그레이션 적용 정책

MCP `apply_migration` / `execute_sql` 모두 **read-only 모드 강제** (`.mcp.json` 설정). DDL 은 Studio SQL Editor 직접 적용. 본 핸드오프 §1.4 의 검증 SQL 로 적용 완료 확인 가능.

### 7.4 senior-qa 의 P0 over-flag 패턴 — 비판적 재평가 의무

오늘 senior-qa 가 3차례 검증에서 P0 7~5건 분류했으나, 비판적 재평가 결과 모두 **진짜 P0 = 0건** 확인. 의도적 over-flag 라기보다는 senior-qa 의 보수적 prudence. 향후 senior-qa 결과 받으면 **항상 비판적 재평가** 후 P0 진위 판단할 것.

---

## 8. 참고 문서 (다음 작업자 권장 읽기 순서)

| # | 문서 | 목적 |
|---|---|---|
| 1 | `2026-04-22 개인 지식 에이전트 기획서 v0.1.md` | 마스터 기획서 (페르소나·KPI·아키텍처) |
| 2 | `2026-04-29 W3 스프린트 명세 v0.5.md` (CONFIRMED) | 본 명세가 v0.4 대체 |
| 3 | `2026-04-29 W3 Day 5 진입 핸드오프.md` (본 문서) | 다른 컴퓨터·다음 세션 진입 |
| 4 | `2026-04-29 W3 Day 2 마감 — Phase 2~4 ship.md` | Phase 별 결과 인덱스 |
| 5 | `2026-04-29 청킹 정책 검토.md` | W4-Q-14 결정 입력 |
| 6 | `2026-04-30 chunk 품질 진단 리포트.md` | G(1) 결과 + W4-Q-15·17 입력 |
| 7 | `api/migrations/README.md` | 003·004 인덱스 |
| 8 | `web/AGENTS.md` | "This is NOT the Next.js you know" — Next.js 16 신 API 주의 (특히 search/error.tsx 의 v16.2.0 신 API 시그니처) |

---

## 9. 한 문장 요약

오늘 (2026-04-29) W3 Day 2 #1 ship 후 senior-qa 결함 3건 발견 → **v0.5 (Option Z) 채택 + Phase 2~4 + Day 4 까지 같은 날 안에 마감 + 9 commit push**, **DE-60·61·62 모두 RESOLVED**, 일정 1d 단축. **다음 = Day 5 (HWPX heading + golden 평가 자료 + DoD)**, 다음 사용자 협조 필요 항목: 합성 HWPX 자료 조달 방법 + golden 20건 쿼리 수집 방법.
