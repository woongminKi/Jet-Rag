# M1 진입 — W-1 게이트 검증 (paid query decomposition 발화율·FP·비용)

> 프로젝트: Jet-Rag / 작성: 2026-05-13 / PRD: `work-log/2026-05-12 검색 정확도 80% 달성 PRD.md` v1.2 §5 M1 (선행 검증)
> **목적**: M1 의 W-1(a) = "`query_decomposer` 를 `/search` 옵션으로 노출 + `JETRAG_PAID_DECOMPOSITION_ENABLED` 게이트" (paid LLM query decomposition). **paid 호출 노출 전에** 현 golden_v2 182 row 에 대해 게이트가 얼마나 발화하는지(=paid 호출 빈도)·FP·FN·비용을 dry-run 측정 — PRD §3 W-1(a) 의 비용·발화율 가정 검증 + W-1(a) 착수 조건 확정.
> **paid 호출 0** (intent_router 룰 기반만 실행, decompose 미진입 — LLM 직전 mock). DB 조회 0. senior-qa 검증 (2026-05-13).

---

## 0. 한 줄 요약

- **발화율 = 20/182 = 11.0%** (cross_doc qtype **9/9** 발화, FN 0 / 단일-doc FP **11/173 = 6.4%**) — handoff §11.3·PRD §3 W-1(a) 의 "~11% 발화율" 그대로.
- **비용 = eval 1회당 ~$0.0005~0.0012** (per-firing ~$0.000026, retry factor 1) — PRD 의 "~$0.005/eval" 은 **4~10배 보수적 과대**. paid 우려 거의 없음. 월 cap $0.30 → ~250~600 eval 분량.
- **게이트 = `needs_decomposition` AND ENV `JETRAG_PAID_DECOMPOSITION_ENABLED` 두 개뿐** — `answer.py` `_LOW_CONFIDENCE_THRESHOLD=0.75` 는 meta 마킹 전용 (docstring "D3 에서 confidence 조합으로 호출 결정 예정"은 **미구현**). → **W-1(a) spec 에 "게이트에 confidence 임계 끼워넣지 말 것" 명시** (끼우면 conf 0.85 인 cross_doc 3 row(G-U-017/031/G-A-127) 막혀 cross_doc 발화 9→6 회귀).
- 결론: **W-1(a) 진입 가능** — 단 ① 단일-doc 회귀는 W-1(a) 본 구현 후 eval 재측정으로 재검증 필수 ② latency: 11% 발화는 PRD 의 "~5%, p95 무영향" 가정보다 2배 → `/search` 응답에 `decomposition_fired` meta 노출 + decompose LLM 호출 timeout 가드 권고 ③ P2 의 함정(sub-query 검색이 여전히 전-doc 풀 → cross_doc 라벨 doc-bound 매칭에 안 맞을 수 있음)을 W-1(a) eval 에서 가장 먼저 확인.

---

## 1. 게이트 로직 (코드 확인 — W-1(a) 가 `/search` 로 옮길 것)

`/answer` (`api/app/routers/answer.py` L460~492) → `query_decomposer.decompose()` 가 **실제 LLM 호출**하려면 순서대로 전부 통과:
1. **`router_decision.needs_decomposition == True`** — `intent_router._decide_decomposition` 산식 = `(T1 or T2 or T3 or T7) or (T5 and T6)`. (cross_doc P1 의 보조 regex `_T1_CROSS_DOC_PAIR`/`PAIR2`/`PLURAL`·`_DOC_NOUN`·`_T2_COMPARE_STEM` 포함.) — **이게 False 면 즉시 skip, LLM 0.**
2. **ENV `JETRAG_PAID_DECOMPOSITION_ENABLED ∈ {true,1,yes,on}`** — `decompose()` `_is_enabled()`. (본 측정은 ON 가정.)
3. **LRU cache miss** — module-level in-process OrderedDict(200건), key = `(query.lower().strip(), triggered_signals)`. **프로세스 재시작 시 휘발** (마이그 016 의 영구 embed-cache 와 달리 persist 안 함 — eval 재현 시 비용 0 보장 안 됨).
4. **budget cap 미초과** — `vision_usage_log` `source_type='query_decomposition'` SUM > `JETRAG_DECOMPOSITION_MONTHLY_CAP_USD`(default $0.30) 면 skip. DB 부재·실패는 graceful(allowed).
5. **LLM 호출** → JSON parse → **subquery 2~5개여야 채택** (`2 ≤ len ≤ 5` 아니면 빈 tuple = no-op). 1개·6개+·파싱 실패·LLM raise 면 cost 0 (단 LLM raise 시 실 API 비용은 발생, `_record_usage` 안 됨).
- **retry**: `_gemini_common._MAX_ATTEMPTS = int(ENV JETRAG_GEMINI_RETRY, "1")` = **기본 1 (재시도 없음)**. → retry factor ≈ 1.
- ※ `_LOW_CONFIDENCE_THRESHOLD=0.75` (`answer.py` L52) 는 `answer_meta["low_confidence"]` 마킹에만 쓰임. `decompose()` 호출 전 confidence 비교 분기 **없음** (L472 무조건 호출). → 실 게이트 = needs_decomposition + ENV.

## 2. 발화 결과 (182 row, ENV ON 가정)

| 항목 | 결과 |
|---|---|
| 전체 발화 (N_fire) | **20 / 182 = 11.0 %** |
| cross_doc qtype 발화 | **9 / 9** (FN 0 — P1 상태 유지) |
| 단일-doc/비-cross_doc FP | **11 / 173 = 6.4 %** |
| cross_doc 미발화 (FN) | **0** |

handoff §11.3 ("cross_doc 9/9 + 단일-doc FP 11/174") 와 동일 — G-U-027 제거·G-U-018 재작성은 발화 집합에 영향 없음(둘 다 발화 안 하던 row).

**cross_doc 9 row** (전부 T1): G-U-015(T1,T2 conf 0.70) / G-U-017(T1 0.85) / G-U-031(T1 0.85) / G-U-032(T1,T2 0.70) / G-A-124(T1,T2 0.70) / G-A-125(T1,T2,T5 0.55) / G-A-126(T1,T2 0.70) / G-A-127(T1 0.85) / G-A-128(T1,T5 0.70). → conf 0.55~0.85 (게이트 무관이나, confidence 임계를 추가하면 0.85 인 3 row 막힘 — 추가 금지).

**단일-doc FP 11 row** (id·qtype·trigger): G-U-028(out_of_scope·T1 PLURAL "자료들에" — out_of_scope 라 decompose 결과 0건, 무해) / G-A-020(exact_fact·T3 "원인") / G-A-042(exact_fact·T7 "효력…과 경과") / G-A-053(exact_fact·T1 PAIR "행사 규정" — "규정"이 `_DOC_NOUN` 화이트리스트라 오발화) / G-A-059(exact_fact·T3 "이유") / G-A-061(exact_fact·T3 "이유") / G-A-075(exact_fact·T2 "차이"+T3 "원인" — 금성/지구 비교, decompose 가 도움될 수도) / G-A-093(exact_fact·T3 causal) / G-A-096(exact_fact·T7 "배경과 성과") / G-A-097(exact_fact·T7 "내용과 성과") / G-A-107(table_lookup·T2 "달라" — 변경점 질문, 비교 아님 — 오발화). → handoff 11/174 = 6.3% 와 변동 없음.

## 3. 비용 추정 (PRD §3 W-1(a) "~$0.005/eval" 평가)

`_estimate_cost_usd` (char/4 ≈ token 근사): system 388자 + user `질문: {q}\nJSON:` ≈ 410~450자 → input ~105~113 tok / output(`_LLM_MAX_OUTPUT_TOKENS=200`, 실 3 subquery ~100~200자) ~25~50 tok / gemini-2.5-flash-lite $0.10·$0.40 per 1M → **~$0.000026 / firing** (retry 1).

| 기준 | per-firing | × 20 firing (eval 1회) |
|---|---|---|
| PRD §3 가정 | ~$0.0002 | ~$0.004 (PRD 본문 "~$0.005") |
| 코드 `_estimate_cost_usd` | ~$0.000026 | **~$0.0005** |
| 보수(한국어 token ≈ char/4 ×1.5~2) | ~$0.00004~0.00006 | ~$0.0008~0.0012 |

→ PRD "~$0.005/eval" = **4~10배 보수적 과대** (안전 측, 그대로 두어도 무방). 실 = eval 1회 **$0.0005~0.0012**. "게이트 통과 = LLM 호출" 거의 1:1 (cache cold, budget 여유) — 단 subquery 채택(`2≤len≤5`)은 LLM 품질 의존이라 본 dry-run 으로 예측 불가.

## 4. latency (PRD "전체 ~5%, p95 무영향" 검증)

발화 11% > PRD 가정 5% → **이론상 p95 에 닿을 수 있음** (cross_doc 5% + FP 6% 의 decompose query 가 +1~2초 추정 — sub-query 별 추가 RPC(top_k=10×N_sub) + RRF merge). 실 측정 필요. → W-1(a) 구현 시: ① 응답에 `decomposition_fired`/`decomposed_subqueries`/`decomposition_cost_usd` meta (`/answer` 와 동일 schema) 노출 — eval 에서 latency 분리·비용 집계 ② decompose LLM 호출에 짧은 timeout 가드 (현 `with_retry` 에 timeout 없음, Gemini SDK 기본 의존).

## 5. W-1(a) 진입 권고 / 착수 전 확인 사항

**진입 가능 — 조건부.** 비용 무문제(eval 1회 ~$0.001), cross_doc 9/9·FN 0, FP 6.4% 수용 가능(전부 단일-doc, decompose 가 원본 top_k=20 > sub top_k=10 RRF merge 라 단일-doc 회귀 위험 낮음 — 단 **W-1(a) 본 구현 후 eval 재측정으로 단일-doc 회귀 0 재검증 필수**).

착수 전 확인:
1. **게이트 = needs_decomposition AND ENV 두 개뿐 — confidence 임계 끼워넣지 말 것** (spec 명시). `answer.py` `_LOW_CONFIDENCE_THRESHOLD` 은 meta 전용 dead-ish, docstring 미구현.
2. `/search` 는 이미 `intent_router.route()` 를 2회 호출(`_is_cross_doc_query` MMR / `_is_cross_doc_class_query` chunk-cap). W-1(a) 추가 시 3회 안 되게 **decision 1회 산출 후 재사용** 리팩토링 권고 (룰 기반이라 비용 0 이나 위생).
3. cache 영속성 없음 (in-process LRU 200). eval 재현성 위해 영구화할지 결정 — MVP 범위에서는 in-process 유지 무난 (eval 비용 $0.001 라 영구화 ROI 낮음).
4. subquery 채택률·cross_doc R@10 변동·단일-doc 회귀·latency 분포 = **W-1(a) 본 구현 직후 `JETRAG_PAID_DECOMPOSITION_ENABLED=true` 로 eval 1회(~$0.001)** 돌려 한 번에 측정. 본 게이트 검증은 그 사전 단계로 완료.
5. **P2 함정 재확인** — P2 ($0 기계분해)가 net-negative 였던 이유 = doc-scoped 검색이 진짜 doc-RRF 아님. paid LLM decompose 도 sub-query 별 검색이 여전히 전-doc 풀(`_DECOMP_TOP_K_PER_SUB=10` 글로벌)이라 cross_doc 라벨 doc-bound 매칭에 안 맞을 가능성 — W-1(a) eval 에서 cross_doc R@10 이 실제로 0.4424→상승하는지 가장 먼저 확인. 안 오르면 (b) doc-scoped RPC 마이그로 fallback (DECISION-2).

## 6. 작업 메모

- dry-run 명령은 `python3 -c "..."` 임시 (커밋 안 함). 재사용 가치 낮음 (5줄) — 필요 시 `evals/dryrun_decomp_gate.py` 로 만들 수 있으나 미생성.
- 코드 무변경 / golden_v2.csv 무변경 / DB 무변경 / LLM·외부 API 호출 0.
- 다음 = W-1(a) 구현 (paid — DECISION-3 한도 ~$0.5 안, 사용자 사전 통지) + W-2 S4-D 동의어 후보 사전 ($0, 구현만 — 효과 측정은 M2 재인제스트 후).

---

## 7. W-1(a) 구현 (코드 변경 — senior-developer)

PRD v1.2 §3 W-1(a) — paid LLM query decomposition 을 `/search` 옵션으로 노출. 게이트 검증(§1~§6)에서 확정한 설계대로 구현. **커밋 전 (senior-qa 검증 대기).**

### 7.1 변경 파일

| 파일 | 한 일 | 동작 변화 |
|---|---|---|
| `api/app/services/multi_query_search.py` (**신규**) | `rrf_merge_pools(pools, *, k)` + `DECOMP_TOP_K_ORIGINAL=20` / `DECOMP_TOP_K_PER_SUB=10` 상수. 순수 함수 모듈 — DB/외부 API/라우터 의존 0 (import 순환 회피). | — |
| `api/app/routers/answer.py` | `_rrf_merge_pools` 정의 삭제 → `multi_query_search.rrf_merge_pools` 를 alias import (`_rrf_merge_pools` 이름 유지). `_DECOMP_TOP_K_ORIGINAL`/`_DECOMP_TOP_K_PER_SUB` 도 alias import. | **0 (순수 리팩토링)** — alias 라 기존 호출부·테스트 불변 |
| `api/app/services/query_decomposer.py` | `is_enabled()` public wrapper 1줄 (`_is_enabled()` 그대로) + `__all__` 추가. | 0 (기능 추가만) |
| `api/app/routers/search.py` | ① 상단 `router_decision = intent_router.route(clean_q)` **1회** 산출. `_is_cross_doc_query`/`_is_cross_doc_class_query` 에 optional `decision=` 인자 추가 → 호출부에서 재사용 (route() 3회→1회). ② `_decompose_with_timeout(clean_q, router_decision)` — 게이트(`needs_decomposition` AND `is_enabled()`) 미충족 시 스레드 0·즉시 빈 결과. 충족 시 `ThreadPoolExecutor(max_workers=1)` + `future.result(timeout=N)` + `shutdown(wait=False)`. timeout/예외 graceful. ③ `mode=="hybrid"` 일 때만 decompose 호출 (ablation `dense`/`sparse` 시 skip). ④ subqueries 비면: `rpc_rows[:20]` original pool + sub-query 당 `_fetch_subquery_pool()`(top_k=10, HyDE OFF, embed-cache snapshot 무시, 항상 graceful) → `multi_query_search.rrf_merge_pools(k=60)` → `rpc_rows` 대체 (이후 chunk cap·MMR·enrich 불변). ⑤ `SearchResponse.meta` 에 `decomposition_fired`/`decomposed_subqueries`/`decomposition_cost_usd`/`decomposition_cached` — RAG path 의 3개 return 전부 `meta=decomp_meta`. ENV OFF·미발화 시 `false`/`[]`/`0.0`/`false`. ⑥ `_DECOMPOSITION_TIMEOUT_SEC` = ENV `JETRAG_DECOMPOSITION_TIMEOUT_SEC` int, default 5, clamp [1,30]. | ENV OFF 시 **완전 불변** (스레드 0, 추가 RPC 0, meta 만 4키 기본값 dict 추가) |
| `api/tests/test_search_decomposition.py` (**신규**) | 8개 — (a) ENV OFF→미발화·meta 기본값·decompose 호출 0·추가 RPC 0 / (b) ENV ON+needs+subqueries 반환→merge(hybrid RPC 1+2=3회)·meta 반영 / (g-1) cached decomp→meta 전파 / (c) ENV ON+not needs→decompose 호출 0 / (d) ENV ON+빈 tuple→미발화·original pool only / (e-1) decompose raise→graceful / (e-2) decompose timeout(1s 강제)→graceful / (f) `intent_router.route()` 1회 호출. stdlib unittest+mock, paid $0. | — |
| `web/src/lib/api/types.ts` | `SearchMeta` interface (decomposition 4필드 + meta_fast path/matched_kind + `[key: string]: unknown`) + `SearchResponse.meta?: SearchMeta | null`. | — |
| `api/../.env.example` | `JETRAG_DECOMPOSITION_TIMEOUT_SEC=5` 주석 1줄 (M1 W-1(a) 블록, clamp [1,30]). | — |

`/answer` 는 `decomposition_fired` 등 **추가 안 받음** (현행 — `decomposed_subqueries`/`decomposition_cost_usd`/`decomposition_cached` 는 이미 answer_meta 에 있음). `mode=dense`/`mode=sparse` RPC split 경로 **무변경**.

### 7.2 게이트 재확인 (§1 대비 불변)

`/search` 의 decompose 게이트 = `router_decision.needs_decomposition`(`= (T1 or T2 or T3 or T7) or (T5 and T6)`) **AND** ENV `JETRAG_PAID_DECOMPOSITION_ENABLED ∈ {true,1,yes,on}` — **이 두 개뿐.** confidence 임계 미사용 (cross_doc conf 0.85 3 row 보호). `query_decomposer.decompose()` 내부 게이트(needs → ENV → LRU cache → budget cap → LLM → parse 2≤len≤5)는 그대로 — `/search` 의 `_decompose_with_timeout` 은 그 위에 (1) 게이트 선판정으로 스레드 자체를 안 만듦 + (2) timeout 가드만 추가.

### 7.3 ablation / doc_id scope 처리

- `mode=dense` / `mode=sparse` (W-9.5 BM25 ablation) → §0-b 에서 `if mode == "hybrid":` 가드로 `_decompose_with_timeout` 호출 자체를 skip. `decomp` 는 빈 결과 → merge 분기 미진입. meta 는 4키 기본값. **ablation RPC split 경로·응용 필터 전부 불변.**
- `doc_id` scope 지정 → decomposition 적용 (각 sub-query 풀에 `doc_id` 동일 필터). cross_doc 라벨이 doc-bound 인 P2 함정(§5.5)은 sub-query 풀이 여전히 전-doc 풀(글로벌 top_k=10)이라 잔존 가능 — W-1(a) eval 에서 cross_doc R@10 실측으로 확인 예정. 안 오르면 DECISION-2(doc-scoped RPC 마이그)로 fallback.

### 7.4 테스트

- 신규 `test_search_decomposition.py` **8개 전부 PASS** (2.4s — timeout 테스트 1s + slow decompose 1.5s 포함).
- 전체 `python3 -m unittest discover -s tests` → **1055 tests** (1047 → +8). 실패 4건 = `test_embed_cache.py` 의 사전부터 flaky (글로벌 LRU 싱글톤 누수, clean main 재현, 격리 시 9/9 PASS) — W-1(a) 무관, 그대로 둠. 그 외 신규/기존 실패 **0**. pytest 미설치 환경이라 unittest 로 실행 (테스트는 전부 `unittest.TestCase`).
- 관련 테스트 집중 실행 (answer/search/doc-rrf/cross-doc/reranker/cover-guard/snippet/user-iso/entity-boost/s4-a-breakdown ×14 파일 90 tests) → 전부 PASS.

### 7.5 사이드이펙트 점검

- [x] `multi_query_search` import 순환 없음 — `from __future__ import annotations` 만. `answer`/`search` 둘 다 이 모듈을 import 하지만 역방향 없음. `python -c "from app.routers import search, answer"` 정상.
- [x] `/answer` 동작 불변 — `_rrf_merge_pools` 는 동일 함수 객체를 alias import (`answer._rrf_merge_pools is multi_query_search.rrf_merge_pools` True). `_DECOMP_TOP_K_*` 값 동일(20/10). answer 테스트 전부 PASS.
- [x] `mode=dense`/`mode=sparse` ablation path 불변 — `if mode == "hybrid":` 가드로 decompose·merge 미진입. `used_split_rpc`·응용 필터 코드 무변경.
- [x] meta_fast_path 불변 — `_run_meta_fast_path` 의 `meta={...}` (path/matched_kind/tags/title_ilike/date_range) 무변경. decomp_meta 는 RAG path 의 3개 return 에만.
- [x] ENV OFF 시 `/search` 완전 불변 — 게이트 선판정으로 스레드 0, sub-query RPC 0. 유일 차이 = `meta` 가 `None` → `{decomposition_fired:false, decomposed_subqueries:[], decomposition_cost_usd:0.0, decomposition_cached:false}` dict (frontend `SearchMeta` 호환). 테스트 (a)로 검증.
- [x] route() 호출 1회 — 테스트 (f)로 검증 (decision 재사용).
- [x] 무료 티어 — ENV OFF default. ON 시 §3 비용 ~$0.000026/firing × ~20 firing = eval 1회 ~$0.0005~0.0012. Gemini 1,500회/일 한도 대비 무문제. budget cap (`JETRAG_DECOMPOSITION_MONTHLY_CAP_USD=$0.30`) 도 `decompose()` 내부에 그대로.
- [x] SLO 수신 <2초 — `/search` 는 검색 엔드포인트(인제스트 아님). decompose timeout default 5s 가 hybrid+발화 query 의 최악 추가 latency 상한. ENV OFF·미발화 시 0 추가.
- [x] 한국어 에러 메시지 — 신규 사용자 노출 에러 없음 (decompose 실패는 graceful skip, 로그 warning 만).

### 7.6 남은 이슈 / 다음 스코프

- **W-1(a) eval 1회 측정** (다음 스텝, paid ~$0.0005~0.0012) — `JETRAG_PAID_DECOMPOSITION_ENABLED=true` 로 golden_v2 182 row eval: ① subquery 채택률 (decompose 가 `2≤len≤5` 반환하는 비율 — LLM 품질 의존, dry-run 불가) ② cross_doc R@10 변동 (0.4424 → ?, P2 함정 §5.5 — 실제로 오르는지 가장 먼저 확인) ③ 단일-doc 회귀 0 재검증 (FP 11 row 의 R@10·top-1) ④ latency p95 분포 (`decomposition_fired` meta 로 firing query 만 분리). 안 오르면 DECISION-2(doc-scoped RPC 마이그)로 fallback.
- decompose LRU cache 영속성 — in-process 200건 유지 (eval 비용 $0.001 라 영구화 ROI 낮음, MVP 범위 무난). 영구화는 M2+ 재검토.
- timeout 시 orphaned worker 스레드 — `shutdown(wait=False)` 라 요청 latency 영향 0, decompose 자체 retry 한도(`JETRAG_GEMINI_RETRY` default 1)로 곧 종료. 단 `_gemini_common.with_retry` 가 Gemini SDK HTTP 호출에 명시적 timeout 을 안 걸어(SDK 기본 timeout 의존, 수십 초) worker 가 그만큼 살아있을 수 있음 — 사용자 latency 영향 0(timeout 가드가 응답 보호), 다발 시 스레드 풀 누수 모니터링은 v2(rq+Redis 이관) 시점 자연 해소. SDK 호출 timeout 부여는 P2 후속 (senior-qa P2-2).
- (senior-qa P2 권고) `multi_query_search.rrf_merge_pools` 전용 단위 테스트 부재 — 현재 `test_answer_router_decomposition`·`test_search_decomposition`(b)로 간접 커버만. 순수 추출 리팩토링이라 동작 불변이 거의 확실하나 `tests/services/test_multi_query_search.py` 신규로 (단일 풀 순서 보존 / 동일 chunk_id 다중 풀 점수 합산 우선 / 빈 chunk_id skip / `rrf_score = Σ 1/(k+rank+1)`) 추가하면 회귀 안전망 강화. P2.

---

## 8. W-1(a) eval 실측 결과 (golden_v2 182 row, paid 호출 발생)

> 측정: 2026-05-13 / 도구: `evals/run_s4_a_d4_breakdown_eval.py` (RRF-only baseline ENV `JETRAG_RERANKER_ENABLED=false`+`JETRAG_MMR_DISABLE=1`+embed-cache, `_BASELINE_ENV` 강제) — W-1(a) 의 `SearchResponse.meta` 4키(`decomposition_fired`/`decomposed_subqueries`/`decomposition_cost_usd`/`decomposition_cached`)를 캡처하도록 도구 확장 (CellResult 4필드 + GroupSummary 3필드 + markdown/raw json 추가, 표 컬럼 무변경).
> (A) OFF: `JETRAG_PAID_DECOMPOSITION_ENABLED` unset → `evals/results/s4_a_d4_w1a_off.{md,json}`. (B) ON: `=true` 만 추가 → `evals/results/s4_a_d4_w1a_decomp_on.{md,json}` (결과 파일 gitignored). 둘 다 전체 182 row.

### 8.1 도구 확장 (커밋 안 함 — senior-qa 검증 후)

- `evals/run_s4_a_d4_breakdown_eval.py` 만 수정:
  - `CellResult` +4필드 (`decomposition_fired`/`decomposed_subqueries`/`decomposition_cost_usd`/`decomposition_cached`, 전부 기본값) — `_make_cell` 키워드 명시라 기존 테스트 무영향.
  - `_measure_one_cell` — `resp.model_dump()` 직후 `data.get("meta")` 읽어 4필드 채움 (ENV OFF·미발화 시 false/[]/0.0/false).
  - `GroupSummary` +3필드 (`n_decomposition_fired`/`decomposition_total_cost_usd`/`decomposition_fired_ids`), `_aggregate_group` 가 cell 에서 집계.
  - `_format_markdown` — §1 overall 아래 1줄 `decomposition fired N/총 row (비용 $X.XXXX, ids: ...)` + §2 qtype 표 *아래에* 별도 줄로 `- \`qtype\` decomp fired n/m` (표 컬럼 추가 X — `split("|")` 카운트 깨짐 방지).
  - `_serialize_cells` per-cell 4필드 / `_summary_to_dict` 3필드 → raw json 자동 반영.
- 단위 테스트: `test_run_s4_a_d4_breakdown_w6` + `..._crossdoc_roundrobin` + `..._compose_off` **44/44 PASS** (변경 무영향). `_format_markdown` 스모크 — fired cell 1건 → "decomposition fired 1/2 row (비용 $0.0006, ids: ...)" + "- \`cross_doc\` decomp fired 1/1" 정상 출력.

### 8.2 (A) OFF 재현 — M0-c 와 **완전 일치** (W-1(a) OFF 경로 회귀 0)

| metric | (A) OFF (W-1(a) HEAD) | M0-c baseline (`76c607a`) | 일치 |
|---|---:|---:|:---:|
| R@10 | 0.6848 | 0.6848 | ✅ |
| top-1 | 0.7910 | 0.7910 | ✅ |
| nDCG@10 | 0.6310 | 0.6310 | ✅ |
| MRR | 0.6013 | 0.6013 | ✅ |
| n_chunk_evaluable | 177 | 177 | ✅ |
| doc_match_fail_zeroed / no_ground_truth | 0 / 5 | 0 / 5 | ✅ |
| cross_doc R@10 / top-1 | 0.4424 / 0.3333 | 0.4424 / 0.3333 | ✅ |
| decomposition_fired | **0/182** | (해당 없음) | — |

→ ENV OFF 시 `/search` 완전 불변 (decompose 스레드 0, sub-query RPC 0). W-1(a) 머지로 인한 OFF 경로 회귀 없음 확정.

### 8.3 (B) ON — overall (OFF vs ON vs M0-c)

| metric | (A) OFF | (B) ON | M0-c | ΔON−OFF |
|---|---:|---:|---:|---:|
| R@10 | 0.6848 | **0.6834** | 0.6848 | **−0.0014** |
| top-1 | 0.7910 | **0.7910** | 0.7910 | **0.0000** |
| nDCG@10 | 0.6310 | 0.6303 | 0.6310 | −0.0006 |
| MRR | 0.6013 | 0.6046 | 0.6013 | +0.0034 |
| n_chunk_evaluable | 177 | 177 | 177 | 0 |
| P95 latency (전체) | 245.5 ms | **2564.5 ms** | — | +2319 ms |
| avg latency (전체) | 191.8 ms | 502.2 ms | — | +310 ms |

→ overall 은 **거의 무변동** (R@10 −0.14%p, top-1 동일). decompose merge 가 단일-doc 다수 row 에 영향 없고 (게이트 미발화) 발화 20 row 에서도 평균적으로 ±0 수준. P95 는 발화 11% 때문에 크게 증가 (전체 P95 가 발화 query 의 latency 로 점프 — 8.6 참조).

### 8.4 cross_doc qtype — **R@10 소폭 하락** (P2 함정 확인됨)

| metric | (A) OFF | (B) ON | M1 목표 | 판정 |
|---|---:|---:|---:|:---:|
| cross_doc R@10 | 0.4424 | **0.4216** | ≥ 0.55 | **미달 (악화)** |
| cross_doc top-1 | 0.3333 | 0.3333 | — | 동일 |
| cross_doc decomp fired | 0/9 | **9/9** | — | 게이트 100% 발화 |

per-row (recall@10 OFF→ON, 9 row): **개선 2** (G-A-125 0.143→0.429, G-U-031 0.111→0.222) / **하락 2** (G-A-126 0.500→0.167, G-U-032 0.375→0.125) / **불변 5** (G-A-124 0.4, G-A-127 0.5, G-A-128 0.286, G-U-015 0.667, G-U-017 1.0 — 일부 nDCG 는 미세 변동 = merge 가 top-10 doc set 은 안 바꾸고 ranking 만 흔듦). 순변동 = (+0.2857+0.1111) − (0.3333+0.2500) = −0.1865 ÷ 9 = **−0.0207** = cross_doc R@10 Δ.

- ⚠ **per-row 인과는 "noise doc 끌어옴"으로 단정 못 함** — 개선 row(G-A-125 "예산 규모 비교", G-U-031 "쏘나타 데이터센터 비교")와 하락 row(G-A-126 "기웅민 이한주 역량 비교", G-U-032 "데이터 활용 방식 비교")가 **동일한 3-subquery 구조**(doc-specific 2 + generic "비교" 1, `s4_a_d4_w1a_decomp_on.json` cells 직접 확인). 즉 분해 *품질* 차이가 아니라, 원본 retrieval 이 이미 라벨 chunk 를 잘 잡던 row 는 글로벌 200-pool RRF 재배치로 라벨 chunk 가 밀리고, 원본이 나쁘던 row 는 sub-query 풀이 누락 chunk 를 보강 — **근본 원인 = sub-query 검색이 doc-scoped 가 아님**. (rrf_merge k=60 / sub top_k=10 이 cross_doc 2nd-doc chunk 를 top_k 안에 못 담는 등 더 싼 진단은 시도 안 함 — DECISION-2 가 W-1(b) doc-scoped RPC 우선이라 생략.)

**판정 = P2 함정 그대로** — W-1(a) §5.5·§7.3 에서 우려한 "sub-query 풀이 여전히 전-doc 글로벌 top_k 라 cross_doc 라벨 doc-bound 매칭에 안 맞음" 이 실증됨. paid LLM 으로 분해해도 RRF 가 doc-scoped 가 아니라 cross_doc R@10 **0.4424 → 0.4216 (−4.7%)** 로 오히려 악화. → **DECISION-2 (doc-scoped RPC 마이그 = W-1(b)) 가 필요** — W-1(a) 단독으로 M1 cross_doc 게이트(R@10 ≥ 0.55) 도달 불가.

### 8.5 decomposition 발화 실측 (dry-run 예측 vs 실제)

| 항목 | dry-run 예측 (§2~3) | 실측 (B) ON |
|---|---:|---:|
| fired N / 182 | 20 (11.0%) | **20 (11.0%)** — 일치 |
| fired ids | cross_doc 9 + 단일-doc 11 | cross_doc 9(G-A-124~128, G-U-015/017/031/032) + 단일-doc 11(G-A-020/042/053/059/061/075/093/096/097/107, G-U-028) — 일치 |
| subquery 채택 (2≤len≤5) | LLM 품질 의존, 예측 불가 | **20/20 (100%)** — 전부 채택, 길이 3개=18 row / 4개=2 row |
| cached | 0 (cold, in-process LRU) | **0** — eval 한 번이라 캐시 miss 100% |
| 총 비용 ($) | ~$0.0005~0.0012 | **$0.000329** (per-firing ~$0.0000165) — 예측 하한보다 더 쌈 |

→ 게이트 발화·subquery 채택 모두 dry-run 가정대로. **subquery 채택률 100%** (decompose LLM 이 항상 3~4개 반환) — "1개·6개+·파싱실패 = no-op" 케이스 0. 비용은 eval 1회 **$0.0003** 으로 PRD "~$0.005/eval" 대비 **15배 보수적 과대**, 월 cap $0.30 → ~900 eval 분량.

### 8.6 단일-doc 회귀 — **회귀 1 row** (목표 0, 미세 — noise level)

비-cross_doc + ground-truth 有 168 row 중 OFF→ON 에서 `recall_at_10` 또는 `top1_hit` 하락:

| id | qtype | fired | R@10 OFF→ON | top-1 OFF→ON | 원인 |
|---|---|:---:|---:|:---:|---|
| **G-A-093** | exact_fact | True | **0.857 → 0.714** | True → True | sub-query 분해가 추가 chunk 를 풀에 끌어와 1건 밀림 (top-1 은 유지 — answer 영향 0) |

- 개선 1 row: **G-A-059** exact_fact, R@10 0.727 → 0.818 (top-1 유지).
- 나머지 발화 18 row (FP 9 + cross_doc 일부) 전부 R@10·top-1 무변동.
- **FP 11 row OFF→ON 변동표** (전부 발화):

| id | qtype | R@10 OFF→ON | top-1 OFF→ON | latency ON |
|---|---|---:|:---:|---:|
| G-A-020 | exact_fact | 0.909 → 0.909 | True → True | 2632 ms |
| G-A-042 | exact_fact | 0.667 → 0.667 | True → True | 2937 ms |
| G-A-053 | exact_fact | 0.857 → 0.857 | True → True | 2405 ms |
| G-A-059 | exact_fact | 0.727 → **0.818** | True → True | 2550 ms |
| G-A-061 | exact_fact | 1.000 → 1.000 | True → True | **8659 ms** (HF/Gemini 변동) |
| G-A-075 | exact_fact | 0.667 → 0.667 | True → True | 2845 ms |
| G-A-093 | exact_fact | 0.857 → **0.714** | True → True | 2749 ms |
| G-A-096 | exact_fact | 0.625 → 0.625 | True → True | 2378 ms |
| G-A-097 | exact_fact | 1.000 → 1.000 | True → True | 2339 ms |
| G-A-107 | table_lookup | 0.750 → 0.750 | True → True | 2447 ms |
| G-U-028 | out_of_scope | (라벨 없음) | — | 2702 ms |

→ 회귀 = G-A-093 1건 (R@10 −0.143, n_eval 분모 168 → overall R@10 −0.0009 기여), top-1 회귀 0, answer 영향 0 (top-1 유지). **사실상 noise 수준** — "회귀 0" 엄밀 기준은 미충족이나 운영 영향 없음. M2 chunk augment 후 재측정 시 자연 해소 여부 확인 권고.

### 8.7 latency — 발화 query vs 미발화 query

| 그룹 | n | p50 | p95 | max |
|---|---:|---:|---:|---:|
| `decomposition_fired=True` | 20 | 2558 ms | **7081 ms** | 8659 ms |
| `decomposition_fired=False` | 162 | 178 ms | 231 ms | 276 ms |

- 발화 query 의 추가 latency = decompose LLM 1회 (~2초 warm, Gemini 호출) + sub-query 3~4개 × RPC(top_k=10) + RRF merge. p50 ~2.5초 = 거의 LLM 호출 시간.
- **p95 7.08초 / max 8.66초** — M1 목표 "firing query p95 ≤ 3초 (warm)" **미달**. cross_doc 의 G-U-015(6998ms)·G-A-128(3526ms), FP 의 G-A-061(8659ms) 이 꼬리. 콜드 스타트 / Gemini 변동성 일부 포함 가능하나, decompose LLM 동기 호출이 본질 비용. → timeout 가드(default 5초)가 있으나 5초 이내 응답도 SLO(검색 ~수백 ms 기대) 대비 큼. 발화 11% → 전체 P95 가 245ms → 2564ms 로 점프 (발화 query 가 상위 11% percentile 을 점유).

### 8.8 M1 게이트 판정 + W-1(b) 필요 여부

| M1 게이트 | 기준 | 실측 (B) ON | 판정 |
|---|---|---:|:---:|
| cross_doc R@10 | ≥ 0.55 | 0.4216 (OFF 0.4424 대비 −4.7%) | **미달 (악화)** |
| overall top-1 | ≥ 0.82 | 0.7910 | **미달** (W-1(a) 무관 — baseline 한계, M2 대상) |
| 단일-doc 회귀 | 0 | 1 row (G-A-093, R@10 −0.143, top-1 0) | **미달 (noise 수준)** |
| firing query p95 (warm) | ≤ 3초 | 7081 ms | **미달** |

**결론**: W-1(a) 단독으로 M1 게이트 4개 중 0개 통과. cross_doc R@10 은 paid 분해로 오히려 **하락** — W-1(a) §5.5·§7.3 에서 명시한 **P2 함정(sub-query 풀이 doc-scoped 가 아님)이 실증됨**. → **DECISION-2 (W-1(b) doc-scoped RPC 마이그) 진행 필수**. W-1(a) 의 paid decompose 자체는 cross_doc 3 row 개선(G-A-125/G-U-031/+동률)·11 row 무변동·1 row 하락이라 net-zero~약간 음(-) → W-1(b) 가 doc-scoped RPC 로 sub-query 풀을 라벨 doc 에 묶지 않으면 W-1(a) ENV 는 **default OFF 유지**가 합리적 (PRD DECISION-2 의 fallback 경로 그대로). latency p95 7초도 W-1(b) 와 함께 (a) sub-query 병렬 RPC (b) decompose 결과 영구 캐시 검토 필요.

### 8.9 남은 이슈 / 다음 스코프

- **W-1(b) = DECISION-2 doc-scoped RPC 마이그** (다음 스텝) — cross_doc 라벨이 `(alias, chunk_idx)` doc-bound 인데 sub-query 풀이 글로벌 top_k 라 noise doc 가 라벨 doc 의 RRF rank 를 밀어냄. sub-query 별로 "라벨 후보 doc set 내" RPC 를 돌리거나, sub-query 풀을 doc 단위로 RRF 한 뒤 doc-balanced merge (eval `_round_robin_cross_doc_chunks` 와 대칭) — 마이그 새 번호 SQL 필요. W-1(b) 후 동일 도구 재실행으로 cross_doc R@10 재측정.
  - **+ US-08 doc-scope query 신규 회귀 테스트 작성** (PRD §5 "W-1(b)-회귀 메모") — W-1(b) 가 `/search` 의 `doc_id` 스코프 경로(US-08 "이 문서 안에서만")를 회귀시킬 수 있는데 golden_v2 에 doc-scope query 0건 → 회귀 테스트 케이스 추가 필요. M1 게이트 "회귀 0" 에 doc-scope 경로 포함.
- **W-1(a) ENV default** — W-1(b) 없이는 cross_doc 악화·latency p95 7초·회귀 1 row 라 `JETRAG_PAID_DECOMPOSITION_ENABLED` **default OFF 유지** 권고. W-1(b) 로 cross_doc R@10 ≥ 0.55 + 회귀 0 확인 후 ON 검토.
- **latency p95 7초** — decompose LLM 동기 호출이 본질. (a) sub-query RPC 병렬화 (현 순차) (b) decompose 결과 in-process LRU → 영구 캐시(마이그) (c) timeout default 5초 → 3초 단축 검토. W-1(b) 와 함께.
- **G-A-093 R@10 −0.143** — 단일-doc exact_fact 인데 decompose 가 끼어들어 1건 밀림. T3("causal") 오발화 (§2 FP 11 중 하나). FP 게이트 정밀화(intent_router T3 보수화)는 M2+ — 현재 top-1 영향 0 이라 보류.
- overall top-1 0.7910 (M1 게이트 0.82 미달) 은 W-1(a)/(b) 무관 — baseline 한계, M2 chunk augment + 전체 클린 재인제스트 대상 (PRD W-3/W-7).

## 9. W-1(b) 옵션 A 구현 + eval 측정

### 9.1 옵션 A 설계 요약 (마이그 0, search.py-only)

senior-planner 가 옵션 A(마이그 0, search.py-only)/B(additive 마이그 1)/C(doc_embedding RPC 마이그 2) 비교 후 **A 권고** — eval `_round_robin_cross_doc_chunks` 가 라벨 doc 만 평가하므로 production 측에서 "후보 doc set 식별 + 필터"만 해도 cross_doc R@10 자연 향상이 가설. A 가 R@10 ≥ 0.55 못 넘기면 B 로 상승.

핵심 로직:
1. `_select_cross_doc_candidates(rpc_rows, top_n)` 순수 함수 — doc 별 group → doc score = sum(top-3 chunk rrf_score) → score desc + doc_id 사전순 tie-break → 상위 N doc_id list.
2. 게이트 4조건 AND: `JETRAG_CROSS_DOC_SCOPED_SEARCH` ON + `mode=hybrid` + `doc_id is None` + `_is_cross_doc_class_query` (T1/T2/T7).
3. 게이트 통과 시 `rpc_rows = [r for r in rpc_rows if r["doc_id"] in candidate_set]` — 나머지 파이프라인 (chunk cap·MMR·enrich) 은 dict 만 보므로 불변.
4. 후보 빈 list (모든 doc score 0) → 환원 (`scoped_applied=False`, `top_n=0`).
5. meta 3키 추가: `cross_doc_scoped_applied` / `cross_doc_candidate_doc_ids` (정렬) / `cross_doc_candidate_top_n`.

### 9.2 변경 / 신규 파일

| 파일 | 변경 종류 | 한 일 |
|---|---|---|
| `api/app/routers/search.py` | 변경 | ENV 2개·상수 4개 추가 / 헬퍼 4개 (`_cross_doc_scoped_enabled` / `_cross_doc_candidate_top_n` / `_select_cross_doc_candidates` / `_search_rag_meta`) / 단계 2-a-3 필터 삽입 / 3개 RAG return 의 `meta=_search_rag_meta(...)` 교체 |
| `web/src/lib/api/types.ts` | 변경 | `SearchMeta` 에 W-1(b) 3 optional 키 추가 (`cross_doc_scoped_applied?` / `cross_doc_candidate_doc_ids?` / `cross_doc_candidate_top_n?`) |
| `.env.example` | 변경 | W-1(b) ENV 2개 블록 추가 (`JETRAG_CROSS_DOC_SCOPED_SEARCH=false` / `JETRAG_CROSS_DOC_CANDIDATE_TOP_N=4`) |
| `api/tests/test_search_cross_doc_scoped.py` | 신규 | T1~T12 = 12 케이스 (헬퍼 5 + e2e 게이트 7) — stdlib unittest + mock, paid $0 |
| `evals/run_s4_a_d4_breakdown_eval.py` | 변경 | `CellResult` +3필드 / `_measure_one_cell` graceful 캡처 / `GroupSummary` +1 / `_aggregate_group`·`_format_markdown` qtype 표 아래 1줄 / `_serialize_cells`·`_summary_to_dict` 반영. 순수 additive |
| `evals/results/s4_a_d4_w1b_off.{md,json}` | 신규 (gitignore) | A0 OFF baseline 실측 |
| `evals/results/s4_a_d4_w1b_only.{md,json}` | 신규 (gitignore) | A1 W-1(b) only ON 실측 |

### 9.3 사이드이펙트 점검

- **단위 테스트 1102 PASS (failures=4 모두 `test_embed_cache.py` 사전 flaky 무관)** — 기존 회귀 테스트 (`test_search_doc_id_filter.py` / `test_search_cross_doc_chunk_cap.py` / `test_search_decomposition.py`) 전부 PASS.
- ENV OFF default 라 `JETRAG_CROSS_DOC_SCOPED_SEARCH` 미설정 시 코드 path 완전 보존 — A0 OFF 가 M0-c run1 (commit `76c607a`) 과 cells 182 row predicted_top10·recall_at_10 **churn 0** 검증 (아래 §9.4).
- meta_fast_path return 은 게이트 ② mode=hybrid 후속이라 도달 안 함 → 영향 0.
- 단일-doc 경로 (doc_id 명시 + non-cross_doc query) 회귀 0 (게이트 ③④ 강하게 보장).

### 9.4 (A0) OFF baseline — M0-c 일치 검증

ENV 미설정 (`JETRAG_CROSS_DOC_SCOPED_SEARCH` unset / `JETRAG_PAID_DECOMPOSITION_ENABLED` unset) 으로 실행. M0-c run1 (`evals/results/s4_a_d4_m0c_run1.json`) 과 비교:

| 항목 | M0-c run1 | A0 OFF | 일치 |
|---|---:|---:|:---:|
| overall R@10 | 0.6848 | 0.6848 | OK |
| overall top-1 | 0.7910 | 0.7910 | OK |
| nDCG@10 | 0.6310 | 0.6310 | OK |
| MRR | 0.6013 | 0.6013 | OK |
| n_chunk_evaluable | 177 | 177 | OK |
| n_rows | 182 | 182 | OK |
| cells `predicted_top10` mismatch | — | 0 / 182 | churn 0 |
| cells `recall_at_10` mismatch | — | 0 / 182 | churn 0 |
| `cross_doc_scoped_applied=True` row | — | 0 / 182 | (의도) |

**A0 = M0-c 와 cells·overall metric 모두 완전 일치 — 회귀 0 확정**. 게이트 OFF 시 코드 path 보존 검증.

### 9.5 (A1) W-1(b) only ON — 주 측정 결과

ENV `JETRAG_CROSS_DOC_SCOPED_SEARCH=true` + `JETRAG_CROSS_DOC_CANDIDATE_TOP_N=4`, paid OFF.

| 항목 | A0 OFF | A1 ON | Δ |
|---|---:|---:|---:|
| overall R@10 | 0.6848 | 0.6830 | **−0.0018** |
| overall top-1 | 0.7910 | 0.7853 | **−0.0057** |
| nDCG@10 | 0.6310 | 0.6294 | −0.0016 |
| MRR | 0.6013 | 0.5995 | −0.0018 |
| n_chunk_evaluable | 177 | 177 | 0 |
| overall P95 (ms) | 239 | 232 | (−7 ms, 변동 범위 내) |
| cross_doc R@10 (n=9) | **0.4424** | **0.4080** | **−0.0344 = −7.8%** |
| cross_doc top-1 (n=9) | 0.3333 | 0.2222 | −0.1111 |
| cross_doc nDCG | 0.3213 | 0.2903 | −0.0310 |
| cross_doc P95 (ms) | 202 | 227 | +25 (변동 범위 내) |
| `scoped_applied` (cross_doc qtype) | 0/9 | **9/9** | (목표 9/9 달성) |
| `scoped_applied` (non-cross_doc) | 0 | 1 (G-U-028 out_of_scope, T2 발화 — metric 영향 0) | — |
| 단일-doc 회귀 (비-cross_doc + GT 有, n=168) | — | r@10↓ **0** / top1↓ **0** | (게이트 ③ PASS) |

#### 9.5.1 cross_doc 9 row per-row 변동표

| id | A0 R@10 | A1 R@10 | A0 top1 | A1 top1 | Δ R@10 | 변화 |
|---|---:|---:|:---:|:---:|---:|:---:|
| G-A-124 | 0.4000 | 0.4000 | F | F | +0.0000 | flat |
| G-A-125 | 0.1429 | 0.1429 | F | F | +0.0000 | flat |
| **G-A-126** | 0.5000 | 0.3333 | **T** | **F** | **−0.1667** | **DOWN** |
| G-A-127 | 0.5000 | 0.5000 | T | T | +0.0000 | flat |
| **G-A-128** | 0.2857 | 0.1429 | F | F | **−0.1429** | **DOWN** |
| G-U-015 | 0.6667 | 0.6667 | F | F | +0.0000 | flat |
| G-U-017 | 1.0000 | 1.0000 | T | T | +0.0000 | flat |
| G-U-031 | 0.1111 | 0.1111 | F | F | +0.0000 | flat |
| G-U-032 | 0.3750 | 0.3750 | F | F | +0.0000 | flat |

**UP 0 / DOWN 2 / flat 7** — 옵션 A 는 cross_doc 에서 한 row 도 못 살리고 2 row 를 더 떨어뜨림.

#### 9.5.2 DOWN row 원인 분석

- **G-A-126** ("기웅민 이력서와 이한주 포트폴리오 핵심 역량") — top-4 후보 doc 에 **두 라벨 doc(`기웅민이력서`·`이한주포폴`) 모두 미포함**. baseline rpc_rows 에서 라벨 doc 의 chunk RRF 가 noise doc 보다 약해 후보 산출 단계에서 잘림. 필터 후 라벨 doc chunk 가 응답에서 사라져 `_round_robin_cross_doc_chunks` 가 `이한주포폴` (acceptable_chunks 의 한쪽만 응답에 진입한 흔적) 만으로 채워짐. top-1 = `이한주포폴 c62`(non-relevant) → top1 hit T→F.
- **G-A-128** ("law sample2 와 law sample3 두 판결") — top-4 후보에 **`law3` 미포함**, `law2` 만 들어옴. predicted_top10 가 law2 only → law3 의 chunk_idx=13 (정답) 미회수, R@10 0.2857 → 0.1429.

**옵션 A 의 본질적 한계**: P2 함정의 변형 — sub-query 풀이 글로벌이라 noise 가 끼는 것이 아니라, **baseline 의 rpc_rows 자체에서 라벨 doc 의 chunk RRF 가 약하면 doc-level aggregation 으로도 후보 진입 못 함**. doc score = sum(top-3 chunk rrf_score) 산식이 noise doc 의 (강한 chunk 1개 + 약한 chunk 2개) 와 라벨 doc 의 (약한 chunk 1개 + 더 약한 chunk 2개) 를 비교했을 때 noise 가 이기는 케이스. 더 강한 doc representation (B = doc_embedding 가산 / C = doc_embedding RPC) 필요.

#### 9.5.3 latency 비교

| 그룹 | A0 OFF p50 | A0 p95 | A1 ON p50 | A1 p95 | Δ p95 |
|---|---:|---:|---:|---:|---:|
| cross_doc (n=9) | 176 ms | 202 ms | 183 ms | 227 ms | +25 ms |
| 기타 (n=173) | 188 ms | 241 ms | 183 ms | 231 ms | −10 ms |

옵션 A 의 추가 비용 = `_select_cross_doc_candidates` (in-app dict group-by) — 50 doc × 3 chunk 정도면 < 1 ms. cross_doc p95 +25ms 는 변동 범위 내 (run-to-run noise). 게이트 ④ "firing query p95 ≤ 3초" 강하게 통과.

### 9.6 M1 게이트 판정 + 옵션 B 필요 여부 + ENV default 권고

| M1 게이트 | 기준 | A1 ON 실측 | 판정 |
|---|---|---:|:---:|
| cross_doc R@10 | ≥ 0.55 | 0.4080 | **미달 (A0 대비 악화)** |
| overall top-1 | ≥ 0.82 | 0.7853 | **미달** (W-1 무관 — baseline 한계, M2 대상) |
| 단일-doc 회귀 | 0 | 0 | **PASS** |
| firing query p95 | ≤ 3초 | 227 ms | **PASS** |

**결론**: M1 게이트 4개 중 **2 PASS / 2 미달**. cross_doc R@10 < 0.50 → **옵션 B (additive 마이그 1, doc-level embedding 또는 doc score boost) 필요 신호 확정**. 옵션 A 는 라벨 doc 가 baseline rpc_rows 의 top-N 후보 안에 들어와야만 효과가 있는데, baseline 의 chunk-level RRF 가 noise doc 에 약한 케이스 (G-A-126·G-A-128) 에서는 doc-level aggregation 만으로 회복 불가. → DECISION-2 (옵션 B) 진행.

**ENV default 권고**:
- `JETRAG_CROSS_DOC_SCOPED_SEARCH` **default OFF 유지** — 단일-doc 경로 회귀는 0 이나 cross_doc 자체가 악화돼 ON 시 net-negative.
- `JETRAG_CROSS_DOC_CANDIDATE_TOP_N` default 4 권고치 유지 (사용 안 됨).
- W-1(b) 옵션 A 코드는 **보존** — 옵션 B 가 doc-level embedding 가산 위에 옵션 A 의 후보 doc 필터를 얹는 구조로 갈 가능성 있음 (의미 있는 doc score → 필터 효과 회복).

### 9.7 남은 이슈 / 다음 스코프

- **옵션 B = DECISION-2 doc-level embedding boost / RPC 마이그** — `documents.doc_embedding` (1024d, summary+implications 임베딩, 마이그 014) 이 이미 존재. `JETRAG_DOC_EMBEDDING_RRF=true` ENV 로 가산 path 가 있으나 baseline 에선 OFF. cross_doc query 한정 ON + 옵션 A 의 후보 doc 필터를 doc-RRF 점수 기반으로 (chunk-RRF 대신) 결정하는 옵션 B-1 부터 검토. 옵션 B-2 = additive 마이그 (doc 별 chunk RRF sum 을 RPC 내부에서 산출하는 view/RPC) — 마이그 1 추가. **다음 W-1(c) 진입 결정 필요** — 사용자 승인 후 senior-planner 호출.
- **G-U-028 false positive 발화 1건** — out_of_scope qtype 인데 intent_router T2 발화 (`이 자료들`). metric 영향 0 (라벨 0 row 라 분모 제외) 이지만 옵션 B 진입 시 false positive 발화 증가 위험 — intent_router 보수화 검토 (현재는 보류).
- **W-1(a) ENV default OFF 유지 결정 불변** — W-1(b) 옵션 A 도 default OFF, W-1(b) 옵션 B 가 cross_doc R@10 ≥ 0.55 만족 시 (a)(b) 조합 default ON 검토.
- **단위 테스트 1102** — 신규 12 (T1~T12) 추가로 1090→1102. 기존 4 flaky (test_embed_cache) 무관.
- **opt-in 운영 진단** — `cross_doc_scoped_applied` meta 키가 search 응답에 노출되므로 프론트 또는 운영 대시보드에서 cross_doc-class 발화 율 모니터링 가능. SearchMeta 타입에 optional 로 반영 완료.
