# M1 진입 — W-1 게이트 검증 (paid query decomposition 발화율·FP·비용)

> 프로젝트: Jet-Rag / 작성: 2026-05-13 / PRD: `work-log/2026-05-12 검색 정확도 80% 달성 PRD.md` v1.2 §5 M1 (선행 검증)
> **목적**: M1 의 W-1(a) = "`query_decomposer` 를 `/search` 옵션으로 노출 + `JETRAG_PAID_DECOMPOSITION_ENABLED` 게이트" (paid LLM query decomposition). **paid 호출 노출 전에** 현 golden_v2 182 row 에 대해 게이트가 얼마나 발화하는지(=paid 호출 빈도)·FP·FN·비용을 dry-run 측정 — PRD §3 W-1(a) 의 비용·발화율 가정 검증 + W-1(a) 착수 조건 확정.
> **paid 호출 0** (intent_router 룰 기반만 실행, decompose 미진입 — LLM 직전 mock). DB 조회 0. senior-qa 검증 (2026-05-13).

---

## 0. 한 줄 요약

- **발화율 = 20/182 = 11.0%** (cross_doc qtype **9/9** 발화, FN 0 / 단일-doc FP **11/173 = 6.4%**) — handoff §11.3·PRD §3 W-1(a) 의 "~11% 발화율" 그대로.
- **비용 = eval 1회당 ~$0.0005~0.0012** (per-firing ~$0.000026, retry factor 1) — PRD 의 "~$0.005/eval" 은 **4~10배 보수적 과대**. paid 우려 거의 없음. 월 cap $0.30 → ~250~600 eval 분량.
- **게이트 = `needs_decomposition` AND ENV `JETRAG_PAID_DECOMPOSITION_ENABLED` 두 개뿐** — `answer.py` `_LOW_CONFIDENCE_THRESHOLD=0.75` 는 meta 마킹 전용 (docstring "D3 에서 confidence 조합으로 호출 결정 예정"은 **미구현**). → **W-1(a) spec 에 "게이트에 confidence 임계 끼워넣지 말 것" 명시** (끼우면 conf 0.85 인 cross_doc 3 row(G-U-017/031/G-A-127) 막혀 cross_doc 발화 9→6 회귀).
- 결론: **W-1(a) 진입 가능** — 단 ① 단일-doc 회귀는 W-1(a) 본 구현 후 eval 재측정으로 재검증 필수 ② latency: 11% 발화는 PRD 의 "~5%, p95 무영향" 가정보다 2배 → `/search` 응답에 `decomposition_fired` meta 노출 + decompose LLM 호출 timeout 가드 권고 ③ P2 의 함정(sub-query 검색이 여전히 전-doc 풀 → cross_doc 라벨 doc-bound 매칭에 안 맞을 수 있음)을 W-1(a) eval 에서 가장 먼저 확인.

---

## 1. 게이트 로직 (코드 확인 — W-1(a) 가 `/search` 로 옮길 것)

`/answer` (`api/app/routers/answer.py` L460~492) → `query_decomposer.decompose()` 가 **실제 LLM 호출**하려면 순서대로 전부 통과:
1. **`router_decision.needs_decomposition == True`** — `intent_router._decide_decomposition` 산식 = `(T1 or T2 or T3 or T7) or (T5 and T6)`. (cross_doc P1 의 보조 regex `_T1_CROSS_DOC_PAIR`/`PAIR2`/`PLURAL`·`_DOC_NOUN`·`_T2_COMPARE_STEM` 포함.) — **이게 False 면 즉시 skip, LLM 0.**
2. **ENV `JETRAG_PAID_DECOMPOSITION_ENABLED ∈ {true,1,yes,on}`** — `decompose()` `_is_enabled()`. (본 측정은 ON 가정.)
3. **LRU cache miss** — module-level in-process OrderedDict(200건), key = `(query.lower().strip(), triggered_signals)`. **프로세스 재시작 시 휘발** (마이그 016 의 영구 embed-cache 와 달리 persist 안 함 — eval 재현 시 비용 0 보장 안 됨).
4. **budget cap 미초과** — `vision_usage_log` `source_type='query_decomposition'` SUM > `JETRAG_DECOMPOSITION_MONTHLY_CAP_USD`(default $0.30) 면 skip. DB 부재·실패는 graceful(allowed).
5. **LLM 호출** → JSON parse → **subquery 2~5개여야 채택** (`2 ≤ len ≤ 5` 아니면 빈 tuple = no-op). 1개·6개+·파싱 실패·LLM raise 면 cost 0 (단 LLM raise 시 실 API 비용은 발생, `_record_usage` 안 됨).
- **retry**: `_gemini_common._MAX_ATTEMPTS = int(ENV JETRAG_GEMINI_RETRY, "1")` = **기본 1 (재시도 없음)**. → retry factor ≈ 1.
- ※ `_LOW_CONFIDENCE_THRESHOLD=0.75` (`answer.py` L52) 는 `answer_meta["low_confidence"]` 마킹에만 쓰임. `decompose()` 호출 전 confidence 비교 분기 **없음** (L472 무조건 호출). → 실 게이트 = needs_decomposition + ENV.

## 2. 발화 결과 (182 row, ENV ON 가정)

| 항목 | 결과 |
|---|---|
| 전체 발화 (N_fire) | **20 / 182 = 11.0 %** |
| cross_doc qtype 발화 | **9 / 9** (FN 0 — P1 상태 유지) |
| 단일-doc/비-cross_doc FP | **11 / 173 = 6.4 %** |
| cross_doc 미발화 (FN) | **0** |

handoff §11.3 ("cross_doc 9/9 + 단일-doc FP 11/174") 와 동일 — G-U-027 제거·G-U-018 재작성은 발화 집합에 영향 없음(둘 다 발화 안 하던 row).

**cross_doc 9 row** (전부 T1): G-U-015(T1,T2 conf 0.70) / G-U-017(T1 0.85) / G-U-031(T1 0.85) / G-U-032(T1,T2 0.70) / G-A-124(T1,T2 0.70) / G-A-125(T1,T2,T5 0.55) / G-A-126(T1,T2 0.70) / G-A-127(T1 0.85) / G-A-128(T1,T5 0.70). → conf 0.55~0.85 (게이트 무관이나, confidence 임계를 추가하면 0.85 인 3 row 막힘 — 추가 금지).

**단일-doc FP 11 row** (id·qtype·trigger): G-U-028(out_of_scope·T1 PLURAL "자료들에" — out_of_scope 라 decompose 결과 0건, 무해) / G-A-020(exact_fact·T3 "원인") / G-A-042(exact_fact·T7 "효력…과 경과") / G-A-053(exact_fact·T1 PAIR "행사 규정" — "규정"이 `_DOC_NOUN` 화이트리스트라 오발화) / G-A-059(exact_fact·T3 "이유") / G-A-061(exact_fact·T3 "이유") / G-A-075(exact_fact·T2 "차이"+T3 "원인" — 금성/지구 비교, decompose 가 도움될 수도) / G-A-093(exact_fact·T3 causal) / G-A-096(exact_fact·T7 "배경과 성과") / G-A-097(exact_fact·T7 "내용과 성과") / G-A-107(table_lookup·T2 "달라" — 변경점 질문, 비교 아님 — 오발화). → handoff 11/174 = 6.3% 와 변동 없음.

## 3. 비용 추정 (PRD §3 W-1(a) "~$0.005/eval" 평가)

`_estimate_cost_usd` (char/4 ≈ token 근사): system 388자 + user `질문: {q}\nJSON:` ≈ 410~450자 → input ~105~113 tok / output(`_LLM_MAX_OUTPUT_TOKENS=200`, 실 3 subquery ~100~200자) ~25~50 tok / gemini-2.5-flash-lite $0.10·$0.40 per 1M → **~$0.000026 / firing** (retry 1).

| 기준 | per-firing | × 20 firing (eval 1회) |
|---|---|---|
| PRD §3 가정 | ~$0.0002 | ~$0.004 (PRD 본문 "~$0.005") |
| 코드 `_estimate_cost_usd` | ~$0.000026 | **~$0.0005** |
| 보수(한국어 token ≈ char/4 ×1.5~2) | ~$0.00004~0.00006 | ~$0.0008~0.0012 |

→ PRD "~$0.005/eval" = **4~10배 보수적 과대** (안전 측, 그대로 두어도 무방). 실 = eval 1회 **$0.0005~0.0012**. "게이트 통과 = LLM 호출" 거의 1:1 (cache cold, budget 여유) — 단 subquery 채택(`2≤len≤5`)은 LLM 품질 의존이라 본 dry-run 으로 예측 불가.

## 4. latency (PRD "전체 ~5%, p95 무영향" 검증)

발화 11% > PRD 가정 5% → **이론상 p95 에 닿을 수 있음** (cross_doc 5% + FP 6% 의 decompose query 가 +1~2초 추정 — sub-query 별 추가 RPC(top_k=10×N_sub) + RRF merge). 실 측정 필요. → W-1(a) 구현 시: ① 응답에 `decomposition_fired`/`decomposed_subqueries`/`decomposition_cost_usd` meta (`/answer` 와 동일 schema) 노출 — eval 에서 latency 분리·비용 집계 ② decompose LLM 호출에 짧은 timeout 가드 (현 `with_retry` 에 timeout 없음, Gemini SDK 기본 의존).

## 5. W-1(a) 진입 권고 / 착수 전 확인 사항

**진입 가능 — 조건부.** 비용 무문제(eval 1회 ~$0.001), cross_doc 9/9·FN 0, FP 6.4% 수용 가능(전부 단일-doc, decompose 가 원본 top_k=20 > sub top_k=10 RRF merge 라 단일-doc 회귀 위험 낮음 — 단 **W-1(a) 본 구현 후 eval 재측정으로 단일-doc 회귀 0 재검증 필수**).

착수 전 확인:
1. **게이트 = needs_decomposition AND ENV 두 개뿐 — confidence 임계 끼워넣지 말 것** (spec 명시). `answer.py` `_LOW_CONFIDENCE_THRESHOLD` 은 meta 전용 dead-ish, docstring 미구현.
2. `/search` 는 이미 `intent_router.route()` 를 2회 호출(`_is_cross_doc_query` MMR / `_is_cross_doc_class_query` chunk-cap). W-1(a) 추가 시 3회 안 되게 **decision 1회 산출 후 재사용** 리팩토링 권고 (룰 기반이라 비용 0 이나 위생).
3. cache 영속성 없음 (in-process LRU 200). eval 재현성 위해 영구화할지 결정 — MVP 범위에서는 in-process 유지 무난 (eval 비용 $0.001 라 영구화 ROI 낮음).
4. subquery 채택률·cross_doc R@10 변동·단일-doc 회귀·latency 분포 = **W-1(a) 본 구현 직후 `JETRAG_PAID_DECOMPOSITION_ENABLED=true` 로 eval 1회(~$0.001)** 돌려 한 번에 측정. 본 게이트 검증은 그 사전 단계로 완료.
5. **P2 함정 재확인** — P2 ($0 기계분해)가 net-negative 였던 이유 = doc-scoped 검색이 진짜 doc-RRF 아님. paid LLM decompose 도 sub-query 별 검색이 여전히 전-doc 풀(`_DECOMP_TOP_K_PER_SUB=10` 글로벌)이라 cross_doc 라벨 doc-bound 매칭에 안 맞을 가능성 — W-1(a) eval 에서 cross_doc R@10 이 실제로 0.4424→상승하는지 가장 먼저 확인. 안 오르면 (b) doc-scoped RPC 마이그로 fallback (DECISION-2).

## 6. 작업 메모

- dry-run 명령은 `python3 -c "..."` 임시 (커밋 안 함). 재사용 가치 낮음 (5줄) — 필요 시 `evals/dryrun_decomp_gate.py` 로 만들 수 있으나 미생성.
- 코드 무변경 / golden_v2.csv 무변경 / DB 무변경 / LLM·외부 API 호출 0.
- 다음 = W-1(a) 구현 (paid — DECISION-3 한도 ~$0.5 안, 사용자 사전 통지) + W-2 S4-D 동의어 후보 사전 ($0, 구현만 — 효과 측정은 M2 재인제스트 후).

---

## 7. W-1(a) 구현 (코드 변경 — senior-developer)

PRD v1.2 §3 W-1(a) — paid LLM query decomposition 을 `/search` 옵션으로 노출. 게이트 검증(§1~§6)에서 확정한 설계대로 구현. **커밋 전 (senior-qa 검증 대기).**

### 7.1 변경 파일

| 파일 | 한 일 | 동작 변화 |
|---|---|---|
| `api/app/services/multi_query_search.py` (**신규**) | `rrf_merge_pools(pools, *, k)` + `DECOMP_TOP_K_ORIGINAL=20` / `DECOMP_TOP_K_PER_SUB=10` 상수. 순수 함수 모듈 — DB/외부 API/라우터 의존 0 (import 순환 회피). | — |
| `api/app/routers/answer.py` | `_rrf_merge_pools` 정의 삭제 → `multi_query_search.rrf_merge_pools` 를 alias import (`_rrf_merge_pools` 이름 유지). `_DECOMP_TOP_K_ORIGINAL`/`_DECOMP_TOP_K_PER_SUB` 도 alias import. | **0 (순수 리팩토링)** — alias 라 기존 호출부·테스트 불변 |
| `api/app/services/query_decomposer.py` | `is_enabled()` public wrapper 1줄 (`_is_enabled()` 그대로) + `__all__` 추가. | 0 (기능 추가만) |
| `api/app/routers/search.py` | ① 상단 `router_decision = intent_router.route(clean_q)` **1회** 산출. `_is_cross_doc_query`/`_is_cross_doc_class_query` 에 optional `decision=` 인자 추가 → 호출부에서 재사용 (route() 3회→1회). ② `_decompose_with_timeout(clean_q, router_decision)` — 게이트(`needs_decomposition` AND `is_enabled()`) 미충족 시 스레드 0·즉시 빈 결과. 충족 시 `ThreadPoolExecutor(max_workers=1)` + `future.result(timeout=N)` + `shutdown(wait=False)`. timeout/예외 graceful. ③ `mode=="hybrid"` 일 때만 decompose 호출 (ablation `dense`/`sparse` 시 skip). ④ subqueries 비면: `rpc_rows[:20]` original pool + sub-query 당 `_fetch_subquery_pool()`(top_k=10, HyDE OFF, embed-cache snapshot 무시, 항상 graceful) → `multi_query_search.rrf_merge_pools(k=60)` → `rpc_rows` 대체 (이후 chunk cap·MMR·enrich 불변). ⑤ `SearchResponse.meta` 에 `decomposition_fired`/`decomposed_subqueries`/`decomposition_cost_usd`/`decomposition_cached` — RAG path 의 3개 return 전부 `meta=decomp_meta`. ENV OFF·미발화 시 `false`/`[]`/`0.0`/`false`. ⑥ `_DECOMPOSITION_TIMEOUT_SEC` = ENV `JETRAG_DECOMPOSITION_TIMEOUT_SEC` int, default 5, clamp [1,30]. | ENV OFF 시 **완전 불변** (스레드 0, 추가 RPC 0, meta 만 4키 기본값 dict 추가) |
| `api/tests/test_search_decomposition.py` (**신규**) | 8개 — (a) ENV OFF→미발화·meta 기본값·decompose 호출 0·추가 RPC 0 / (b) ENV ON+needs+subqueries 반환→merge(hybrid RPC 1+2=3회)·meta 반영 / (g-1) cached decomp→meta 전파 / (c) ENV ON+not needs→decompose 호출 0 / (d) ENV ON+빈 tuple→미발화·original pool only / (e-1) decompose raise→graceful / (e-2) decompose timeout(1s 강제)→graceful / (f) `intent_router.route()` 1회 호출. stdlib unittest+mock, paid $0. | — |
| `web/src/lib/api/types.ts` | `SearchMeta` interface (decomposition 4필드 + meta_fast path/matched_kind + `[key: string]: unknown`) + `SearchResponse.meta?: SearchMeta | null`. | — |
| `api/../.env.example` | `JETRAG_DECOMPOSITION_TIMEOUT_SEC=5` 주석 1줄 (M1 W-1(a) 블록, clamp [1,30]). | — |

`/answer` 는 `decomposition_fired` 등 **추가 안 받음** (현행 — `decomposed_subqueries`/`decomposition_cost_usd`/`decomposition_cached` 는 이미 answer_meta 에 있음). `mode=dense`/`mode=sparse` RPC split 경로 **무변경**.

### 7.2 게이트 재확인 (§1 대비 불변)

`/search` 의 decompose 게이트 = `router_decision.needs_decomposition`(`= (T1 or T2 or T3 or T7) or (T5 and T6)`) **AND** ENV `JETRAG_PAID_DECOMPOSITION_ENABLED ∈ {true,1,yes,on}` — **이 두 개뿐.** confidence 임계 미사용 (cross_doc conf 0.85 3 row 보호). `query_decomposer.decompose()` 내부 게이트(needs → ENV → LRU cache → budget cap → LLM → parse 2≤len≤5)는 그대로 — `/search` 의 `_decompose_with_timeout` 은 그 위에 (1) 게이트 선판정으로 스레드 자체를 안 만듦 + (2) timeout 가드만 추가.

### 7.3 ablation / doc_id scope 처리

- `mode=dense` / `mode=sparse` (W-9.5 BM25 ablation) → §0-b 에서 `if mode == "hybrid":` 가드로 `_decompose_with_timeout` 호출 자체를 skip. `decomp` 는 빈 결과 → merge 분기 미진입. meta 는 4키 기본값. **ablation RPC split 경로·응용 필터 전부 불변.**
- `doc_id` scope 지정 → decomposition 적용 (각 sub-query 풀에 `doc_id` 동일 필터). cross_doc 라벨이 doc-bound 인 P2 함정(§5.5)은 sub-query 풀이 여전히 전-doc 풀(글로벌 top_k=10)이라 잔존 가능 — W-1(a) eval 에서 cross_doc R@10 실측으로 확인 예정. 안 오르면 DECISION-2(doc-scoped RPC 마이그)로 fallback.

### 7.4 테스트

- 신규 `test_search_decomposition.py` **8개 전부 PASS** (2.4s — timeout 테스트 1s + slow decompose 1.5s 포함).
- 전체 `python3 -m unittest discover -s tests` → **1055 tests** (1047 → +8). 실패 4건 = `test_embed_cache.py` 의 사전부터 flaky (글로벌 LRU 싱글톤 누수, clean main 재현, 격리 시 9/9 PASS) — W-1(a) 무관, 그대로 둠. 그 외 신규/기존 실패 **0**. pytest 미설치 환경이라 unittest 로 실행 (테스트는 전부 `unittest.TestCase`).
- 관련 테스트 집중 실행 (answer/search/doc-rrf/cross-doc/reranker/cover-guard/snippet/user-iso/entity-boost/s4-a-breakdown ×14 파일 90 tests) → 전부 PASS.

### 7.5 사이드이펙트 점검

- [x] `multi_query_search` import 순환 없음 — `from __future__ import annotations` 만. `answer`/`search` 둘 다 이 모듈을 import 하지만 역방향 없음. `python -c "from app.routers import search, answer"` 정상.
- [x] `/answer` 동작 불변 — `_rrf_merge_pools` 는 동일 함수 객체를 alias import (`answer._rrf_merge_pools is multi_query_search.rrf_merge_pools` True). `_DECOMP_TOP_K_*` 값 동일(20/10). answer 테스트 전부 PASS.
- [x] `mode=dense`/`mode=sparse` ablation path 불변 — `if mode == "hybrid":` 가드로 decompose·merge 미진입. `used_split_rpc`·응용 필터 코드 무변경.
- [x] meta_fast_path 불변 — `_run_meta_fast_path` 의 `meta={...}` (path/matched_kind/tags/title_ilike/date_range) 무변경. decomp_meta 는 RAG path 의 3개 return 에만.
- [x] ENV OFF 시 `/search` 완전 불변 — 게이트 선판정으로 스레드 0, sub-query RPC 0. 유일 차이 = `meta` 가 `None` → `{decomposition_fired:false, decomposed_subqueries:[], decomposition_cost_usd:0.0, decomposition_cached:false}` dict (frontend `SearchMeta` 호환). 테스트 (a)로 검증.
- [x] route() 호출 1회 — 테스트 (f)로 검증 (decision 재사용).
- [x] 무료 티어 — ENV OFF default. ON 시 §3 비용 ~$0.000026/firing × ~20 firing = eval 1회 ~$0.0005~0.0012. Gemini 1,500회/일 한도 대비 무문제. budget cap (`JETRAG_DECOMPOSITION_MONTHLY_CAP_USD=$0.30`) 도 `decompose()` 내부에 그대로.
- [x] SLO 수신 <2초 — `/search` 는 검색 엔드포인트(인제스트 아님). decompose timeout default 5s 가 hybrid+발화 query 의 최악 추가 latency 상한. ENV OFF·미발화 시 0 추가.
- [x] 한국어 에러 메시지 — 신규 사용자 노출 에러 없음 (decompose 실패는 graceful skip, 로그 warning 만).

### 7.6 남은 이슈 / 다음 스코프

- **W-1(a) eval 1회 측정** (다음 스텝, paid ~$0.0005~0.0012) — `JETRAG_PAID_DECOMPOSITION_ENABLED=true` 로 golden_v2 182 row eval: ① subquery 채택률 (decompose 가 `2≤len≤5` 반환하는 비율 — LLM 품질 의존, dry-run 불가) ② cross_doc R@10 변동 (0.4424 → ?, P2 함정 §5.5 — 실제로 오르는지 가장 먼저 확인) ③ 단일-doc 회귀 0 재검증 (FP 11 row 의 R@10·top-1) ④ latency p95 분포 (`decomposition_fired` meta 로 firing query 만 분리). 안 오르면 DECISION-2(doc-scoped RPC 마이그)로 fallback.
- decompose LRU cache 영속성 — in-process 200건 유지 (eval 비용 $0.001 라 영구화 ROI 낮음, MVP 범위 무난). 영구화는 M2+ 재검토.
- timeout 시 orphaned worker 스레드 — `shutdown(wait=False)` 라 요청 latency 영향 0, decompose 자체 retry 한도(`JETRAG_GEMINI_RETRY` default 1)로 곧 종료. 단 `_gemini_common.with_retry` 가 Gemini SDK HTTP 호출에 명시적 timeout 을 안 걸어(SDK 기본 timeout 의존, 수십 초) worker 가 그만큼 살아있을 수 있음 — 사용자 latency 영향 0(timeout 가드가 응답 보호), 다발 시 스레드 풀 누수 모니터링은 v2(rq+Redis 이관) 시점 자연 해소. SDK 호출 timeout 부여는 P2 후속 (senior-qa P2-2).
- (senior-qa P2 권고) `multi_query_search.rrf_merge_pools` 전용 단위 테스트 부재 — 현재 `test_answer_router_decomposition`·`test_search_decomposition`(b)로 간접 커버만. 순수 추출 리팩토링이라 동작 불변이 거의 확실하나 `tests/services/test_multi_query_search.py` 신규로 (단일 풀 순서 보존 / 동일 chunk_id 다중 풀 점수 합산 우선 / 빈 chunk_id skip / `rrf_score = Σ 1/(k+rank+1)`) 추가하면 회귀 안전망 강화. P2.
