# 2026-05-12 종합 마감 + 2026-05-13 진입 핸드오프

> 프로젝트: Jet-Rag
> 작성: 2026-05-12 세션 최종 마감 (handoff v2 `c50c9bb` 이후 ~ embed query 영구 캐시 ship + 마이그 016 DB 적용 직후)
> 목적: 본 1장만 보고 5분 안 컨텍스트 회복 + 다음 진입 시 1순위 = acceptable_chunks LLM-judge 2차 라운드 (paid ~$0.05, 사전 승인 필요) 또는 cross_doc 약점 진단 sprint. 전 handoff(`work-log/2026-05-11 종합 + 2026-05-12 진입 핸드오프.md`)는 Phase 0~10 시점 — 본 문서가 그 후속(Phase 11 / S3 D6 종결 / cold-start / logging / S4-B / embed-cache + 마이그 016).

---

## 0. 한 줄 요약

> **2026-05-12 — handoff v2 이후 6 commit ship. Phase 11 (acceptable_chunks LLM-judge 자동 보완 — golden_v2 12 row 채움, paid ~$0.15) + S3 D6 (실 BGE-reranker 재측정 → reranker net-negative on 회복 DB → RRF-only 확정, S3 100% 종결) + HF embed query cold-start 완화 (lifespan warmup ping + Retry-After 존중) + app 로거 부트스트랩 (`app.*` INFO 콘솔 가시화) + S4-B 본 PC 재검증·entity_boost ablation (효과 0 — default OFF 유지 확정, entity 백필 441/2469) + embed query→dense 벡터 영구 캐시 (마이그 016 + `embed_query_cache` 테이블, eval 재현성 — DB 적용·검증 완료). 단위 테스트 930 → 997 / skipped 0 / 회귀 0 (+67). chunks DB 2469 / entities 441 / vision 191. master plan §6: S3 100% (D6 종결), S4-A 50%+ 유지, S4-B 본 PC 인수·ablation 완료. main HEAD `1358bda`.**

---

## 1. 본 세션 ship 표 (handoff v2 `c50c9bb` 이후, commit hash 포함)

| # | sprint / 작업 | 상태 | 핵심 산출물 | commit | 단위 테스트 | paid |
|---|---|---|---|---|---:|---:|
| 11 | S4-A — acceptable_chunks LLM-judge 자동 보완 | ✅ Ship | `evals/run_acceptable_chunks_judge.py` 신규 (default dry-run, `--with-acceptable-judge` paid, `.bak.YYYYMMDD` 자동 백업, row 선택 = empty acceptable ∩ non-empty relevant ∩ negative≠"true" ∩ doc_id 존재, caption_dependent 우선 정렬, CostGuard fail-close 부분 ship, 연속 실패 3 early stop, atomic CSV write utf-8-sig BOM + 14 cols) + `evals/_acceptable_judge.py` 확장(`make_acceptable_judge_caller` / `evaluate_acceptable`) + `vision_metrics._VALID_SOURCE_TYPES += "acceptable_judge"`. golden_v2: 23 선택 → **12 row acceptable_chunks 채움** (146→158, 11건은 threshold 0.5 위 후보 없어 `[empty]`). `golden_v2.csv.bak.20260511` 커밋. | `ed48e0b` | 930 → 951 (+21) | ~$0.15 (Gemini judge) |
| D6 | S3 — 실 BGE-reranker 재측정 (종결) | ✅ Ship(측정) | 측정용 process-scope override `JETRAG_RERANKER_MONTHLY_CAP_CALLS=10000000` + `JETRAG_RERANKER_DEGRADE_THRESHOLD=1.0` (`reranker_invoke` 30d count 1136 > cap 1000 → all-degraded 회피). combo a(RRF-only) vs combo b/c(reranker on) × golden_v2 → **reranker net-negative on 회복 DB** (combo b/c R@10·top-1 < combo a — 05-10 broken-DB 결과의 정반대). HF free-tier reranker P95 196s = 운영 불가(timeout 빈발). → **production default = RRF-only 확정** (`JETRAG_RERANKER_ENABLED=false` 이미 default → 코드 변경 0, reranker ENV 옵션 보존). **S3 100% 종결.** 교훈: reranker 가치는 base retrieval 품질에 반비례. | `216086e` (work-log) | 951 유지 (측정만) | $0 (free-tier) |
| O1+O4a | HF embed query cold-start 완화 | ✅ Ship | `app/main.py` `lifespan` + `_warmup_bgem3()` (fire-and-forget warmup ping `"문서 검색"` → HF cold-start 선흡수, `app.state.bgem3_warmup_task`) + `bgem3_hf_embedding.py` & `bge_reranker_hf.py` `_parse_retry_after(exc)` (`Retry-After` 헤더 존중, ≤60s clamp, HTTP-date fallback) in `_with_retry`. 발견: combo a P95 67.6s = HF Inference free-tier scale-to-zero (503 "model is loading", 3 retry 백오프 ~95s worst). 한계: 부팅 후 첫 query 만 도움 — 재-idle 시 무효(구조적, O3 keepalive 미채택). | `c47bdd1` | 951 → 968 (+17) | $0 |
| — | app 로거 콘솔 핸들러 부트스트랩 | ✅ Ship | `app/logging_setup.py` 신규 `configure_app_logging(level=INFO)` 멱등 — `getLogger("app").handlers` 비면 `StreamHandler` + formatter `"%(levelname)s:     %(name)s - %(message)s"` + INFO + `propagate=False`, 있으면 no-op(외부 `--log-config` 존중). `app/main.py` 가 routers import 전 호출. 배경: uvicorn 기본 dictConfig 가 `uvicorn.*` 로거만 핸들러 부착·root 미설정 → `app.*` INFO 콘솔 드롭(WARNING/ERROR 만 `lastResort`). | `0648053` | 968 → 973 (+5) | $0 |
| S4-B | 본 PC 재검증 + entity_boost factor ablation | ✅ Ship | origin ~80% ship 분(entity_extract.py 룰 기반 + chunk.py ingest 통합 + search.py entity_boost ENV OFF + backfill 도구) 본 PC 검증. 백필 `--apply` → **chunks.metadata.entities 441/2469**. `_ENTITY_BOOST_FACTOR(1.10 하드코딩)` → `JETRAG_ENTITY_BOOST_FACTOR` ENV 화 (`_parse_factor_env`, default 1.10/[0.5,3.0] fallback, 동작 변경 0). ablation: baseline(OFF)+f110+f150+f200 × golden_v2 183 row → **4 설정 metric 동일** (R@10 0.6553 / top-1 0.7443 / nDCG 0.5975 / MRR 0.5616, qtype별 Δ 0.0000, cross_doc subset R@10 0.1273). 원인: 183 query 중 `extract_entities(query)` non-empty 1건뿐(G-U-019 "12%"), 그마저 정답 chunk 매칭 0 → 룰 기반 effect 모수 ≈ 0. **→ entity_boost default OFF 유지 확정** (코드/ENV 무변경), entity_boost = cross_doc 약점 레버 아님 — query decomposition / multi-doc fusion / cross_doc golden 라벨 재점검 쪽 ROI 높음. | `b3f7a64` | 973 → 981 (+8) | $0 |
| Q | embed query → dense 벡터 영구 캐시 (eval 재현성) | ✅ Ship + DB 적용 | `api/migrations/016_embed_query_cache.sql` 신규 (`embed_query_cache`: id UUID PK / text_sha256 / model_id / dim / vector JSONB / created_at / UNIQUE(text_sha256, model_id) + `idx_embed_query_cache_lookup` + RLS `service_role FOR ALL`) + `api/app/services/embed_query_cache.py` 신규 (vision_cache 패턴: `is_enabled` / `lookup`(dim 검증) / `upsert`(on_conflict ignore_duplicates) / `model_id` / `_warn_first`, ENV `JETRAG_EMBED_QUERY_CACHE` default "1", `_MODEL_ID="BAAI/bge-m3"` / `_DENSE_DIM=1024`) + `bgem3_hf_embedding.embed_query` **2단 캐시** (in-process LRU 512 → 영구 캐시 → HF, 그 후 upsert), `_last_cache_source`("lru"/"persistent"/"miss"), graceful. `embed`/`embed_passages`/`embed_batch` 무변경, `_URL` byte-identical. 캐시 키 = `sha256(NFC(text.strip()))` 64 hex + 'BAAI/bge-m3' (모델 교체 시 자동 invalidate, PII 우려로 query 원문 미저장). `tests/__init__.py` 에 `JETRAG_EMBED_QUERY_CACHE="0"` 단위테스트 강제. **마이그 016 = 사용자가 Supabase Studio 에 compact 버전 수동 적용 — `to_regclass` non-null + idx 1 + policy 1 + rls true 검증 완료.** (COMMENT 4문은 compact 버전이라 미적용 — repo 016 파일엔 있음, 동작 무관) | `1358bda` | 981 → 997 (+16) | $0 |

**누적**: 930 → **997** / skipped 0 / 회귀 0 (+67 = Phase 11 +21 / D6 측정만 / cold-start +17 / logging +5 / S4-B +8 / embed-cache +16)
**push 완료**: `fba1316..ed48e0b` (Phase 11, 사이에 `c50c9bb` handoff v2 doc) → `..216086e` (D6) → `..c47bdd1` (cold-start) → `..0648053` (logging) → `..b3f7a64` (S4-B) → `..1358bda` (embed-cache)
**main HEAD**: `1358bda`
**chunks DB**: 2469 (entities 441) / vision 191 — 변동 없음 (S4-B 백필은 metadata.entities 추가만, chunk 수 무변)

---

## 2. Master plan §6 sprint 진척률 (2026-05-12 마감 시점)

| Sprint | 목표 | 진척률 | 비고 |
|---|---|---:|---|
| S0 비용 계측 / 캐시 / cap | 1주 | 100% ✅ | 변동 0 |
| S1 골든셋 + 실 query 로그 | 1주 | 100% ✅ | 변동 0 |
| S1.5 vision_need_score PoC (+v3) | 3일+0.5일 | 100% ✅ | 변동 0 |
| S2 Vision 선별 + budget 본 ship | 1주 | 95% | D5 phase 2 보류 유지 |
| **S3 Cheap router + decomposition + reranker** | 1주 | **100% ✅** | **D6 실 reranker 재측정으로 종결 — reranker net-negative on 회복 DB → RRF-only 확정.** reranker 코드/ENV 옵션 보존(켜는 시나리오는 신중히) |
| **S4-A 인제스트 보조 인덱스** | 1~2주 | **50%+** (변동 없음) | D1+D2+D3 Phase 1 + D4 Phase 1/2/3 + D5 Phase 9 회복 + Phase 10 multimodal judge + **Phase 11 acceptable judge(골든셋 신호 정확도 보강)**. D6 chunk text augmentation 미진입 |
| S4-B 핵심 엔티티 추출 | 3일 | ~85% (본 PC 인수·ablation 완료) | entity_extract.py + ingest 통합 + backfill 완료(441/2469). entity_boost = 현 골든셋 효과 0 판명 → default OFF 확정. **남음: LLM 보강(persons/orgs/products) — 우선순위 낮음** |
| S4-C section 요약 | 3일 | 0% | S4-B LLM 보강 또는 다른 sprint 후 |
| S4-D 동의어 후보 사전 확장 | 2일 | 0% | — |
| S5 /answer UX | 1주 | ~10% | top-1 ≥ 0.80 DoD 도달(origin 보고). caption 정합 재측정 필요 |

---

## 3. 사용자 사인오프 권고 (sprint-level 결정 — 이견 시 회신)

1. **S3 종결 — production default = RRF-only 확정** (`JETRAG_RERANKER_ENABLED=false` default 유지, 코드 변경 0). D6 재측정 결과: 회복 DB 위에서 reranker net-negative + HF free-tier P95 196s 운영 불가. reranker 켜는 시나리오는 base retrieval 품질이 낮은 한정 상황(예: 특정 qtype 제외)만 신중히 검토.
2. **S4-B entity_boost default OFF 유지 확정** (코드/ENV 무변경). 룰 기반 entity_boost = 현 골든셋(183 query) 효과 측정 모수 ≈ 0. LLM 보강(persons/orgs/products) 통합 우선순위 낮음 — cross_doc 약점은 query decomposition / multi-doc fusion / golden 라벨 재점검 쪽 ROI 높음.

---

## 4. 활성 한계 (변동 — 다음 sprint 진입 시 점검)

### 신규 / 갱신
- **cross_doc R@10 0.1273 (D6, RRF-only / S4-B ablation 재확인)** — 9 qtype 중 최약(top-1 0.0). **reranker 도 entity_boost 도 못 고침**(2026-05-12 ablation 으로 entity_boost 레버 아님 판명). 후보: query decomposition 강화(`JETRAG_PAID_DECOMPOSITION_ENABLED` ON 검증) / multi-doc fusion(doc_embedding RRF) 가중 튜닝 / cross_doc golden row 라벨 재점검(doc 매칭 fail 1건 포함) — 진입 전 진단 sprint 필요.
- **acceptable judge huge-doc candidate 한계 (Phase 11)** — 데이터센터 doc(~1000 chunk)의 caption_dependent 7 row 가 cosine top-15 후보로 모두 `[empty]`. 2차 라운드 시 candidate top-K 15→30+ 확대 or `/search` 기반 후보 + threshold 0.5→0.6 / max_count 8→5 튜닝. judge 약간 관대(max_count 8 cap·marginal chunk 포함).
- **HF embed query cold-start (D6 발견 → 완화 ship)** — RRF-only 도 검색 P95 67.6s = HF Inference free-tier scale-to-zero. lifespan warmup + Retry-After 로 완화 — **단 부팅 후 사용자 query 전 모델 재-idle 시 무효**(구조적, O3 keepalive 미채택). self-host embed 보류.
- **HF embed query API 비결정성 (S4-B 부수발견 → embed-cache `1358bda` + 마이그 016 으로 해소)** — HF Inference API 가 같은 query 에 미세하게 다른 dense 벡터 반환 → 세션 간 ~92 row 랭킹 churn(집계 ±0.005 상쇄, 회귀 아님). `embed_query_cache` 테이블로 첫 fetch 벡터 freeze → 워밍 후 결정적. **마이그 016 적용 완료 → 발효. 단 캐시 비어 있음** — eval 1회 돌려 채워야 결정성 발현. **미검증: eval 2회 연속 churn 0 + HF 0 확인**(다음 권장 액션, paid $0).
- **chunks DB = entity 포함 상태 (2026-05-12 백필)** — `metadata.entities` 보유 chunk 441/2469. entity_boost OFF 면 검색 무영향이나 이후 측정 baseline 은 이 상태 기준. revert: `UPDATE chunks SET metadata = metadata - 'entities' WHERE metadata ? 'entities'` (단 새 ingest 자동분도 함께 삭제 — 사실상 영구).
- **마이그 016 DB↔repo 사소 불일치** — 사용자가 compact 버전 적용 → `COMMENT ON ...` 4문이 DB 에 없음(repo `016_embed_query_cache.sql` 엔 있음). 동작 무관, 재적용 불필요. 다른 마이그 동기화 작업 시 인지만.

### 해소
- ~~uvicorn 로깅 갭~~ → `logging_setup.configure_app_logging()` 으로 해소 (`app.*` 콘솔 핸들러 부착, routers import 전 호출).
- ~~vision_diagram faithfulness 0.0~~ → Phase 10 multimodal judge (이전 핸드오프).
- ~~chunks DB 회귀 -185~~ → Phase 9 옵션 A 회복 (이전 핸드오프).

### 유지 (이전 핸드오프에서 이월)
- **데이터센터 p.40** single-line table — v3 회복, R@10 baseline 은 chunk 374 text-only 라 S2 D5 phase 1 미회수.
- **G-A-107/111 search empty** — 포트폴리오 chunk page DB NULL 별개 이슈.
- **본 PC 측정 가능 row N=3** (golden v1 hint cross-check) — 다른 PC 진입 또는 골든 v2 row 추가 필요.
- **R@10 v1 ↔ v2 직접 비교 부적절** — graded scoring 분모 inflation, 회귀 판정은 v2↔v2 만.
- **reranker net-negative on 회복 DB (D6 결론)** — combo b/c < combo a, HF free-tier reranker 운영 불가(P95 196s). reranker 가치는 base retrieval 품질에 반비례.
- `run_s3_d5_search_stack_eval.py` / `eval_retrieval_metrics.py` markdown 헤더 cosmetic 버그 — 측정값 영향 0.

---

## 5. 다음 진입 — 우선순위

> 진입 전 사용자에게 확인할 것: 1순위(acceptable judge 2차)는 paid LLM ~$0.05 사전 승인 필요.

| 순위 | 작업 | paid | 진입 메모 |
|---:|---|---:|---|
| 0 | **embed-cache 검증** (코드 변경 아님) | $0 (HF 호출만) | 캐시 비어 있음 → eval(135 QA) 1회 돌려 query→vector 채움 → 2회 연속 실행 → top-10 churn 0 + HF 호출 0(워밍 후) 확인. S4-B ablation 의 ~92 row churn 해소되는지. (`_first_warn_logged` True 인 기존 프로세스는 재시작해야 warn 채널 리셋 — 동작 무영향) |
| 1 | **acceptable judge 2차 라운드** | ~$0.05 (승인 필요) | Phase 11 finding 적용: candidate top-K 15→30+ 또는 `/search` 기반 후보 + threshold 0.5→0.6 / max_count 8→5 튜닝 → caption_dependent 7 row `[empty]` 재시도. DoD: caption_dependent row acceptable 추가 / R@10 ±0.02 / 회귀 0 |
| 2 | **cross_doc 약점 진단 sprint** (entity_boost 아님 — 판명) | $0 진단 → 일부 paid | cross_doc R@10 0.1273 / top-1 0.0. 후보: query decomposition(`JETRAG_PAID_DECOMPOSITION_ENABLED` ON 검증) 강화 / multi-doc fusion(doc_embedding RRF) 가중 튜닝 / cross_doc golden 라벨 재점검(doc 매칭 fail 1건). 진단 sprint 먼저 |
| 3 | O4(b) 첫-attempt timeout 분리 (cold-start follow-up) | $0 | `bgem3_hf_embedding.py` `_REQUEST_TIMEOUT=60.0` 단일 → warm 상태 hiccup 시에도 첫 attempt 60s 대기. probe(짧게)/load(길게) 분리 — 인제스트 `embed_batch` 영향 검토 필요(큰 batch 는 첫 attempt 도 길게 유지) |
| 4 | chunk text augmentation (S4-A D6, 5~7일) | $0 (재인제스트 cost 별도) | caption-like prefix / surrounding context window 추가 시 R@10 변동 측정 |
| 5 | G-A-208 multimodal judge timeout 진단 / reranker degrade cap dev 관리 (Q-D6-3) | $0 | G-A-208: Gemini multimodal 60s timeout + retry 정책 / `reranker_invoke` monthly cap 1000·30일 윈도우가 dev 측정기에 작음 → 윈도우 확대 vs env-tunable vs test-run cleanup |
| 6 | `_last_cache_source` /stats 메트릭 연결 (embed-cache follow-up) | $0 | `_last_cache_source`("lru"/"persistent"/"miss") → `/stats` 분포 노출(캐시 hit 률 가시화). 마이그 016 적용됨 → 의미 있음 |
| 7 | multimodal judge 17 row 확대 (~$0.15-0.21, 가치 낮음 보류) | ~$0.15-0.21 | RAGAS judge 재호출이 비용 대부분. 측정 일반화일 뿐 검색/답변/UX 개선 아님 → 강등 |

---

## 6. 환경 / 검증 메모 (다음 진입 시)

- **단위 테스트**: `cd api && uv run pytest -q` → 997 OK / skipped 0 / 회귀 0.
- **embed-cache ENV**: `JETRAG_EMBED_QUERY_CACHE` default "1"(켜짐). eval ablation 시 "0" 으로 우회 가능. 캐시 비우기 = `DELETE FROM embed_query_cache;` (또는 model_id 별 부분).
- **entity_boost ENV**: `JETRAG_ENTITY_BOOST_FACTOR` default 1.10 / [0.5,3.0] fallback. entity_boost 자체는 코드 상 항상 적용 경로 있으나 매칭 entity 가 거의 없어 사실상 무효 — 켜고 끄는 별도 토글 없음(factor 만 조정).
- **reranker ENV**: `JETRAG_RERANKER_ENABLED=false`(default, RRF-only). 측정 시 `JETRAG_RERANKER_MONTHLY_CAP_CALLS` / `JETRAG_RERANKER_DEGRADE_THRESHOLD` process-scope override 로 cap 회피.
- **마이그 016**: DB 적용 완료(`embed_query_cache` 6 cols + index + RLS service_role policy). 신규 환경 부트스트랩 시 `api/migrations/016_embed_query_cache.sql` 적용 필요(read-only MCP 환경이면 사용자가 Supabase Studio 에 수동). 미적용 상태에서도 graceful(캐시 항상 miss + 404, 검색 무변경).
- **uvicorn 로깅**: `app/main.py` import 시 `configure_app_logging()` 자동 호출 → `app.*` INFO 콘솔 출력. 외부 `--log-config` 사용 시 no-op(존중).
- **HF cold-start**: 서버 부팅 시 `lifespan` 이 `_warmup_bgem3()` fire-and-forget — 로그에 "BGE-M3 warmup 시작/완료(%.1fs, dim=%d)" 또는 "실패" 출력. 첫 실 query 지연 완화용.
- **work-log**: 본 세션 작업별 상세 — `2026-05-11 acceptable_chunks LLM-judge 자동 보완 ship.md`, `2026-05-11 S3 D6 재측정 — reranker net-negative, RRF-only 확정.md`, `2026-05-12 HF embed query cold-start 완화 ship.md`, `2026-05-12 app 로거 로깅 부트스트랩 ship.md`, `2026-05-12 S4-B 본PC 재검증 + entity_boost ablation.md`, `2026-05-12 embed query 영구 캐시.md`.
