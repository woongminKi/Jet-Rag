# 2026-09-04 — Phase 1 진입 준비 (Step 0 착수 정지선)

> **범위**: Phase 0 판정 종료 후, Phase 1(기반 계층 + CF 프록시) 착수 전에 닫아야 할 3건.
> **결과**: **3건 전부 완료.** 인벤토리 확정 과정에서 **앞선 기록 2건이 틀린 것도 드러났다**(§정정).
> **목표 재확인(사용자, 2026-09-04)**: Railway 를 없앤다. 붙어 있는 것 전부 Supabase 로 옮긴다.

## 0. 한눈에 보기

| 항목 | 상태 | 근거 |
|---|---|---|
| 0-1 Railway 인벤토리 확정 | ✅ **완료** | 대시보드 스크린샷 7장 + 코드 대조. **정정 2건 발생** |
| 0-2 스파이크 함수 정리 | ✅ 완료 | `spike` 삭제 — 프로젝트에 Edge Function **0개** |
| 0-3 `answer` 응답 상한 확인 | ✅ 완료 | 실측 7.2~8.7s — Edge 초기 응답 150s 대비 **17배 여유** |
| Cloudflare 프록시 가능 여부 | ✅ **확인** | `woong-s.com` 이 이미 Cloudflare 존. 레지스트라 이전 불필요 |

## 0-1 — Railway 인벤토리 (확정)

대시보드 7화면 + 코드 대조. **코드만 보고 쓴 앞선 기록 2건이 틀렸다 — 아래 §정정 참조.**

### 서비스 · 배포

| 항목 | 값 |
|---|---|
| 서비스 | `Jet-Rag` (GitHub `woongminKi/Jet-Rag` 연결) |
| **Root Directory** | **`/api`** |
| 빌더 | Dockerfile 자동 감지 → **`api/Dockerfile`** (Dockerfile Path 비어 있음) |
| 브랜치 | `main`, **Auto deploy ON**, Wait for CI **OFF** |
| Custom Start Command | 없음 (Dockerfile `CMD` 사용: `uvicorn --port ${PORT:-8000}`) |
| **Cron Schedule** | **없음 — 미설정** |
| Healthcheck Path | 없음 |
| Serverless | OFF (상시 구동) |
| Restart Policy | On Failure ×10 |
| Teardown | OFF |
| Config-as-code 파일 | 없음 |
| **Volumes** | **섹션 자체가 없음 = 미사용** (영속 데이터 전부 Supabase) |

### 네트워킹

| 도메인 | 용도 | 포트 |
|---|---|---|
| `jet-rag-production.up.railway.app` | Railway 기본 | 8080 |
| **`jetrag-api.woong-s.com`** | 커스텀 (프론트가 호출) | 8080 |
| `jet-rag.railway.internal` | 사설망 (미사용) | — |

Edge: Under Attack Mode OFF · CDN Caching OFF · Edge Rules 0건 → **Railway Edge 기능 의존 없음.**

### 스케일

Region **US West (California)** · Replica 1 · CPU 8 vCPU · Memory 8GB (플랜 상한값).

### ENV — 사용자 정의 24개 (전부 코드에서 참조 확인)

`DEEPINFRA_API_TOKEN` `DEFAULT_USER_ID` `GEMINI_API_KEY` `HF_API_TOKEN`
`JETRAG_AUTH_ENABLED` `JETRAG_BILLING_CRON_SECRET` `JETRAG_BILLING_KEY_ENCRYPTION_KEY`
`JETRAG_BILLING_REDIRECT_BASE` `JETRAG_CORS_ORIGINS` `JETRAG_EMAIL_WEBHOOK_SECRET`
`JETRAG_EMBED_PROVIDER` `JETRAG_GEMINI_RETRY` `JETRAG_KAKAOPAY_CID` `JETRAG_PAYMENT_PROVIDER`
`JETRAG_PDF_VISION_ENRICH` `OPENAI_API_KEY` `OWNER_USER_ID` `SUPABASE_JWKS_URL`
`SUPABASE_JWT_ALGORITHM` `SUPABASE_JWT_SECRET` `SUPABASE_KEY` `SUPABASE_SERVICE_ROLE_KEY`
`SUPABASE_STORAGE_BUCKET` `SUPABASE_URL`

**미참조(죽은 변수) 0개** — 24개 전부 `api/` 안에서 읽힌다. 그대로 Edge Functions secrets 로 옮긴다.

Railway 주입 8개(`RAILWAY_PUBLIC_DOMAIN` 등)는 **코드가 하나도 읽지 않는다** → 이관 대상 아님.
`PORT` 만 Dockerfile 이 쓰는데 Edge 에는 개념이 없다.

### 코드가 읽는데 Railway 에 없는 것 — 기본값으로 동작 중

코드 전체가 읽는 ENV 는 **45개**, Railway 설정은 24개. 차이 21개는 코드 기본값으로 돈다.
그중 운영에 의미 있는 것:

| 변수 | 위치 | 의미 |
|---|---|---|
| **`JETRAG_KAKAOPAY_SECRET_KEY`** | `app/config.py` | **결제가 아직 라이브가 아니라는 증거** (심사 대기 상태와 일치) |
| `JETRAG_EMAIL_INGEST_DOMAIN` | `app/config.py` | 이메일 인제스트 도메인 |
| `JETRAG_HYDE_ENABLED` / `JETRAG_RERANKER_ENABLED` / `JETRAG_QUERY_EXPANSION` | `routers/search.py` | 검색 기능 토글 — **기본값이 곧 운영 동작**이다 |
| `JETRAG_DOC_EMBEDDING_RRF*` (3개) | `routers/search.py` | RRF 가중치 |
| `JETRAG_VISION_*` / `JETRAG_PDF_VISION_ENRICH_MAX_*` (4개) | `ingest/`, `services/` | vision 상한 |
| `RAGAS_*` (2개) | `services/ragas_eval.py` | 평가 모델 |
| `SEARCH_SNIPPET_AROUND` | `routers/search.py` | 스니펫 폭 |

나머지 5개(`JETRAG_API_BASE*`, `JET_RAG_API_BASE`, `JETRAG_W4_*`)는 `scripts/` 전용이라 무관.

> **이관 시 주의**: "Railway 에 없으니 안 옮겨도 된다"가 아니다. **기본값이 곧 현재 운영 동작**
> 이므로, Edge 포팅본의 기본값이 Python 쪽과 같은지 대조해야 한다. 다르면 검색 결과가 조용히 바뀐다.

## 정정 — 앞서 코드만 보고 쓴 2건이 틀렸다

| 앞선 기록 | 실제 | 영향 |
|---|---|---|
| "저장소 루트 `Dockerfile`" | **`api/Dockerfile`** (Root Directory `/api`) | 경미 |
| "**Railway cron** 이 `billing_charge.py` 를 돌린다" | **Cron Schedule 미설정** — 아무것도 안 돈다 | **이관 항목이 아니라 "아직 안 만든 것"** |

두 번째가 중요하다. `payments.py` 주석의 "주 경로는 `scripts/billing_charge.py` (Railway cron)"
는 **설계 의도**였지 현재 상태가 아니다. 메모리의 "W5-6 라이브 전 필수: 마이그025·ENV·**cron**·약관"
미완 목록과 일치하고, 카카오페이 심사 대기 상태와도 맞는다.

→ Phase 4 는 "Railway cron 을 pg_cron 으로 이관"이 아니라 **"정기결제 배치를 pg_cron 으로 신규 구축"**
이다. 공수 산정이 달라진다. `JETRAG_KAKAOPAY_SECRET_KEY` 미설정도 같은 사실을 가리킨다.

> **교훈(반복)**: 코드 주석은 의도이지 상태가 아니다. `payments.py` 주석 한 줄을 근거로
> "Railway cron 이 돌고 있다"고 쓴 게 잘못이었다. 대시보드를 봤어야 했다.

## 그 외 이관 항목 — lifespan 상주 작업 2건

`main.py:113` lifespan 에서 BGE-M3 cold-start warmup + `ingest_jobs` 고아 job sweep 을
fire-and-forget 으로 1회씩 돌린다. **Edge Functions 에는 상주 프로세스가 없어 등가물이 없다.**
마스터 플랜의 `028_cron_maintenance.sql`(pg_cron)이 이걸 덮는 게 맞다.
라우터 목록에는 안 잡히는 종류라 인벤토리에 명시해 둔다.

## 문서 불일치 1건

`README.md:61` 은 백엔드를 **"Railway Hobby (Singapore)"** 라고 적었지만 실제 Region 은
**US West (California)** 다. Phase 6 에서 README 갱신 시 함께 고친다.

## 0-3 — `answer` 응답 상한 (해소)

플랜 §5 의 미결 결정: "LLM 응답이 Edge 초기 응답 150s 를 넘는가".

`answer` 는 **동기 호출**이다(`answer.py:18` — "Q6 동기 호출, streaming 은 v1.5 이후").
운영 엔드포인트를 3회 호출해 실측:

| 질의 | `took_ms` | wall |
|---|---|---|
| 문서 핵심 요약 | 8,597 | 10.28s |
| 세무 판결 요지 | 7,171 | 7.68s |
| 데이터센터 신청 자격 | 8,747 | 9.46s |

→ **150s 제한은 문제가 아니다.** `EdgeRuntime.waitUntil` + 폴링 설계는 **불필요**.
Phase 2 는 평범한 동기 Edge Function 으로 간다.

### 다만 질문이 하나 옮겨갔다 — wall 이 아니라 CPU 다

위 8.6s 의 대부분은 Gemini 대기(I/O)이고, **Edge 의 2s 제한은 CPU 시간**이다.
즉 150s 는 통과했지만 진짜 관문은 "검색·리랭크·프롬프트 구성의 CPU 몫이 2s 안인가"로 바뀐다.

`search_metrics_log` 최근 1,000건 (마지막 기록 2026-07-19):

| 지표 | 값 |
|---|---|
| 최소 / 중앙 | 84ms / **182ms** |
| p90 / p99 | 818ms / **3,089ms** |
| 최대 | **42,106ms** |
| 2,000ms 초과 | 25/1,000 = **2.5%** |

**이 값은 wall clock 이라 그대로 CPU 로 읽으면 안 된다.** 임베딩 API·DB RPC 왕복이 포함돼
있어 42s 짜리는 외부 API 지연일 가능성이 높다. 지금 가진 계기로는 **분해가 안 된다**.

→ **Phase 2 진입 첫 작업은 검색 경로의 CPU 몫 실측**으로 잡는다. Phase 0 하네스와 같은
방식(동기 블록만 `measure()` 로 감싸 배포 후 호출)이면 바로 나온다.
샘플이 1.5개월 전 것이라는 점도 같이 기록해 둔다 — 그 사이 트래픽이 거의 없었다.

## 0-2 — 스파이크 함수 삭제

`supabase functions delete spike` → `Deleted Edge Function.` 확인 결과 프로젝트의
Edge Function **0개**. `verify_jwt=false` 공개 표면(`?kind=burn` 으로 CPU 소모 가능)이 닫혔다.

코드는 저장소에 그대로 있으므로 재측정이 필요하면 `supabase functions deploy spike
--no-verify-jwt` 로 **20초**면 복구된다. Phase 2 CPU 실측 때 다시 쓸 예정이다.

## Phase 6 철거 체크리스트 (이 인벤토리가 대조 기준이다)

전환 완료 후 Railway 를 지우기 전에 아래가 전부 Supabase 쪽에 있어야 한다.

- [ ] ENV 24개 → Edge Functions secrets (`supabase secrets set`)
- [ ] `jetrag-api.woong-s.com` → CF Worker route (오렌지 구름 전환)
- [ ] lifespan 상주 작업 2건 → `028_cron_maintenance.sql` (pg_cron)
- [ ] 정기결제 배치 → `027_cron_billing.sql` (pg_cron) — **신규 구축**, 이관 아님
- [ ] `jet-rag-production.up.railway.app` 참조 제거 (`README.md:62`)
- [ ] `README.md:61` Region 표기 수정 (Singapore → 해당 없음)
- [ ] GitHub Auto-deploy 연결 해제 (`main` push 가 Railway 를 재배포하지 않도록)
- [ ] 볼륨 없음 확인됨 → **영속 데이터 이관 작업 0건**

## 다음

Phase 1 Task 1.1(Deno 골격 + `config.ts`)부터. 상세 플랜은
`docs/superpowers/plans/2026-08-24-phase1-기반-계층.md` (1,262줄).

> Task 1.7(CF 프록시)은 운영 API 의 트래픽 경로를 바꾸는 **유일한** 단계다.
> 회색 구름 복귀로 즉시 롤백되지만, 저트래픽 시간대에 롤백 절차를 먼저 적고 진행한다.
