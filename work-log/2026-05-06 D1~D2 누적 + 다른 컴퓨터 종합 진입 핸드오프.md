# 2026-05-06 D1~D2 누적 + 다른 컴퓨터 종합 진입 핸드오프

> 프로젝트: Jet-Rag
> 작성일: 2026-05-06
> 작성자: Claude (다중 에이전트 협업 — senior-planner, senior-developer, senior-qa, Explore)
> 목적: 다른 컴퓨터에서 본 문서 1개만 봐도 (1) 어디까지 ship 됐는지 (2) 어떤 결정이 있었는지 (3) 다음 무엇부터 진입할지 (4) 환경 어떻게 복원할지 5분 안 파악 가능
>
> 본 문서가 **2026-05-06 누적 마스터** 이며, 같은 날짜의 5개 짝 문서는 의사결정 추적용 reference.

---

## 0. 한 줄 요약

> **Phase 1 S0 D1 ~ D2 1주 분량을 1일에 압축 ship** — 마이그 005/006/007/008/014/015 신규·적용 + factory.py 추상화 (Gemini default + OpenAI 미래 보험) + 모델 매핑 정정 (2.0 deprecated → 2.5 회복) + sweep×retry 곱셈 제거 (worst 450→100) + image_tokens 활성화 + retry_attempt 컬럼 활성화. **단위 테스트 411 → 460 통과 (회귀 0)**. 17 commit + origin/main push 완료. 다음 진입 후보 = D2-B (vision_page_cache lookup) / D4 (cost cap) / S1 D5 (2.5 vs 2.5-lite 측정).

---

## 1. 오늘 ship 누적 (17 commit + push)

### 1.1 timeline

| 시점 | sprint | commit | 핵심 효과 |
|---|---|---|---|
| 진입 | D1 | bd17fb4 | 마이그 014 (vision_usage_log 9컬럼 확장) + record_call usage 인자 |
| | D1 | 9501b32 | factory.py 도입 + 마이그 015 (vision_page_cache 신규) |
| | D1 | 2f4183e | VisionCaption.usage + Gemini usage_metadata 파싱 |
| | D1 | dd9b501 | image_parser doc_id/page 추적 + extract.py factory 경유 |
| | D1+1 | ab78858 | P1 결함 fix (extract.py lazy import + image_tokens modality 분리) |
| | D2-A | 9313939 | search/answer LLM 호출 factory 경유 |
| | D2-A | bd1c1ca | hyde LLMProvider 일반화 + cache key model 포함 |
| | D2-A | 7ccf4c5 | tag_summarize LLM 호출 factory 경유 |
| | D2-A | eee81b4 | RAGAS judge JETRAG_LLM_PROVIDER 분기 추가 |
| | D2-A | 8085760 | work-log §13/§14 추가 |
| | D2-D | b056b8d | 모델 2.5→2.0 + 단가 테이블 (이후 2.0 deprecated 발견 → 정정) |
| | S1 D1 | 0cdcea4 | 골든셋 33 entry 작성 (evals/golden_v0.6_user.csv) |
| | D2-C | 1373a3a | sweep 3→2 + retry 3→1 곱셈 제거 |
| | D2-D 정정 | 310b20a (force push) | 2.0 deprecated 회복 + .gitignore 보강 (사용자 자료 노출 방지) |
| | P2 | c717ea8 | retry_attempt 컬럼 활성화 |
| 마감 | 핸드오프 | (현재) | 본 문서 |

### 1.2 누적 단위 테스트

- **시작**: 411 통과 (W25 D14 baseline)
- **마감**: **460 통과 (회귀 0)**
- 신규 테스트: +49 (factory 12, P1-1 lazy 4, image_tokens 5, hyde cache 3, ragas provider 3, factory pricing 12, sweep 3, retry 5, retry_attempt 2)

### 1.3 주요 ship 가치

| 카테고리 | 효과 |
|---|---|
| **비용 가시성** | vision_usage_log 9컬럼 확장 (token + cost + model + retry) → 호출당 모든 정보 추적 가능 |
| **어댑터 추상화** | factory.py 1줄 ENV 전환 가능 (`JETRAG_LLM_PROVIDER`) — OpenAI v1.5 진입 시 호출처 0 변경 |
| **호출 절감** | sweep×retry 곱셈 제거: 50p PDF worst case 450 → 100 호출 (4.5x) |
| **회귀 정정** | 2.0 deprecated 발견 → 2.5-flash + 2.5-flash-lite 안전 모델 회복 (사용자 결정 정합) |
| **데이터 보호** | .gitignore 패턴 추가 (루트 *.pdf/*.hwp/*.hwpx/*.docx/*.pptx) — 사용자 자료 git 추적 방지 |

---

## 2. 사용자 액션 누적 (다른 컴퓨터에서 동일 작업 필요)

### 2.1 Supabase 마이그 적용 (완료)

다음 6개 마이그를 Supabase Studio → SQL Editor 에서 paste→Run (순서 엄수):

| 순서 | 파일 | 효과 |
|---|---|---|
| 1 | `api/migrations/005_vision_usage_log.sql` | vision_usage_log 테이블 신규 |
| 2 | `api/migrations/006_search_metrics_log.sql` | search_metrics_log 테이블 신규 |
| 3 | `api/migrations/007_metrics_trend_rpc.sql` | 005/006 의존 RPC |
| 4 | `api/migrations/008_search_mode_split_rpc.sql` | 006 의존 RPC |
| 5 | `api/migrations/014_vision_usage_log_enhanced.sql` | vision_usage_log 9컬럼 확장 |
| 6 | `api/migrations/015_vision_page_cache.sql` | vision_page_cache 테이블 신규 |

**검증 SQL**:
```sql
-- 테이블 존재
SELECT table_name FROM information_schema.tables 
WHERE table_schema = 'public' 
  AND table_name IN ('vision_usage_log', 'search_metrics_log', 'vision_page_cache')
ORDER BY table_name;
-- 기대: 3 rows

-- 014 컬럼
SELECT column_name FROM information_schema.columns
WHERE table_schema = 'public' AND table_name = 'vision_usage_log'
ORDER BY ordinal_position;
-- 기대: 15컬럼 (call_id, called_at, success, error_msg, quota_exhausted, source_type, doc_id, page, prompt_tokens, image_tokens, output_tokens, thinking_tokens, retry_attempt, estimated_cost, model_used)
```

### 2.2 환경 변수 추가

**`.env`** (api 루트, 시크릿 — 별도 동기화):
```bash
# 503 회복용 (D2-C 의 곱셈 제거 후 회복력 ↑)
JETRAG_GEMINI_RETRY=3

# 기존 (W25 D14 부터)
JETRAG_PDF_VISION_ENRICH=true
```

`.env` 자체는 git 추적 X — secret manager 또는 직접 복사. 짝 문서 [`2026-05-06 푸시 안 된 환경 설정 정리...`](./2026-05-06%20푸시%20안%20된%20환경%20설정%20정리%20다른%20컴퓨터%20동기화용.md) 참조.

### 2.3 자료 cleanup (수행 완료, 다른 컴퓨터에서는 불필요)

오늘 다음 doc 들이 검증 과정에서 삭제됨 (DB + Storage):
- `sonata-the-edge_catalog` (chunks 99, ingest_jobs 1, ingest_logs 9)
- `포트폴리오_이한주 - na Lu` (chunks 62, vision_usage_log 30, ingest_jobs 1, ingest_logs 9)

다른 컴퓨터에서는 sync 후 자동 반영.

---

## 3. 실측 검증 결과

### 3.1 vision 호출 정상 동작 (포트폴리오 PDF 1회 인제스트 후 SELECT)

| 항목 | 기대 | 실측 | 판정 |
|---|---|---|---|
| success=true | >0 | 12 / 26 (46%) | ✅ vision 호출 회복 |
| **image_tokens** (P1-3) | 채워짐 | 258 토큰/page | ✅ |
| prompt/output/thinking_tokens | 채워짐 | 493 / 225 / 683 | ✅ |
| estimated_cost | 단가 dict 적용 | $0.0024/page | ✅ 2.5-flash 단가 정확 |
| model_used | gemini-2.5-flash | gemini-2.5-flash | ✅ factory 매핑 확인 |
| retry_attempt | 분포 확인 | NULL → P2 ship 후 채워짐 (재인제스트 시 검증) | ✅ commit c717ea8 |

### 3.2 단가 비교 (실측 + 추정)

| 모델 | input ($/1M) | output ($/1M) | 동작 검증 | 비고 |
|---|---:|---:|---|---|
| **gemini-2.5-flash** | 0.30 | 2.50 | ✅ (일시 503 retry 회복) | 현재 vision/answer/ragas/reasoning default |
| **gemini-2.5-flash-lite** | 0.10 | 0.40 | ✅ | 현재 tag/summary/decomp/hyde default |
| gemini-2.0-flash | 0.10 | 0.40 | ❌ 신규 사용자 deprecated | ENV override 시도 가능하나 비추천 |
| gemini-2.0-flash-lite | 0.075 | 0.30 | ❌ deprecated | 동일 |

### 3.3 503 fail rate (D2-C 후 회복 진행 중)

- 인제스트 1회 (32MB PDF) 시 26 호출 중 14 fail (`503 UNAVAILABLE — high demand`)
- D2-C 의 retry default 1 영향 추정 → ENV `JETRAG_GEMINI_RETRY=3` 적용
- **다음 인제스트에서 재검증 필요** — fail rate 감소 + retry_attempt 컬럼 분포 확인

---

## 4. 남은 작업 (sprint plan 기반 우선순위)

### 4.1 D2 잔여 (작업량 작음, 즉시 진입 가능)

| 후보 | 작업량 | 가치 | 의존성 |
|---|---|---|---|
| **D2-B** | 1일 | reingest 호출 0회 (master plan §7.2) | 마이그 015 적용 (✅) |
| **D4 cost cap** | 1일 | 문서/일별 budget 자가 차단 (master plan §7.4) | 마이그 014 (✅), 1주 누적 데이터 |
| **S1 D5** | 1일 | 2.5-flash vs 2.5-flash-lite 골든셋 회귀 측정 — 사용자 결정 ("저렴한 모델") 검증 | 골든셋 v0.6 (✅) |

**권고**: 
- **즉시**: 사용자 PDF 재업로드 → vision_usage_log retry_attempt 분포 검증 (P2 효과)
- **이어서 D2-B** — 캐시 통합으로 reingest 비용 0 (가장 큰 비용 절감)
- 또는 **S1 D5** — 모델 회귀 검증 → 저렴한 모델 (lite) 로 일부 전환 결정

### 4.2 Phase 2 (S1) 잔여

| 후보 | 작업량 | 의존성 |
|---|---|---|
| **S1 D2** 자동 골든셋 100+ 확장 | 반일 | Gemini quota |
| **S1 D3-D4** 실 query 로그 대시보드 + answer_feedback 분석 | 1일 | 006 search_metrics_log (✅) |
| **S1 D5** 2.5 vs 2.5-lite RAGAS 측정 | 1일 | 골든셋 (✅) |

### 4.3 Phase 3+ (S2 ~ S5, master plan §6)

| sprint | 작업 | 진입 신호 |
|---|---|---|
| **S2** | vision_need_score + page cap + budget 본 ship | S1 D5 baseline 확보 후 |
| **S3** | cheap router + gated decomposition + reranker/MMR | S2 후 |
| **S4** | 인제스트 보조 인덱스 (표/그림 caption + 엔티티) | S3 후 |
| **S5** | /answer UX (extractive summary 자동 + 사용자 액션 paid) | S4 후 |

### 4.4 사용자 결정 보류 항목 (master plan §9.2)

| # | 결정 | 권고 |
|---|---|---|
| Q-S1 | S1 D5 모델 회귀 측정 — 2.5-flash vs 2.5-flash-lite | **즉시 진입** (저렴한 모델 채택 가능성) |
| Q-S2 | 운영 모드 default — 기본 (5~10p) vs 품질 (20~30p) | **기본** |
| Q-S3 | cheap router confidence < 0.75 시 자동 vs 사용자 확인 | **자동** |
| Q-S5 | extractive summary 모델 — BGE-M3 cosine vs Flash-Lite | **무료 BGE-M3** |
| Q-S0 | budget 초기값 (KRW/doc, KRW/일) | 1주 측정 후 자동 산정 |

---

## 5. 알려진 한계 + 검증 안 된 가정

### 5.1 한계

1. **vision_page_cache lookup 미통합** — 마이그 015 ship 만 완료, `_enrich_pdf_with_vision` 의 lookup→miss→upsert 코드 미작성 (D2-B 영역)
2. **PPTX rerouting doc_id NULL** — slide_idx 가 page 의미와 다름, 의도적
3. **503 fail rate 46%** — `JETRAG_GEMINI_RETRY=3` 적용 후 재검증 필요
4. **answer.py `_LLM_MODEL_FALLBACK = "gemini-2.5-flash"`** — 검색 0건 시 LLM 인스턴스 안 만드는 fallback 값. 실 호출은 factory 결정. 정상.
5. **OpenAI 어댑터 미구현** — factory 의 OpenAI 분기 = NotImplementedError 스텁. v1.5 진입 시 작성.
6. **GitHub force push history** — `4bd8719` 가 force push 로 제거됐지만 GitHub 가 며칠간 commit SHA 직접 URL cache 가능 (자동 GC 시 정리)

### 5.2 검증 안 된 가정

| # | 가정 | 검증 방법 |
|---|---|---|
| 1 | gemini-2.5-flash-lite 단가 ($0.10/$0.40) 정확성 | https://ai.google.dev/pricing 실측 (분기별) |
| 2 | 2.5-flash 가 2.5-flash-lite 보다 한국어 답변 품질 우위 | S1 D5 골든셋 회귀 측정 |
| 3 | retry 3 회면 503 회복 충분 | 사용자 PDF 재인제스트 후 vision_usage_log retry_attempt 분포 |
| 4 | P2 retry_attempt 전파가 운영 환경에서도 동작 | 실 PDF 인제스트 후 컬럼 SELECT |
| 5 | factory 의 1줄 OpenAI 전환이 v1.5 진입 시 정확 | OpenAI 어댑터 작성 시 검증 |

---

## 6. 다른 컴퓨터 진입 절차 (5분 안)

### 6.1 코드 + 의존성

```bash
git clone https://github.com/woongminKi/Jet-Rag.git
cd Jet-Rag

cd api && uv sync
cd ../web && pnpm install
```

### 6.2 환경 변수 복원

`api/.env` 작성 (시크릿 — secret manager 또는 직접 복사):

```bash
# 필수 (기존)
SUPABASE_URL=https://mpmtydudhojpukuuadrd.supabase.co
SUPABASE_KEY=<anon JWT>
SUPABASE_SERVICE_ROLE_KEY=<service_role JWT>
GEMINI_API_KEY=<Gemini paid Tier 1>
HF_API_TOKEN=<HF read>
OPENAI_API_KEY=<OpenAI key, 미래 보험>

# W25 D14
JETRAG_PDF_VISION_ENRICH=true

# 2026-05-06 D2-C 회복용 (503 retry)
JETRAG_GEMINI_RETRY=3
```

`web/.env`:
```bash
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_SUPABASE_URL=https://mpmtydudhojpukuuadrd.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon JWT>
```

자세한 변수 매트릭스 — 짝 문서 [`2026-05-06 푸시 안 된 환경 설정 정리...`](./2026-05-06%20푸시%20안%20된%20환경%20설정%20정리%20다른%20컴퓨터%20동기화용.md)

### 6.3 baseline 검증

```bash
cd api && uv run python -m unittest discover tests
# 기대: Ran 460 tests / OK

cd ../web && pnpm exec tsc --noEmit && pnpm lint
# 기대: 0 error
```

### 6.4 Supabase 마이그 상태 확인

위 §2.1 의 검증 SQL 실행 → 6개 테이블/컬럼 정합 확인. 누락 시 §2.1 의 마이그 적용.

### 6.5 dev 시작

```bash
# Terminal 1
cd api && uv run uvicorn app.main:app --reload

# Terminal 2
cd web && pnpm dev   # localhost:3001
```

### 6.6 진입 후 첫 행동 (5분 안 컨텍스트 회복)

1. **본 문서 §0·§1·§4 읽기** (오늘 ship + 다음 후보)
2. `git log --oneline -20` 으로 최근 commit 확인 (마지막 = `c717ea8`)
3. 사용자 결정 받기 — 다음 진입 후보 (D2-B / D4 / S1 D5)
4. 진입 시 senior-developer + senior-qa 에이전트 의뢰

---

## 7. 활성 한계 (sprint 진입 전 점검)

| # | 한계 | 영향 | 회복 절차 |
|---|---|---|---|
| 1 | 503 fail rate 검증 부족 | vision 인제스트 부분 실패 가능 | 사용자 PDF 재인제스트 → retry_attempt 분포 SELECT |
| 2 | thinking-exp 모델 ID 미검증 | reasoning purpose 미사용 (현재는 2.5-flash 사용) | S3 cross-doc 진입 시 검증 |
| 3 | 단가 dict 가격 변경 추적 | estimated_cost 부정확 가능 | 분기별 https://ai.google.dev/pricing 재확인 |
| 4 | e2e_pipeline 테스트 mock 한계 | 실 LLM 응답 형식 변화 회귀 미검증 | 사용자 PDF 인제스트 시 직접 확인 |

---

## 8. 참고 문서 우선순위

다른 컴퓨터 진입 시 본 문서 → 아래 순서로 읽기:

| # | 문서 | 목적 |
|---|---|---|
| 1 | **본 문서** | 2026-05-06 D1~D2 누적 + 다음 진입 (master) |
| 2 | [`2026-05-06 무료유료 모델 전략 통합 plan + 다른 컴퓨터 핸드오프.md`](./2026-05-06%20무료유료%20모델%20전략%20통합%20plan%20+%20다른%20컴퓨터%20핸드오프.md) | Sprint S0~S5 master plan + 모델 배치 + 골든셋 가이드 |
| 3 | [`2026-05-06 푸시 안 된 환경 설정 정리 다른 컴퓨터 동기화용.md`](./2026-05-06%20푸시%20안%20된%20환경%20설정%20정리%20다른%20컴퓨터%20동기화용.md) | `.env` 변수 매트릭스 + secret 동기화 절차 |
| 4 | [`2026-05-06 Phase 1 S0 D1 LLMAdapter 팩토리 + 마이그 014_015.md`](./2026-05-06%20Phase%201%20S0%20D1%20LLMAdapter%20팩토리%20+%20마이그%20014_015.md) | D1 ship 상세 (LLMAdapter 도입 + 마이그 014/015) |
| 5 | [`2026-05-05 W25 D14+1 종합 + 다음 세션 진입 핸드오프.md`](./2026-05-05%20W25%20D14+1%20종합%20+%20다음%20세션%20진입%20핸드오프.md) | 어제까지 baseline (W25 마감) |
| 6 | [`검색 파이프라인 동작 명세 (living).md`](./검색%20파이프라인%20동작%20명세%20(living).md) | 인제스트·검색·표시 동작 (living, S3 ship 시 갱신) |
| 7 | [`2026-04-22 개인 지식 에이전트 기획서 v0.1.md`](./2026-04-22%20개인%20지식%20에이전트%20기획서%20v0.1.md) | 페르소나·KPI·DoD 마스터 |

---

## 9. 핵심 결정 누적 (사후 점검용)

| # | 결정 | 근거 | 근거 문서 |
|---|---|---|---|
| 1 | 모든 paid LLM = Gemini default 유지 | 가격·성능 비교 결과 OpenAI 명확 우위 영역 0개 | master plan §1 |
| 2 | OpenAI key = Adapter 인터페이스 + 미래 보험 | "성능 차이 크게 없으면 저렴한 모델" 사용자 기준 | master plan §1 |
| 3 | Phase 1 + Phase 2 병렬 진입 | 의존성 0 + 시간 절감 | 본 문서 §1.1 |
| 4 | senior-planner Option A (기존 Protocol 유지 + 보강 3건) | 회귀 위험 최소 + master plan §6/§7 정합 | D1 work-log |
| 5 | sweep 2 + retry 1 default + ENV override | master plan §7.3 정합, ENV 회복 가능 | D2-C 결정 |
| 6 | 모델 2.5-flash + 2.5-flash-lite 채택 | 2.0 deprecated 확인 후 안전 모델 | D2-D 정정 |
| 7 | force push 로 사용자 자료 history 제거 | public repo 노출 위험 | 본 문서 §1.1 |
| 8 | retry_attempt 컬럼 활성화 (P2) | 503 회복 시점 추적 가능 | P2 ship |

---

## 10. 한 문장 요약

> 2026-05-06 D1~D2 1주 분량을 1일에 압축 — 17 commit, 460 통과 회귀 0, master plan §6 의 S0~S2 인프라 (마이그 + factory + 단가 + retry_attempt + .gitignore) 모두 ship. 다음 권고 = 사용자 PDF 재인제스트로 retry_attempt 검증 + D2-B (캐시 통합) 또는 S1 D5 (2.5-flash vs lite 측정). 다른 컴퓨터 진입은 본 문서 → §6 절차 5분이면 baseline 회복.
