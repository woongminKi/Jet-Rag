# 2026-05-09 종합 + 2026-05-10 다른 PC 진입 핸드오프

> 프로젝트: Jet-Rag
> 작성: 2026-05-09 마감 시점 (S4-A D3 Phase 1 ship 직후)
> 목적: **다른 PC** 에서 본 1장만 보고 5분 안 컨텍스트 회복 + S4-A sprint 이어 진입 가능. 본 PC (사용자 데스크탑) 의 11 docs 자료 위치 + Supabase 적재 상태 + 어제까지 누적 통합.

---

## 0. 한 줄 요약

> **2026-05-09 — 26 commit push 완료** (`64e59b6` ~ `4aa2d87`, origin/main). **Master plan §6 — S2 95% / S3 100% ✅ / S4-A 25% (D1+D2+D3 Phase 1) + S1.5 v3 multi-line table 휴리스틱 ship**. 단위 테스트 632 → **746 / skipped 1 / 회귀 0** (+114). 다른 PC 진입 시 1순위 권고 = **S4-A D4 (golden_v2 R@10 재측정, qtype × caption × v1↔v2 3축 breakdown)**. **Q-S4-A-D3 Phase 2/3 (cross_doc 보강 / 다른 PC 자료) 사용자 명시 진입 결정 보류**.

---

## 1. 오늘 누적 ship (26 commit, 13 sprint, 모두 origin push 완료)

| # | sprint | commit | 핵심 변경 | 단위 테스트 |
|---|---|---|---|---:|
| 1-2 | **S2 D3 + P1-1** | `64e59b6` + `aeb0807` | 운영 모드 UI 토글 (fast/default/precise) + page cap mode 매핑 + P1-1 sweep default 통일 | 632 → 654 (+22) |
| 3-4 | **S2 D4 ablation** | `4bc23e5` + `37b85b7` | vision_need_score threshold 튜닝 — 본 PC N=3 한계 → S1.5 v3 진입 결정 | 654 → 667 (+13) |
| 5-6 | **S1.5 v3 + P1 fix 4건** | `735ed27` + `514c368` | multi-line table 휴리스틱 — 데이터센터 p.40 D4 0/11 → v3 11/15 catch | 667 → 678 (+11) |
| 7-8 | **S2 D5 phase 1** | `e2c5250` + `46ea446` | sample-report reingest 사전 점검 + 실 호출 — vision_call=8 / cost=$0.111 / per-doc cap $0.10 정상 발동 / 35p 잔존 | 678 → 684 (+6) |
| 9-10 | **R@10 도구 fix (acceptable_chunks)** | `90eaa70` + `85d2145` | binary 1.000 → graded 0.4 → 분모 정합화 0.336 (G-A-021 false negative 제거) | 684 → 689 (+5) |
| 11-12 | **S3 D1 intent_router (7 trigger)** | `5a41735` + `720414f` | T1 cross-doc / T2 비교 / T3 인과 / T4 변경점 / T5 긴 query / T6 low confidence / T7 복수 대상 | 689 → 698 (+9) |
| 13-14 | **S3 D2 fast path + confidence** | `dc47c38` + `5f46527` | meta_filter_fast_path + /search /answer 와이어 + intent_router 통합 (low_confidence 가드) | 698 → 710 (+12) |
| 15-16 | **S3 D3 query decomposer** | `5bbf7a0` + `ba113ac` | gated paid (default OFF) + LRU 200 + monthly cap $0.30 + RRF merge k=60 | 710 → 719 (+9) |
| 17-18 | **S3 D4 reranker** | `b20a504` + `56373b7` | candidate cap=20 + LRU cache 500 + monthly degrade + MMR λ=0.7 (cross_doc only) | 719 → 732 (+13) |
| 19-20 | **S3 D5 정량 측정** | `674d9b4` + `78098fb` | 골든셋 v1 × 3 combo × 7 metric — combo a R@10=0.7172 (목표 0.75 의 -0.003), b/c mock random 0.4751 폭락 | 732 (mock 한정) |
| 21-22 | **S4-A D1 vision prompt 5필드 + cache v2** | `f314a5a` + `82a3a78` | table_caption / figure_caption 추가 + cache v1→v2 bump (auto invalidate via lookup miss) | 732 → 735 (+3) |
| 23-24 | **S4-A D2 metadata 전파 + chunk.text 합성** | `aa92d25` + `c589994` | (a) text 합성 옵션 채택 — vision-derived chunk 한정 `[표: ...]` / `[그림: ...]` 부착 | 735 → 739 (+4) |
| 25-26 | **S4-A D3 Phase 1 골든셋 v2** | `4ecb71a` + `4aa2d87` | doc 매칭 fix 22건 (fail 잔존 0) / NFC 정규화 / caption_dependent 18 / doc_type 5종 cover / `--goldenset {v1\|v2}` 듀얼 지원 | 739 → 746 (+7) |

**누적 단위 테스트**: 632 → **746 / skipped 1 / 회귀 0** (+114)
**web tsc + lint**: 0 error (S2 D3 의 운영 모드 UI 토글 / VisionPageCapExceededCard 만 web 변경, lint 통과)

---

## 2. Master plan §6 sprint 진척률 (2026-05-09 마감 시점)

| Sprint | 목표 | 진척률 | 마감일 | 비고 |
|---|---|---:|---|---|
| **S0** 비용 계측 / 캐시 / cap | 1주 | **100% ✅** | 2026-05-07 | (어제와 동일) |
| **S1** 골든셋 + 실 query 로그 | 1주 | **100% ✅** | 2026-05-07 | (어제와 동일) |
| **S1.5** vision_need_score PoC | 3일 | **100% ✅** | 2026-05-07 | (어제와 동일) |
| **S1.5 v3** multi-line table 휴리스틱 | 0.5일 | **100% ✅** | 2026-05-09 | 데이터센터 p.40 catch — D4 0/11 → v3 11/15 회수 |
| **S2** Vision 선별 + budget 본 ship | 1주 | **95%** (D5 phase 1) | 2026-05-09 | D1+D2+D3+D4 ship + D5 phase 1 만 완료. phase 2/3 보류 (사용자 명시 진입 시) |
| **S3** Cheap router + decomposition + reranker | 1주 | **100% ✅ (mock-reranker 한정 KPI 보류)** | 2026-05-09 | D1~D5 ship. 실 reranker D6 별도. combo a R@10=0.7172 만 유효 |
| **S4** 인제스트 보조 인덱스 | 1~2주 | **~25%** | 진행 중 | S4-A D1+D2+D3 Phase 1 ship. D3 Phase 2/3 + D4/D5 + S4-B/C/D 후속 |
| **S5** /answer UX | 1주 | 0% | — | |

### S0 효과 (어제와 동일)
- vision worst case 450 → 50 호출
- reingest cache 0 호출
- 폭주 3중 차단 + 빌링 cross-check

### S1.5 v3 효과 (오늘 신규)
- 데이터센터 p.40 single-line table — D4 ablation 0/11 → v3 **11/15 catch**
- multi-line table 구조적 사각지대 회수 (block bbox cluster + caption regex 정밀화)
- 단 baseline R@10=1.000 chunk 374 는 text-only 정합 — v3 catch 가 회귀 0 (실 reingest 시점 검증 후속)

### S2 D5 phase 1 효과 (오늘 신규)
- sample-report reingest — vision_call=8 / cost=$0.111 / per-doc cap $0.10 **정상 발동**
- 35p 잔존 (의도된 가드, phase 2 cap 상향 또는 다른 PDF 진입 후 회수)

### S3 D5 효과 (mock-reranker 한정 — KPI 판정 보류)
- combo a (RRF-only baseline) **R@10=0.7172** (목표 0.75 의 -0.003)
- combo b/c (mock random reranker) **R@10=0.4751** 폭락 — 실 BGE-reranker 측정 D6 까지 보류
- R@10 도구 fix (acceptable_chunks 분모 정합화) — G-A-021 0.000 → 0.182 회복

### S4-A D1+D2+D3 Phase 1 효과 (오늘 신규)
- vision prompt 5필드 (table_caption / figure_caption 추가, ≤60자, default null)
- cache v1→v2 bump — v1 cache row 자동 회피 (`prompt_version` 키)
- chunk.text 합성 — vision-derived chunk 한정 `[표: ...]` / `[그림: ...]` 부착, 검색 path 변경 0
- golden v2 (157 row × 14 컬럼) — doc 매칭 fail **0** / caption_dependent 18 (11.5%) / qtype 9 × dtype 5 cover

---

## 3. 다른 PC 진입 절차 (5분)

### 3.1 코드 + work-log 동기화

```bash
git clone https://github.com/woongminKi/Jet-Rag.git    # 첫 진입
# 또는
cd Jet-Rag && git pull origin main                     # 이미 있으면

cd Jet-Rag
git log --oneline -5
# 기대: 4aa2d87 / 4ecb71a / c589994 / aa92d25 / 82a3a78 (현재 origin head)

# 의존성
cd api && uv sync
cd ../web && pnpm install

# 단위 테스트 baseline
cd ../api && uv run python -m unittest discover tests
# 기대: Ran 746 tests, OK (skipped=1)

# web 검증
cd ../web && pnpm exec tsc --noEmit && pnpm lint
# 기대: 0 error
```

### 3.2 환경 변수 (`api/.env`)

어제 §3.2 ENV 6종 (Supabase / Gemini / HF / Default / W25 / S0) 유지 + 오늘 신규 추가:

```bash
# ── 어제까지 누적 (재인용) ──
SUPABASE_URL=https://mpmtydudhojpukuuadrd.supabase.co
SUPABASE_KEY=<anon>
SUPABASE_SERVICE_ROLE_KEY=<service_role>
GEMINI_API_KEY=<gemini>
HF_API_TOKEN=<hf>
JETRAG_PDF_VISION_ENRICH=true
JETRAG_GEMINI_RETRY=3
JETRAG_VISION_MAX_SWEEPS=2
# JETRAG_DOC_BUDGET_USD=0.10           # default
# JETRAG_DAILY_BUDGET_USD=0.50         # default
# JETRAG_24H_BUDGET_USD=0.50           # default = daily
# JETRAG_VISION_NEED_SCORE_ENABLED=true  # default true
# JETRAG_VISION_PAGE_CAP_PER_DOC=50      # default 50

# ── 오늘 신규 (S2 D3 ship) ──
# JETRAG_VISION_MODE_FAST_CAP=10         # default 10 (fast 모드)

# ── 오늘 신규 (S3 D3 ship — paid decomposition, default OFF) ──
# JETRAG_PAID_DECOMPOSITION_ENABLED=false      # default false (paid 토글)
# JETRAG_DECOMPOSITION_MONTHLY_CAP_USD=0.30    # default 0.30
# JETRAG_DECOMPOSITION_CACHE_DISABLE=0          # default 0 (LRU 200)

# ── 오늘 신규 (S3 D4 ship — reranker 4중 가드) ──
# JETRAG_RERANKER_CANDIDATE_CAP=20             # default 20
# JETRAG_RERANKER_CACHE_DISABLE=0               # default 0 (LRU 500)
# JETRAG_RERANKER_MONTHLY_CAP_CALLS=1000       # default 1000
# JETRAG_RERANKER_DEGRADE_THRESHOLD=0.8        # default 0.8
# JETRAG_MMR_LAMBDA=0.7                         # default 0.7
# JETRAG_MMR_DISABLE=0                          # default 0

# ── 오늘 신규 (S4-A D1 ship — vision prompt v2) ──
# JETRAG_VISION_PROMPT_VERSION=v2              # default v2 (v1 → v2 bump, cache 자동 invalidate)
```

`web/.env` — 변경 없음.

### 3.3 본 PC 의 11 docs 자료 (다른 PC 에는 없음)

본 PC `<repo>/` 루트 직속 사용자 자료 11건 — 어제 §3.3 그대로:

- PDF 5: `(붙임2) 2025년 데이터센터 산업 활성화 지원 사업 통합_안내서.pdf` / `law sample3.pdf` / `sonata-the-edge_catalog.pdf` / `보건의료_빅데이터_플랫폼_시범사업_추진계획(안).pdf` / `포트폴리오_이한주 - na Lu.pdf`
- HWP 1: `law sample2.hwp`
- HWPX 2: `직제_규정(2024.4.30.개정).hwpx` / `한마음생활체육관_운영_내규(2024.4.30.개정).hwpx`
- DOCX 2: `승인글 템플릿1.docx` / `승인글 템플릿3.docx`
- PPTX 1: `브랜딩_스튜디오앤드오어.pptx`

`.gitignore` 의 `/*.{pdf,hwp,hwpx,docx,pptx}` 로 git 추적 X. 다른 PC 동기화 옵션:

- **A. USB / 외장 디스크** → `<repo>/` 루트 또는 `<repo>/assets/` 직속 복사 (자동 인식, ENV 0)
- **B. iCloud 등 사용자 클라우드** → 자동 동기화
- **C. Supabase 만 활용** — chunks 이미 적재 → 실 reingest 안 하고 retrieval 만 측정 (S4-A D4 진행 가능)

`assets/public/` 8건 (PDF 4 + HWPX 2 + HWP 1 + sample-report) 은 git 추적 → `git pull` 만으로 자동 회귀.

**오늘 신규 — golden v2 적재 분포** (`evals/golden_v2.csv`, S4-A D3 Phase 1 ship):
- 157 row × 14 컬럼 (v1 12 컬럼 + `doc_type` / `caption_dependent`)
- doc 매칭 fail **0** / cross_doc 7 / caption_dependent 18
- doc_type 5종 cover: pdf 89 / hwpx 24 / docx 22 / hwp 12 / pptx 5
- qtype 9 cover (exact_fact 113 / summary 9 / numeric 7 / fuzzy 6 / table 6 / out_of_scope 5 / synonym 4 / cross_doc 4 / vision 3)
- **옵션 C 만으로도 S4-A D4 진행 가능** — Supabase chunks 적재 + golden v2 git 추적

### 3.4 dev 시작

```bash
# Terminal 1
cd api && uv run uvicorn app.main:app --reload

# Terminal 2
cd web && pnpm dev
```

---

## 4. 다음 후보 우선순위 (남은 작업 plan 메모리 반영)

### 4.1 1순위 — S4-A D4 (golden_v2 R@10 재측정)

**왜?** S4-A D1+D2+D3 Phase 1 ship 직후 — vision prompt v2 + chunk.text 합성 + golden v2 가 모두 갖춰진 시점. 검색 path 변경 0 이므로 **순수 데이터/프롬프트 효과만 측정** 가능. 옵션 C (Supabase 만 활용) 로도 충분 진행.

**작업 (1~1.5일 ETA)**:

| 항목 | 설계 결정 |
|---|---|
| 측정 도구 | `evals/run_s2_d4_pre_regression.py --goldenset v2` (S4-A D3 듀얼 지원 활용) |
| 3축 breakdown | (1) qtype 별 (9종) (2) caption_dependent subset (18 row) (3) prompt v1 vs v2 (cache invalidate 후 신규 row 만) |
| 외부 API cost | 0 (BGE-M3 무료, Gemini 호출 0, vision 호출 0) — D5 reingest 진입 전 단계 |
| 산출물 | `evals/results/s4_a_d4_results.md` + `s4_a_d4_raw.json` (S3 D5 패턴 그대로) |
| 단위 테스트 | qtype breakdown 도구 + caption subset 분리 + 회귀 보호 |

**진입 조건**: 사용자 명시 진입 (옵션 C 만이면 외부 API cost 0 — 즉시 가능)

### 4.2 2순위 — S4-A D5 reingest phase

**왜?** D4 결과가 prompt v2 의 효과를 입증하면 v1 cache 폐기 → v2 신규 reingest. **vision API cost ~$0.05** (sample-report 기반 추정).

**진입 조건**: 사용자 명시 승인 (cost 동반).

### 4.3 3~10순위

| # | 후보 | 작업량 | 진입 조건 | 권고도 |
|---|---|---|---|---|
| 3 | **S4-A D3 Phase 2** cross_doc 보강 | 0.5~1일 | 사용자 명시 진입 (cross_doc 4 → 8~10 확장) | ★★ |
| 4 | **S4-B** 핵심 엔티티 추출 (룰 + Flash-Lite) | 3일 | S4-A D5 후 | ★★ |
| 5 | **S4-C** section 요약 | 3일 | S4-B 후 | ★★ |
| 6 | **S4-D** 동의어 후보 사전 확장 | 2일 | S4-C 후 | ★★ |
| 7 | **S5** /answer UX | 1주 | S4 마감 후 | ★★ |
| 8 | **S2 D5 phase 2** (조건부) | 0.5일 | per-doc cap 상향 결정 + 데이터센터/sample-report 35p 잔존 회수 필요 시 | ★ |
| 9 | **S3 D6 실 BGE-reranker 재측정** | 0.5~1일 | 실 reranker 측정 후 KPI 판정 — combo b/c 회복 가설 검증 | ★ |
| 10 | **P2 7건 (deferred)** | 가변 | 우선순위 낮은 fix 묶음 | ★ |

---

## 5. 활성 한계 (다른 PC 진입 시 점검)

| # | 한계 | 영향 | 회복 절차 |
|---|---|---|---|
| 1 | **데이터센터 p.40 single-line table** | v3 catch 11/15 회수, 단 baseline R@10=1.000 chunk 374 는 text-only | S2 D5 phase 2 ENV ON 검증 (per-doc cap 상향 시) |
| 2 | **S2 D5 phase 1 35p 잔존** | per-doc cap $0.10 정상 발동 — 의도된 가드 | Phase 2 cap 상향 또는 다른 PDF 진입 후 회수 |
| 3 | **S3 D5 mock-reranker 한정** | KPI 판정 보류 (combo a R@10=0.7172 만 유효, b/c 0.4751 폭락) | D6 실 BGE-reranker 재측정 |
| 4 | **golden v2 caption_dependent proxy** | substring rule, v1 reingest 전 한정 | S4-A D5 reingest 후 metadata 직접 활용 |
| 5 | **golden v2 minority qtype 표본 작음** | cross_doc 4 / vision 3 / synonym 4 | Phase 2 cross_doc 보강 + Phase 3 다른 PC 자료 진입 |
| 6 | **G-A-107/111 search empty** | 포트폴리오 chunk page DB NULL — 별개 이슈 | S3 D5 통합 측정 시 fix |
| 7 | **BGE-M3 HF API timeout 7~8회** | G-U-005/G-U-007 fail-safe | HF inference 안정성 모니터링 |
| 8 | **E1 진단 미완** (어제 §5 #6 그대로) | ETA 정밀화 (A2/A3/A4/A5) | PDF reingest 가능 PC 진입 시 |

---

## 6. 사용자 결정 보류 항목

| # | 항목 | 권고 default | 결정 시점 |
|---|---|---|---|
| Q-S2-D5-3 | Phase 2 진입 시점 | S3 D5 후 R@10 회귀 측정 통합 | S4-A D4 결과 후 |
| Q-S3-D1-1~4 | router 임계 정합 | D5 골든 분포 측정 후 보정 | S4-A D4 후 |
| Q-S3-D3 ENV ON | paid decomposition (~$0.30/월) | 사용자 명시 ON 시점 | 별도 phase |
| Q-S3-D5 (b vs c) production default | 실 reranker 측정 후 | D6 |
| Q-S4-A-D3 Phase 2/3 | cross_doc 보강 / 다른 PC 자료 | 사용자 명시 진입 |
| Q-S4-A-D4 측정 시점 | **즉시 가능 (옵션 C, cost 0)** | 사용자 진입 결정 |
| Q-S4-A-D5 reingest 시점 | D4 결과 후 + 사용자 cost 명시 승인 | 별도 phase |
| ~~Q-S2-2 11 docs 재인제스트~~ | ~~다른 PC~~ | 본 PC 일부 적재 (golden v2 doc_type 5/5) — **일부 해소** |
| Q-S1.1 | 2.5-flash vs lite factory | S1.1 100+ 재측정 후 | 별도 sprint |
| Q-E1 | 정확도 vs latency 우선순위 | (a) 정확도 P0 | E1 1차 ship 진입 시 |

---

## 7. 참고 문서 우선순위 (5분 회복)

| # | 문서 | 5분 행동 |
|---|---|---|
| 1 | **본 문서 §0 / §3 / §4** | 마감 상태 + 진입 절차 + 다음 후보 |
| 2 | `2026-05-09 S4-A D3 Phase 1 골든셋 v2 보강.md` | golden v2 ship 본문 — 22 doc fix / NFC bug / qtype·dtype 분포 |
| 3 | `2026-05-09 S4-A D2 metadata 전파 + 검색 path 합성.md` | (a) text 합성 옵션 / vision-derived chunk 한정 / 검색 path 변경 0 |
| 4 | `2026-05-09 S4-A D1 vision prompt 5필드 + cache invalidate.md` | 5필드 spec + cache v2 invalidate 메커니즘 |
| 5 | `2026-05-09 S3 D5 정량 측정 + S3 종합 핸드오프.md` | S3 D1~D5 통합 — combo a/b/c × 7 metric / mock 한정 한계 |
| 6 | `2026-05-09 S2 D5 phase 1 sample-report reingest.md` | per-doc cap $0.10 정상 발동 / 35p 잔존 |
| 7 | `2026-05-08 종합 + 2026-05-09 진입 핸드오프.md` | 어제 마감 (S0+S1+S1.5 100% + S2 D1+D2+D4-pre) |
| 8 | `2026-04-22 개인 지식 에이전트 기획서 v0.1.md` | 페르소나 A · KPI · DoD 마스터 |
| 9 | `2026-05-06 무료유료 모델 전략 통합 plan + 다른 컴퓨터 핸드오프.md` | Master plan §6 의 6 sprint 정의 (S0~S5) |

---

## 8. 핵심 변경 파일 목록 (오늘 26 commit, 13 sprint)

### 신규 (services/scripts/tests/work-log)
- `api/app/services/intent_router.py` (S3 D1) — 7 trigger 룰 + IntentRouterDecision
- `api/app/services/meta_filter_fast_path.py` (S3 D2) — 룰 detector + Supabase SELECT
- `api/app/services/query_decomposer.py` (S3 D3) — gated paid + LRU 200 + budget guard
- `api/app/services/reranker_cache.py` (S3 D4) — OrderedDict LRU 500 + sha1 hex 16자 키
- `api/app/services/mmr.py` (S3 D4) — λ=0.7 greedy MMR + cosine sim
- `api/app/services/ingest_mode.py` (S2 D3) — IngestMode + resolve_page_cap
- `web/src/components/jet-rag/ingest-mode-select.tsx` (S2 D3)
- `web/src/components/jet-rag/cards/vision-page-cap-exceeded-card.tsx` (S2 D3)
- `evals/run_s2_d4_threshold_ablation.py` (S2 D4, 1067 LOC)
- `evals/run_s3_d5_search_stack_eval.py` (S3 D5, 954 LOC)
- `evals/build_golden_v2.py` (S4-A D3 Phase 1, 817 LOC)
- `evals/golden_v2.csv` (157 row × 14 컬럼)
- `api/tests/test_*.py` 다수 (총 +114 단위 테스트)
- work-log 12건 (S2 D3 / S2 D4 / S1.5 v3 / S2 D5 phase 1 / S3 D1 / S3 D2 / S3 D3 / S3 D4 / S3 D5 / S4-A D1 / S4-A D2 / S4-A D3 Phase 1)
- 본 핸드오프 1건

### 수정
- `api/app/adapters/impl/gemini_vision.py` — S4-A D1 _PROMPT 5필드
- `api/app/adapters/vision.py` — S4-A D1 VisionCaption 2필드
- `api/app/adapters/parser.py` + `api/app/adapters/impl/image_parser.py` — S4-A D2 caption section 키 부착
- `api/app/services/vision_cache.py` — S4-A D1 cache v2 invalidate
- `api/app/services/budget_guard.py` — S2 D3 mode → page cap 매핑 + S3 D3 query_decomposition scope + S3 D4 reranker degrade
- `api/app/ingest/stages/extract.py` — S2 D3 mode override + S4-A D2 metadata 보존 + S4-A D2 caption propagation
- `api/app/ingest/stages/chunk.py` — S4-A D2 `_compose_vision_chunk_text` 헬퍼 + `_is_vision_section` 분기
- `api/app/ingest/incremental.py` — S2 D3 mode override + S4-A D2 caption 전파
- `api/app/ingest/pipeline.py` + `api/app/ingest/upload.py` — S2 D3 mode 인자 흐름
- `api/app/routers/documents.py` — S2 D3 4 엔드포인트 mode 인자
- `api/app/routers/search.py` — S3 D2 fast path + S3 D4 reranker 4 path 분기 + MMR
- `api/app/routers/answer.py` — S3 D2 intent_router wire + S3 D3 decomposer 분기 + S3 D4 X-Reranker-Path 헤더
- `api/app/services/vision_need_score.py` — S1.5 v3 block-level fallback + caption regex 정밀화
- `evals/run_s2_d4_pre_regression.py` — R@10 도구 fix (acceptable_chunks 전달) + S4-A D3 `--goldenset` 듀얼
- `web/src/lib/api/types.ts` + `index.ts` — S2 D3 IngestMode 타입 + 시그니처 확장
- `web/src/app/doc/[id]/page.tsx` + `web/src/components/jet-rag/ingest-ui.tsx` — S2 D3 통합
- `.env.example` — S2 D3 + S3 D3 + S3 D4 + S4-A D1 ENV 매트릭스 추가

---

## 9. 다른 PC 진입 후 첫 명령 (즉시 실행)

```bash
# 1) 동기화 + baseline (3분)
cd Jet-Rag && git pull origin main
cd api && uv sync && uv run python -m unittest discover tests
# 기대: Ran 746 tests, OK (skipped=1)

# 2) S4-A D4 진입 (1순위, 1~1.5일 ETA, 외부 API cost 0)
# senior-developer 에 의뢰 — golden_v2 R@10 3축 breakdown 측정
# - qtype 별 (9종 — exact_fact / cross_doc / vision / synonym 등)
# - caption_dependent subset (18 row) 분리
# - v1 vs v2 prompt 비교 (cache invalidate 후 신규 row 한정)
# - 옵션 C (Supabase 만 활용) 진입 가능 — vision/Gemini 호출 0
# - 산출물: evals/results/s4_a_d4_results.md + s4_a_d4_raw.json
# - 단위 테스트 + work-log
```

---

## 10. 한 문장 마감

> **2026-05-09 마감 — 26 commit push 누적 (`64e59b6` ~ `4aa2d87`)**. **S2 95% / S3 100% (mock-reranker 한정 KPI 보류) / S4-A 25% (D1+D2+D3 Phase 1) + S1.5 v3 ship**. 단위 테스트 632 → **746** / skipped 1 / 회귀 0 (+114). golden v2 (157 row × 14 컬럼) doc 매칭 fail 0 + caption_dependent 18 + doc_type 5/5 cover 완료. S3 D5 mock 측정 — combo a R@10=0.7172 (목표 -0.003), b/c 폭락 (실 reranker D6 보류). S4-A D1+D2 — vision prompt 5필드 + chunk.text 합성, 검색 path 변경 0. 다른 PC 진입 시 §3 절차 5분 + §4.1 의 **S4-A D4 golden_v2 R@10 3축 breakdown 측정** (1순위, 옵션 C 만으로 외부 API cost 0, 1~1.5일 ETA). 사용자 자료 11 docs 동기화는 §3.3 옵션 A/B/C 중 선택 — 옵션 C 는 Supabase 적재 + golden v2 git 추적으로 **D4 즉시 가능**.
