# 2026-04-24 W1 마감 검증 + W2 진입 준비

> W1 Day 6-7+ 24 커밋 + 종합 정리 §0 을 마친 다음 날. **사용자 브라우저 검증 체크리스트** 를 남겨두고, `.gitignore` 정리 + origin push + W2 명세 준비로 이어간다.

---

## 0. 오늘 흐름 요약

| # | 액션 | 결과 |
|---|---|---|
| 1 | `2026-04-23 W1 Day 6-7+ 종합 정리.md` §0 읽고 현재 상태 점검 | 작업트리 clean, origin 대비 26 커밋 앞 (§0 작성 시점 25 + 종합정리 commit 1) |
| 2 | untracked `docs/jet-rag wireframe/` (v0 원본 Next.js 프로젝트, 4/23 16:13 유입) → `.gitignore` 추가 | 로컬 참고용으로만 유지 |
| 3 | origin push (26 커밋) | 다른 컴퓨터 동기화 기준선 확보 |
| 4 | `.gitignore` 변경분 별도 커밋 | repo 청소 |
| 5 | 사용자 검증 — 별도 진행 (이 문서 §2 체크리스트) | — |
| 6 | W2 명세 senior-planner 위임 | W2 진입 준비 |

---

## 1. 진입 순서 결정 (§0.5 A 방식)

사용자 선택: **검증 먼저 직접 돌려본 뒤 이슈 없으면 push → W2** (A 안).
`.gitignore` 커밋 분리: **push 이후 별도 처리** (B 안).

실제 실행 순서:
1. `.gitignore` 수정 (working tree 에만 유지)
2. origin push (26 커밋) — .gitignore 변경 미포함
3. `.gitignore` 별도 커밋 → 후속 push
4. W2 명세 위임

검증은 사용자가 직접 하기로 하고, 이 문서에 체크리스트 남김.

---

## 2. 사용자 검증 체크리스트 (별도 실행)

### 2.1 두 서버 동시 기동

```bash
# 터미널 1 — API (http://localhost:8000)
cd ~/Desktop/piLab/Jet-Rag/api
uv run uvicorn app.main:app --reload

# 터미널 2 — Web (http://localhost:3000)
cd ~/Desktop/piLab/Jet-Rag/web
pnpm dev
```

### 2.2 시나리오 10건

| # | 액션 | 기대 결과 | OK? |
|---|---|---|---|
| 1 | <http://localhost:3000> 진입 | Hero + 2컬럼 그리드, OKLCH primary 톤 + Noto Sans KR | ☐ |
| 2 | 검색 인풋에 "반도체" → Enter | `/search?q=반도체` 결과 카드 + Highlighted `<mark>` | ☐ |
| 3 | 홈 인기 태그 클릭 | `/search?q=<태그>` 라우팅 + 결과 표시 | ☐ |
| 4 | DevTools 모바일 뷰 → 햄버거 토글 | 헤더 아래 패널 (검색 인풋 + 업로드 CTA) | ☐ |
| 5 | `/ingest` → 정상 PDF 드롭 | 7-stage 진행 (extract → chunk → tag_summarize → load → embed → doc_embed → dedup) + 완료 success 색 | ☐ |
| 6 | 같은 파일 재드롭 (정상 doc) | "이미 같은 내용이 등록되어 있어요" warning | ☐ |
| 7 | 작년 `sonata-the-edge_catalog.pdf` 다시 드롭 (`flags.failed=true` 상태) | 자동 reset → 새 파이프라인 시작 → 정상 완료 | ☐ |
| 8 | 실패한 doc 카드에서 "재시도" 버튼 클릭 | `POST /documents/{id}/reingest` 호출 → 새 폴링 시작 | ☐ |
| 9 | <http://localhost:8000/docs> Swagger UI | 7 엔드포인트 전부 노출, `?include_failed` 쿼리 옵션 확인 | ☐ |
| 10 | `GET /stats` Execute | `popular_tags`(top-10), `added_this_month`, `added_last_7d`, `failed_count` 필드 노출 | ☐ |

### 2.3 이슈 발견 시 처리

- 시나리오 번호 + 실제 동작 + 기대 동작 을 work-log 에 추가 작성
- W2 착수 전에 fix 커밋 우선
- Critical (e.g., 검색 결과 안 뜸, 폴링 멈춤) → 즉시 senior-developer 위임
- Minor (디자인 톤, 문구) → W2 사이클에 일괄 반영

### 2.4 알려진 스냅샷 (작성 시점)

- Supabase `documents` 에 `sonata-the-edge_catalog.pdf` (`flags.failed=true`) 1 건 — 시나리오 7 로 자연 회복 가능
- 회복 후 기대: `failed_count` = 0, `total_documents` = 실제 정상 문서 수

---

## 3. Push 결과 (이 문서 commit 이후 실행 예정)

- 명령: `git push origin main`
- 예상 반영 커밋: 26 (§0 종합 정리 포함)
- `.gitignore` 변경분은 이 push 이후 별도 커밋

---

## 4. W2 진입 계획 (초안)

W1 종합 정리 §6 "W2 이월" 항목 중 W2 범위:

1. **멀티포맷 어댑터** — HWP / HWPX / DOCX / PPTX / 이미지 / URL
2. **Vision 경로** — Gemini 2.5 Flash 내장 OCR
3. **입력 게이트 B** — 콘텐츠 게이트
4. **업로드 SLO** — 8.6MB 3.7초 → BackgroundTask 분리
5. **쿼리 분석 row** — NL → DSL (W2 착수 후반 또는 W3 이월 가능)
6. **동시 업로드 N개 폴러 최적화** — 단일 폴러 + batch status

위 목록을 senior-planner 에 명세 위임 → W2 kick-off 문서 확보 후 senior-developer 착수.

---

## 5. 다음 세션 진입점

검증 결과 + push 결과 확인 후:

- (A) 검증 전부 OK → W2 명세 위임 → senior-planner 산출물 검토
- (B) 이슈 발견 → fix 커밋 → 재검증 → A 루트 재진입

어느 쪽으로 빠져도 다음 세션 시작은 **이 문서 §2 체크 상태 + §4 W2 명세 초안** 부터 읽으면 된다.
