# 2026-05-03 W16 4 Day + W16 종합 + W17 진입 핸드오프

> **다음 세션 5분 안 진입 자료.**
> 마지막 commit: `0548483` (origin/main)

---

## 0. 한 줄 요약

W16 4 day — 마이그레이션 007 추세 RPC + `/stats/trend` API + frontend SVG sparkline + 한계 #40·#84·#90 동시 회수. 단위 테스트 **243 → 254** ran. **한계 회수 누적 30 → 33**. **외부 의존성 0 유지** (recharts/visx 회피, SVG 직접 그리기).

---

## 1. W16 4 Day 종합

| Day | commit | 본질 | 단위 테스트 | 한계 |
|---|---|---|---:|---|
| Day 1 | `4dc2100` | 마이그레이션 007 추세 RPC SQL (search/vision 2개) | 243 | — (SQL only) |
| Day 2 | `361ae5f` | `/stats/trend` API endpoint + 단위 테스트 +4건 | 247 | (graceful 패턴 ship) |
| Day 3 | `acb0a33` | frontend MetricsTrendCard SVG sparkline + getStatsTrend client | 247 | (의존성 0 유지) |
| Day 4 | `0548483` | 한계 #40·#84·#90 회수 + 단위 테스트 +7건 | 254 | #40·#84·#90 |
| **Day 5** | **(본 문서)** | **W16 종합 + W17 진입 핸드오프** | 254 | — |

### 1.1 W16 누적 변경

- **commit**: 5 (4 코드 + 1 핸드오프)
- **단위 테스트**: 243 → **254** (+11: trend 4 + sourceType 3 + errorMsg 4)
- **한계 회수 누적**: 30 → **33** (+ 3건: #40·#84·#90)
- **마이그레이션**: 6 → **7** (+ 007)
- **외부 의존성 추가**: **0** (recharts/visx 회피, SVG 직접 그리기)
- **신규 모듈**:
  - `api/migrations/007_metrics_trend_rpc.sql`
  - `api/tests/test_stats_trend.py`
  - `web/src/components/jet-rag/cards/metrics-trend-card.tsx`

### 1.2 비판적 재검토 (W16)

| Day | 발견 |
|---|---|
| Day 1 | 6h bucket 정렬 시 HOUR 모듈러 산술 (date_trunc + EXTRACT(HOUR) % N) 보다 **epoch floor (to_timestamp(FLOOR(epoch/bucket_secs)*bucket_secs))** 가 단순 + timezone-safe + DST 영향 없음 |
| Day 2 | metric 별 endpoint 분리 vs 통합 — **통합 채택** (frontend client 단순, axios 1회 호출 패턴 일관). TrendBucket schema 통합 (None 허용 부가 필드) |
| Day 3 | recharts (~150KB) vs SVG 직접 그리기 — **SVG 채택** (외부 의존성 0 유지, Server Component + Static SVG 충분). interactivity 는 W17 확장 |
| Day 4 | #85 (debug→info 승격) — 마이그레이션 미적용 시점 매 Vision 호출마다 info 로그 1건 → 로그 노이즈 폭증 우려로 **보류**. 첫 1회만 warn 패턴 W17 follow-up |

---

## 2. 누적 KPI (W16 Day 5 마감)

| KPI | 목표 | 결과 | 충족 |
|---|---|---|---|
| HWPX section_title | ≥30% | **100%** | ✅ |
| PDF section_title | ≥30% | **99.84%** | ✅ |
| 출처 일치율 | ≥0.95 | **1.0** | ✅ |
| P95 검색 응답 | ≤3초 | **219ms** | ✅ |
| 인제스트 SLO 달성률 | ≥90% | `/stats.ingest_slo_aggregate` 노출 | ✅ |
| **추세 시각화** | — | **SVG sparkline + zero-fill 시계열** | 🆕 |
| 단위 테스트 | 0 회귀 | **254/254** | ✅ |
| 유저 스토리 | 8건 | **8/8** | ✅ |
| e2e | — | **9/9 + extract 5/5 + 스캔 PDF cap** | ✅ |
| metrics 영속화 | — | DB write-through + graceful + RPC aggregate | ✅ |

### 2.1 commit 누적 (W3~W16)

W3~W14·86 + W15·4 + **W16·5 = 95**

(이전 합계는 W15 핸드오프 §2.1 의 88. 본 W16 5 day 추가 → 93. + 본 핸드오프 commit (작성 후 push) = 94. 다음 commit 1 추가 후 95 도달 가능. **현재 시점 92 + 본 문서 = 93** — 정확한 카운트는 origin/main `git log --oneline | wc -l` 로 재확인 권장.)

### 2.2 한계 회수 누적: **33건**

| 분류 | 한계 # |
|---|---|
| W7~W14 회수 (24건) | #15·#16·#17·#19·#20·#23·#26·#28·#29·#32·#33·#37·#38 lite·#44·#47·#49·#50·#53·#58·#59·#61·#67·#68·#77·#78·#83 |
| W15 회수 (6건) | #34·#61·#62·#64·#76·#81 |
| **W16 회수 (3건)** | **#40** mobile 가독성 / **#84** error_msg truncate env 변수화 / **#90** vision_usage source_type enum 강제 |

---

## 3. 환경 + 진입 절차

### 3.1 저장소

```bash
git pull origin main  # 0548483
cd api && uv run python -m unittest discover tests  # 254 ran (회귀 0)
cd ../web && pnpm exec tsc --noEmit && pnpm lint     # 0 error
```

### 3.2 마이그레이션 적용 상태

| # | 파일 | 적용 |
|---|---|---|
| 001~004 | (W2~W3) | ✅ |
| 005 | `005_vision_usage_log.sql` | 🟡 **사용자 Studio 적용 대기** |
| 006 | `006_search_metrics_log.sql` | 🟡 **사용자 Studio 적용 대기** |
| **007** | **`007_metrics_trend_rpc.sql`** | 🟡 **사용자 Studio 적용 대기 (005·006 후 순차)** |

### 3.3 사용자 액션 (W16 활성 조건)

1. Supabase Studio → SQL Editor
2. 005 → 006 → 007 순차 paste & Run
3. uvicorn 재시작 (자동 활성)
4. 홈 dashboard 접근 → "검색 응답 추세" 카드 자동 노출 (graceful: 미적용 시 안내 문구)
5. 검증:
   ```sql
   SELECT * FROM get_search_metrics_trend('24h', 'all') LIMIT 5;
   SELECT * FROM get_vision_usage_trend('24h') LIMIT 5;
   ```

### 3.4 검증 SQL (각 마이그레이션 헤더에 명시)

007 헤더 §검증 SQL 참고. 24h range 의 경우 25 row (1h 단위 + 시작 boundary, zero-fill).

---

## 4. W17 우선순위

### 4.1 메인 스레드 가능

| 우선 | 항목 | 비용 |
|---|---|---|
| 1 | **MetricsTrendCard interactivity** (range/mode 토글, client component) | ~2h |
| 2 | **vision metric 시계열 카드** 추가 (현재 search 만 노출) | ~1h |
| 3 | **#85 follow-up** — DB persist 첫 1회만 warn 패턴 (한계 회수 +1) | ~1h |
| 4 | **DoD ④ OpenAI 어댑터 스왑 시연** (사용자 보류 해제 시) | ~3h |
| 5 | **augment 본 검증** | quota 회복 |
| 6 | **트래픽 누적 후 시계열 시연 영상** | 사용자 Studio 적용 + 검색 트래픽 누적 후 |

### 4.2 사용자 협조 필요

| 항목 | 액션 | DoD |
|---|---|---|
| 마이그레이션 005·006·007 적용 | Studio direct apply | W15·W16 영속화·시각화 자동 활성 |
| Ragas 의존성 승인 | `uv add ragas` | KPI ②·③ |
| 평가 데이터셋 45건 | HWP/PDF/스샷/DOCX | KPI ② |
| Golden Path 영상 | 시나리오 녹화 | KPI ⑤ |
| 4 플랫폼 QA | Win/Mac/Android/iOS | KPI ⑥ |
| GitHub Actions secrets | `JET_RAG_API_BASE` | monitor cron |

### 4.3 추천 sprint — W17 진입

**Option A (interactivity, ~2h)**: MetricsTrendCard 의 range/mode 토글 + 'use client' 전환 + Server Component 분리 (RSC + Client Component composition 패턴). useSWR/useState 도입 검토.

**Option B (vision 카드, ~1h)**: vision metric 시계열 카드 추가 — 현재 search 만 노출. RPD 20 cap 모니터링 가시성 강화.

**Option C (한계 회수, ~3h)**: #85 follow-up + #79·#80 등 작은 trade-off 묶음 회수.

**메인 추천: Option A → Option B 묶음 (~3h)** — W16 sparkline 자산 위에 사용자 가치 (range 비교 / vision 가시성) 직접 회수. 단, Option A 는 'use client' 도입 시 Next.js 16 App Router 의 Server/Client Component composition 패턴 재검토 필요 (`web/AGENTS.md`).

비판적 재검토 — Option A 의 useSWR (~30KB 의존성) vs 기본 fetch + useEffect — **stdlib React 만 사용** (의존성 0 정책 일관). 폴링 미필요, 단순 mount-once fetch 면 충분.

---

## 5. 활성 한계 (W16 마감, ~22건)

회수 완료 (33건) — §2.2 참조.

활성 주요 (~22건):
- #38 본격 (Gemini SDK quota header)
- #48 augment 본 검증 (quota 회복)
- #56·#57 SDK 변경 가정
- #65·#70~#75·#79·#80·#82·#85·#86~#89 작은 trade-off

---

## 6. 운영 정책 (W3~W16 누적)

W15 §6 의 30개 + W16 신규:

31. **시간 버킷 RPC epoch floor 패턴** — `to_timestamp(FLOOR(EXTRACT(EPOCH FROM ts) / bucket_secs) * bucket_secs)`. timezone-safe + DST 영향 없음. HOUR 모듈러 산술보다 단순 (W16 Day 1)
32. **frontend 시각화 의존성 0** — recharts/visx 회피. SVG `<polyline>` + `viewBox` + `preserveAspectRatio="none"` 으로 카드 폭 자동 fit. 패턴 재사용 가능 (W16 Day 3)
33. **graceful endpoint 패턴** — 마이그레이션 미적용 환경 RPC raise 시 빈 배열 + `error_code='migrations_pending'` 반환. frontend 가 안내 카드로 분기 (W16 Day 2)
34. **mobile-first grid** — `grid-cols-1 sm:grid-cols-2` 패턴. 작은 폰 (≤320px) 텍스트 잘림 방지 (W16 Day 4 #40)
35. **enum 검증 normalize 패턴** — `_normalize_source_type` 같은 헬퍼로 잘못된 값 → None fallback + warn log. DB row 의미 일관성 유지 (W16 Day 4 #90)
36. **임계값 env override + fallback** — `_error_msg_max_len()` 패턴. 잘못된 env (음수/비숫자) 자동 default 복구 (W16 Day 4 #84)

자율 진행 v2 정착 회고: W11 5d + W12 3d + W13 4d + W14 5d + W15 4d + **W16 4d = 25 day** 모두 사용자 확인 0 패턴 정상 작동.

---

## 7. 다음 작업자 권장 읽기 순서

| # | 문서 | 목적 |
|---|---|---|
| 1 | `README.md` | 프로젝트 진입 |
| 2 | `2026-04-22 개인 지식 에이전트 기획서 v0.1.md` | 마스터 |
| 3 | **`2026-05-03 W16 종합 + W17 진입 핸드오프.md`** (본 문서) | 즉시 진입 |
| 4 | `2026-05-03 W15 종합 + W16 진입 핸드오프.md` | 직전 W |
| 5 | `api/migrations/README.md` | 005·006·007 적용 가이드 |
| 6 | `api/migrations/007_metrics_trend_rpc.sql` | epoch floor RPC 패턴 |
| 7 | `web/src/components/jet-rag/cards/metrics-trend-card.tsx` | SVG sparkline 패턴 |
| 8 | `/Users/kiwoongmin/Desktop/piLab/CLAUDE.md` (file system) | 자율 진행 v2 |
| 9 | `web/AGENTS.md` | Next.js 16 |

---

## 8. 한 문장 요약

W16 4 day — 마이그레이션 007 추세 RPC + `/stats/trend` endpoint + frontend SVG sparkline + 한계 #40·#84·#90 동시 회수. 단위 테스트 243 → **254**, **외부 의존성 0 유지**. 마이그레이션 005·006·007 사용자 Studio 적용 후 시계열 자동 활성. 자율 진행 v2 25 day 정착. W17 진입 추천: **Option A+B 묶음 (MetricsTrendCard interactivity + vision 시계열 카드)**.
