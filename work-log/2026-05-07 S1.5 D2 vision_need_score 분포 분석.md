# 2026-05-07 S1.5 D2 vision_need_score 분포 분석

> master plan §6 S1.5 D2 — D1 PoC 의 페이지별 점수 CSV 를 stdlib 만으로 분석. per-doc / aggregate 분포, 신호 간 Pearson 상관, 임계 후보 시뮬레이션, 골든셋 v1 의 vision_diagram + table_lookup 정확도 측정. 운영 코드 변경 0, 의존성 추가 0, 단위 테스트 회귀 0.

---

## 0. 한 줄 요약

> 2026-05-07 S1.5 D2 ship — `scripts/analyze_vision_need_score.py` 신규 (stdlib 만, ~310 LOC) + `evals/results/vision_need_score_d2_analysis.csv` 신규 (per-page detail + composite score + 임계별 boolean). 5 PDF / 115 페이지 분포: **composite score p50=0.20, p90=0.39, max=0.46** (max 0.5 미만 → D1 default 0.5 임계 무효). **신호 간 Pearson 모두 |r|<0.11 → 신호 독립**. **D1 OR rule recall = 5/6 (83.3%)** (golden v1 vision_diagram + table_lookup 매칭 6건). 데이터센터 PDF p.40 표 1건 miss (모든 신호 0). **entity regex 8 후보 패턴 시뮬레이션 결과 5 PDF 본문에서 총 2 hit** — entity 신호 deprecated 권고. 단위 테스트 588 통과 / skipped 1 / 회귀 0.

---

## 1. 분포 분석 (D1 CSV 기반)

### 1.1 입력

- `evals/results/vision_need_score_poc.csv` (D1 ship 산출, 116 line / header 포함)
- 5 PDF · 115 페이지 (master plan §6 PDF 전수)
- D1 측정 신호: 3종 — `text_density` / `entity_hits` / `table_like_score` (master plan §6 의 6 신호 중 image_area_ratio / text_quality / caption_existence 는 D1 미측정 — D3 본 ship 시 계측 권고)

### 1.2 per-doc 분포

| doc | pages | needs% | sc_p50 | sc_p90 | sc_max | dens_p50 | tbl_p90 | ent_sum |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| (붙임2) 데이터센터 통합_안내서 | 41 | 12.2% | 0.07 | 0.18 | 0.32 | 1.56e-03 | 0.03 | 0 |
| law sample3 | 4 | 0.0% | 0.01 | 0.12 | 0.16 | 2.45e-03 | 0.00 | 0 |
| sonata-the-edge_catalog | 29 | 69.0% | 0.19 | 0.27 | 0.33 | 8.48e-04 | 0.00 | 0 |
| 포트폴리오_이한주 | 15 | 100.0% | 0.33 | 0.41 | 0.44 | 1.76e-04 | 0.42 | 0 |
| 보건의료_빅데이터_플랫폼 | 26 | 88.5% | 0.36 | 0.43 | 0.46 | 1.11e-03 | 0.82 | 0 |

(`composite_score` = 3 신호 등가중 평균. D2 분석 전용 합산식 — D1 PoC 는 boolean 만 산출했음.)

### 1.3 전체 페이지 분포 (115 pages)

```
composite score: p10=0.004 p25=0.075 p50=0.199 p75=0.317 p90=0.387 max=0.457
  histogram (10 bins, [0.0~1.0]):
    [0.00-0.10]=34 | [0.10-0.20]=24 | [0.20-0.30]=19
    [0.30-0.40]=29 | [0.40-0.50]=9  | [0.50-1.00]=0
text_density:    p10=1.95e-04 p25=6.32e-04 p50=1.12e-03 p75=1.56e-03 p90=2.13e-03 max=5.35e-03
table_like:      p10=0.000 p25=0.000 p50=0.007 p75=0.193 p90=0.640 max=0.893
entity_hits:     p10=0.000 p25=0.000 p50=0.000 p75=0.000 p90=0.000 max=0.000
```

**핵심 관찰**:
- **composite score 가 모두 0.5 미만** — D1 hand-tuned default 0.5 임계는 의미 없음 (115/115 페이지 needs=False 처리). D1 결과의 needs_vision 54.8% 는 OR 규칙 산출이지, composite score 임계 산출이 아님.
- **score 분포 좌-스큐** — p50 0.20 / p90 0.39 / max 0.46. 등가중 평균 시 표 한 개 신호로 ≤0.33 지배 (3 신호 중 2개 zero 면 max ⅓).
- **entity_hits 전 페이지 0** — D1 senior-developer 관찰 (a) 정합. 신호 무효.

### 1.4 신호 간 Pearson 상관 (3×3)

```
                density(neg)    table_like        entity
density(neg)           1.000         0.109         0.000
table_like             0.109         1.000         0.000
entity                 0.000         0.000         0.000
```

(density 는 'lower → vision' 이라 부호 반전.)

- 절대값 모두 < 0.11 → **3 신호 거의 독립**. 신호 중복 0.
- entity 는 raw 0 분산이라 상관 정의 불가 (Pearson denom=0 → 0 처리).
- density·table 약상관 0.109 — 정합. 표 페이지가 본문 대비 sparse (낮은 density) 경향이지만 약함.

---

## 2. 임계값 1차 조정안

### 2.1 composite score 임계 시뮬레이션

| threshold | needs_n | needs% |
|---:|---:|---:|
| 0.30 | 38 | 33.0% |
| 0.50 |  0 |  0.0% |
| 0.70 |  0 |  0.0% |

**D1 default 0.5 는 0% needs 산출 — 분포 base 로 invalid**.

### 2.2 target 비율 역산

| 목표 needs_vision | composite score 임계 |
|---:|---:|
| 30% | ≈ 0.312 |
| 50% | ≈ 0.199 |
| 70% | ≈ 0.107 |

### 2.3 신호별 임계 시뮬레이션

**density**:
| density< | pages | % |
|---:|---:|---:|
| 5e-04 | 24 | 20.9% |
| 1e-03 | 51 | 44.3% |
| 2e-03 | 99 | 86.1% |

**table_like**:
| table≥ | pages | % |
|---:|---:|---:|
| 0.30 | 26 | 22.6% |
| 0.50 | 19 | 16.5% |
| 0.70 | 10 |  8.7% |

### 2.4 권고 (D3 결정 prerequisite)

| 항목 | analytical (분포 base) | measured (golden recall base) | 권고 default |
|---|---|---|---|
| **score 임계** | 0.20 (≈ p50 ≈ needs 50%) | 0.30 (golden 매칭 6건 중 0.30↑ = 2건 = 33% recall) | **score 사용 보류, OR rule 유지** (composite score 자체가 max 0.46 으로 신호 부족) |
| **density 임계** | 1e-3 (현재 default) → needs 44.3% | sonata + 포트폴리오 카탈로그 의도와 정합 | **1e-3 유지** — 임계 변경 시 sonata catalog 신호 손실 |
| **table 임계** | 0.5 (현재) → 16.5% needs | 데이터센터 p.40 표 miss (table_score 0.00) — table 휴리스틱 자체가 보수적 false negative | **0.3 권고 + 보강 필요** — 0.3 시 needs 22.6% (현재 16.5% 대비 +6%, 데이터센터 p.40 은 여전히 잡히지 않음 → table 휴리스틱 자체 개선 D3 신호) |

**핵심**: composite score 가 D1 OR rule 보다 measured recall 이 낮음 (composite 33% vs OR 83%). D3 에선 **OR rule 유지** + **신호별 임계 보정 (table 0.3)** + **table 휴리스틱 보강** 권고. 임계 통합 score 도입은 6 신호 모두 측정된 후 (D3 본 ship) 재검토.

---

## 3. vision_diagram 카테고리 정확도

### 3.1 매칭 정책

- 골든셋 v1 의 `query_type ∈ {vision_diagram, table_lookup}` row 만 추출 (총 9 row — vision_diagram 3건 + table_lookup 6건)
- `expected_doc_title` 의 NFC substring 으로 D1 CSV doc 매칭 (macOS NFD 파일명 vs 골든셋 NFC 정규화 fix 포함)
- `source_hint` 의 `p.{N}` 으로 페이지 번호 추출 — 없으면 doc 평균
- 매칭된 페이지의 D1 needs_vision (OR rule) + composite score 측정

### 3.2 결과

| qid | type | doc | page | in_PoC | needs | score |
|---|---|---|---:|---|:---:|---:|
| G-U-003 | table_lookup | sonata | AVG | IN | 69.0% | 0.18 |
| G-U-005 | vision_diagram | sonata | AVG | IN | 69.0% | 0.18 |
| G-U-006 | vision_diagram | 데이터센터 | 6 | IN | **Y** | 0.24 |
| G-U-016 | vision_diagram | 직제_규정 | - | OUT | - | - |
| G-A-008 | table_lookup | 데이터센터 | 40 | IN | **N** | 0.06 |
| G-A-011 | table_lookup | 브랜딩 | 2 | OUT | - | - |
| G-A-021 | table_lookup | sample-report | 91 | OUT | - | - |
| G-A-107 | table_lookup | 포트폴리오 | 7 | IN | **Y** | 0.30 |
| G-A-111 | table_lookup | 포트폴리오 | 7 | IN | **Y** | 0.30 |

(IN = D1 측정 5 PDF 안. OUT = 다른 PDF/포맷. AVG = page hint 없이 doc 평균.)

### 3.3 정확도 합산 (매칭 N=6)

- **D1 OR rule recall = 5/6 = 83.3%** (G-A-008 1건 miss)
- **composite score 임계별 recall**:
  - score ≥ 0.3 → 2/6 = 33.3% (G-A-107, G-A-111 만)
  - score ≥ 0.5 → 0/6 = 0%
  - score ≥ 0.7 → 0/6 = 0%

### 3.4 G-A-008 (데이터센터 p.40) miss 분석

```
p.40: chars=813 dens=1.62e-03 ent=0 tbl=0.00 need=False sig=
```

- table_lookup 인데 table_like_score = 0.00 — **표 휴리스틱 자체가 한국어 표를 못 잡음**. PyMuPDF span 분리 미스 가능성 (한 line 안 한국어 cell 이 별도 span 으로 분리되지 않음).
- density 1.62e-3 > 1e-3 → 본문 텍스트 다수 (표 + 캡션 + 본문 혼재 페이지로 추정)
- entity 0 — caption 미사용
- **D3 actionable**: table 휴리스틱 v2 — span 분리 fallback 으로 (1) `chars/line` 길이 분포 (cell 짧음) (2) 같은 y 좌표에 ≥3 단어 (word 기반) 측정. PoC 단계라 별도 sprint.

### 3.5 신뢰도 한계

- **N=6** — 매우 작음. 95% CI 폭 ±20pp+ 추정. 정성 분석 우선.
- **OUT 3건** (G-U-016 직제규정 HWPX, G-A-011 브랜딩 PPTX 추정, G-A-021 sample-report 본 PC 부재) — 다른 컴퓨터 11 docs 진입 시 본격 측정 가능.
- **vision_diagram 3건 중 PDF 측정 가능 = 2건** (sonata G-U-005, 데이터센터 G-U-006) — sonata 는 page hint 없어 doc 평균 매칭 (정확도 약함).

---

## 4. D1 senior-developer 관찰 3건 정정안

### 4.1 (a) entity regex 0 hit — 한국어 추가 패턴 시뮬레이션

5 PDF 본문 전수 스캔, 8 후보 패턴 시뮬레이션 결과:

| 후보 패턴 | 5 PDF 총 hit | hit doc 수 |
|---|---:|---:|
| `[표 N]` / `[그림 N]` (D1 default) | 0 | 0 |
| `<표 N>` / `<그림 N>` (D1 default) | 0 | 0 |
| `Figure N` / `Table N` (D1 default) | 0 | 0 |
| `식 (N)` / `Eq.(N)` (D1 default) | 0 | 0 |
| 한국어 `표/그림 N` (괄호 없는, 단어 경계) | 1 | 1 |
| 한국어 `(표 N)` / `(그림 N)` (괄호) | 0 | 0 |
| `Fig. N` | 0 | 0 |
| 한국어 `표|그림 N` (loose) | 2 | 2 |

**총 8 후보 × 5 PDF = 본문 전수 스캔 결과 누적 hit 2** (loose 패턴 기준). 자모 분리 NFD 효과 가능성 — 전체 본문 평문에선 NFC 전제, 매칭 영향 미미.

**권고 (D3)**: entity 신호 가중치 0 또는 deprecated. 5 PDF 가 한국어 정부·기업 자료 mix 인데 hit 사실상 0 — entity reference 자체가 사용자 자료의 본문 패턴이 아님. 가중 회수해 density / table 에 재분배.

### 4.2 (b) low_density 51/63 우세 — sonata catalog 검증

- sonata catalog 29 pages 중 density<1e-3 = **20/29 (69%)**
- D1 needs=True 비율 (sonata) = 20/29 = 69.0%
- → **sonata 의 needs=True 는 모두 low_density 신호로부터 옴** (table 0, entity 0)
- sonata 카탈로그는 차량 사진 + 트림 표 + 짧은 카피 페이지 위주 — **vision 후보 적정성 높음**. 사용자 직관과 정합.

**권고 (D3)**: density 1e-3 유지. 카탈로그성 PDF 의 vision 후보 인식이 본 임계의 핵심 가치. 5e-4 로 낮추면 sonata 의 needs 비율 20.9% (전체 기준) 까지 떨어져 카탈로그 시그널 손실. 2e-3 로 올리면 86.1% 까지 needs 비율 폭증 (본문 PDF 도 다수 끌어당김).

### 4.3 (c) table_like 0.5 임계 — 보수성 검증

- 빅데이터 추진계획: p90 0.82 / max 0.89, needs 88.5%
- 포트폴리오: p90 0.42 → table_score ≥ 0.5 페이지가 거의 없음에도 needs 100% (low_density 가 다 잡음)
- 데이터센터 p.40 (표 페이지): table_score 0.00 — **false negative** (앞서 §3.4 분석)
- 0.3 임계 시 needs 비율: 22.6% (전체) — 0.5 대비 +6%
- 0.7 임계 시: 8.7% — sonata + 포트폴리오 의 table 신호 거의 사라짐

**권고 (D3)**:
1. 0.5 → 0.3 임계 완화 — 빅데이터 추진계획 / 포트폴리오 의 multi-column 페이지 추가 cover
2. table 휴리스틱 v2 — span 분리 fallback (한국어 cell 이 단일 span 으로 합쳐지는 PDF 대응)
3. 임계 변경 + 휴리스틱 보강 둘 다 D3 본 ship 으로 진입

---

## 5. D3 진입 결정 사항 (사용자 결정 필요)

| # | 항목 | 권고 default | 대안 |
|---|---|---|---|
| 1 | needs_vision 결정 규칙 | **OR rule 유지** (D1 default — recall 83.3%) | composite score 임계 (p50 ≈ 0.20) — 단 max 0.46 / OR 대비 recall 33% |
| 2 | density 임계 | **1e-3 유지** (sonata 카탈로그 신호 보호) | 5e-4 (보수적) / 2e-3 (관대 — 본문 끌어당김) |
| 3 | table 임계 | **0.3 권고** (현재 0.5 → 보강) | 0.5 유지 (현 default) / 0.7 (보수) |
| 4 | table 휴리스틱 v2 (span 분리 fallback) | **D3 본 ship 진입** — 데이터센터 p.40 false negative 회복 | 별도 sprint 분리 (vision_score v2) |
| 5 | entity 신호 처리 | **deprecated** (가중 0 또는 OR 항목 제거) — 5 PDF hit 사실상 0 | 한국어 추가 패턴 도입 (시뮬레이션 hit 2/115 — ROI 낮음) |
| 6 | 6 신호 중 미측정 3종 | image_area_ratio / text_quality / caption_existence — **D3 본 ship 시 추가 계측** | D3 단계 분리 (3 신호만 1차, 6 신호는 후속) |
| 7 | 11 docs 본 측정 | 본 PC 5 PDF 한정 → **사용자 PC (다른 컴퓨터) 11 docs 측정 후 D3 본 결정** | 본 PC 5 PDF 분포로 D3 진입 — 통계 신뢰도 낮음 (CI ±20pp) |

---

## 6. 회귀 보호 검증

```
$ cd api && uv run python -m unittest discover tests
Ran 588 tests in 14.873s
OK (skipped=1)
```

- baseline 588 / skipped 1 / **회귀 0**
- 운영 코드 (`vision_score.py` / `extract.py` / `pymupdf_parser.py`) 변경 0
- 외부 API 호출 0
- 의존성 추가 0 (pandas/numpy 회피 — stdlib `csv`/`statistics`/`math` 만)
- DB 마이그 0
- 신규 분석 산출 (CSV 1건) 은 `evals/results/.gitignore` (`*` + `!.gitignore`) 정책으로 자동 제외

---

## 7. 변경 파일

| 경로 | 역할 | LOC | 변경 |
|---|---|---:|---|
| `api/scripts/analyze_vision_need_score.py` | D2 분석 스크립트 (stdlib 만) | ~310 | **신규** |
| `evals/results/vision_need_score_d2_analysis.csv` | per-page detail + composite score + 임계별 boolean | ~117 row | **신규** (gitignore 됨) |
| `work-log/2026-05-07 S1.5 D2 vision_need_score 분포 분석.md` | 본 문서 | ~330 | **신규** |
| `work-log/2026-05-07 D3 진입 - E1 sprint 신설 + plan 작성.md` | master timeline / §11.1 / §11.4 갱신 | - | 갱신 |

---

## 8. commit

```
feat(vision): S1.5 D2 ship — 분포 분석 + 임계 조정안 + vision_diagram 정확도
```

본문에 다음 핵심 5건 명시:
1. 5 PDF / 115 페이지 분포 — composite score p50=0.20 / p90=0.39 / max=0.46
2. 신호 간 Pearson 상관 모두 |r|<0.11 → 신호 독립 (중복 0)
3. D1 OR rule recall 83.3% (5/6) — 데이터센터 p.40 표 1건 false negative
4. entity 8 후보 패턴 시뮬레이션 hit 2 — deprecated 권고
5. 회귀 0 (analysis only, stdlib 만, 의존성 0)

---

## 9. 다음 후보 (D3 진입 전 사용자 결정)

| 후보 | 작업 | 진입 조건 |
|---|---|---|
| **A. S1.5 D3 — 본 ship** | OR rule 유지 + table 임계 0.3 + table v2 휴리스틱 + entity deprecated + 운영 통합 (`extract.py` / `chunk.py`) | D2 결과 + 사용자 PC 11 docs 측정 결과 |
| **B. S1.5 D2 보강** | 다른 컴퓨터에서 11 docs PoC 재실행 → CSV 합산 → D2 분석 재수행 | 다른 컴퓨터 진입 |
| **C. 다른 sprint 우선** (E1 잔여 / S0 D6 / S2 진입) | master plan §6 우선순위 재논의 | 즉시 |

권고: **B → A** — 11 docs 통계 보강 후 D3 본 ship. 본 PC 5 PDF 만으로 D3 결정 시 통계 신뢰도 낮음 (vision_diagram N=6, CI ±20pp).

---

## 10. 한 문장 요약

> 2026-05-07 S1.5 D2 ship — `analyze_vision_need_score.py` (~310 LOC, stdlib 만) + `vision_need_score_d2_analysis.csv` (per-page detail). D1 CSV 의 5 PDF / 115 페이지 분포 분석: **composite score p50=0.20 / p90=0.39 / max=0.46** (D1 default 0.5 임계는 invalid), **신호 간 Pearson 모두 |r|<0.11 (독립)**, **D1 OR rule recall 5/6 = 83.3%** (데이터센터 p.40 표 1건 false negative — table_score 0.00). **entity 8 후보 패턴 본문 hit 2/115 → deprecated 권고**. D3 결정 사항 7건 정리 (OR rule 유지 + density 1e-3 유지 + table 0.3 + table v2 휴리스틱 + entity deprecate + 6 신호 중 3 미측정 추가 + 11 docs 측정). 단위 테스트 588 / skipped 1 / **회귀 0**. **D3 본 ship 진입 전 11 docs 측정 권고** (사용자 결정).
