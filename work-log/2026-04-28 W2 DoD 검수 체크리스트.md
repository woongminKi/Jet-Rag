# 2026-04-28 W2 DoD 검수 체크리스트

> W2 (HWP·스샷·URL 입수) 마감 검수용 브라우저 시나리오 모음. 사용자가 직접 클릭·입력하면서 각 항목 통과 여부를 체크. 코드/단위 smoke 는 Day 2~6 work-log 에 일별로 기록되어 있고, 본 문서는 **사용자 검증 단계**.

---

## 0. 사전 조건

- [ ] **API 서버**: `cd api && uv run uvicorn app.main:app --reload` → http://localhost:8000 응답
- [ ] **웹 서버**: `cd web && pnpm dev` → http://localhost:**3001** 응답
- [ ] **Supabase MCP**: 새 Claude Code 세션이면 `mcp__supabase-jetrag__list_tables` 먼저 ping (4 테이블 응답)
- [ ] **마이그레이션 002 적용 확인** (Day 2): `received_ms` 컬럼 존재 (Day 7 W2 종합 정리 §1 참조)

테스트용 파일 준비 (한 폴더에 모아두기 권장):
- 합성 PDF (PyMuPDF 로 만들거나 작은 1페이지 PDF)
- 합성 HWPX (hwpx.HwpxDocument.new() 활용 또는 공공기관 다운로드 1건)
- PNG 이미지 1장 (스크린샷이라도)
- HEIC 1장 (옵션 — 폰에서 캡처 후 AirDrop)

---

## 1. 시나리오 1 — Primary Golden Path (단일 파일 자동 이동)

기획서 §6.1 — 가장 흔한 케이스: 단일 파일 업로드 → 결과 페이지 자동 이동 → 검색 가능.

| # | 액션 | 기대 결과 | 통과 |
|---|---|---|---|
| 1.1 | 브라우저 → http://localhost:3001/ingest | 업로드 페이지 표시, 한국어 헤딩 "문서 업로드" | ☐ |
| 1.2 | DropZone 에 합성 PDF 1건 drag-and-drop | 즉시 카드 1건 추가, 상태 "대기 중" → "처리 중" 진행 | ☐ |
| 1.3 | StageProgress 가 extract → chunk → content_gate → tag_summarize → load → embed → doc_embed → dedup 순서로 진행 | "안전 검사" 단계가 chunk 와 tag_summarize 사이에 보임 | ☐ |
| 1.4 | 인제스트 완료 (~30s) | 자동으로 `/doc/<id>?uploaded=1` 로 이동 | ☐ |
| 1.5 | `/doc/<id>` 페이지 로드 | 제목 · doc_type 뱃지 · 메타 · 요약 · 태그 모두 표시 | ☐ |
| 1.6 | 상단 "업로드가 완료되어 이 문서로 이동했어요" 배너 노출 | ☐ |
| 1.7 | 페이지 상단 Hero 검색 인풋에 태그 중 하나 입력 → 엔터 | `/search?q=태그` 로 이동, 결과 페이지에 본 문서 노출 | ☐ |

---

## 2. 시나리오 2 — 중복 업로드 (duplicated 즉시 이동)

DE-28 + DE-50 — SHA-256 매칭 즉시 결과 페이지로.

| # | 액션 | 기대 결과 | 통과 |
|---|---|---|---|
| 2.1 | 시나리오 1 에서 업로드한 PDF 와 **동일 파일** 을 다시 업로드 (`/ingest` 로 돌아가기) | 즉시 (≤ 2초) 자동으로 `/doc/<id>?duplicated=1` 로 이동 | ☐ |
| 2.2 | 상단에 "이미 같은 내용이 등록되어 있어요. 기존 문서로 이동했습니다" 배너 노출 | ☐ |
| 2.3 | 처음 업로드와 같은 doc_id 로 라우팅됨 (URL 비교) | ☐ |

---

## 3. 시나리오 3 — 멀티 파일 업로드 (첫 완료만 자동)

DE-28 — 다중 시 첫 completed 만 자동 이동, 나머지는 "상세 보기" 링크.

| # | 액션 | 기대 결과 | 통과 |
|---|---|---|---|
| 3.1 | `/ingest` 에서 서로 다른 PDF 3건 동시 drag-and-drop | 카드 3건 추가, 동시 진행 | ☐ |
| 3.2 | 첫 번째 completed 시점에 자동 이동 | `/doc/<첫 완료 doc>?uploaded=1` | ☐ |
| 3.3 | (다시 /ingest 로 back 또는 새 탭) 두 번째·세 번째 doc 카드에는 **"상세 보기"** 버튼 노출 + completed 뱃지 | ☐ |
| 3.4 | "상세 보기" 클릭 → 해당 `/doc/<id>` 페이지 정상 로드 | ☐ |

---

## 4. 시나리오 4 — HWPX 업로드

W2 §3.C 항목 C — python-hwpx 본 처리.

| # | 액션 | 기대 결과 | 통과 |
|---|---|---|---|
| 4.1 | 합성 또는 다운로드한 HWPX 1건 업로드 | 인제스트 8-stage 통과 (~15s) | ☐ |
| 4.2 | `/doc/<id>` 페이지 | doc_type 뱃지 = "HWPX", 한국어 요약 자연스러움 | ☐ |
| 4.3 | tags 한국어 5건 이상 | ☐ |
| 4.4 | 청크 1건 이상 (작은 doc 도) | ☐ |

---

## 5. 시나리오 5 — 이미지 업로드 (Vision OCR)

W2 §3.D 항목 D — Pillow 정규화 + Gemini Vision.

| # | 액션 | 기대 결과 | 통과 |
|---|---|---|---|
| 5.1 | PNG 이미지 (텍스트 포함) 1장 업로드 | 인제스트 ~20s 완료 | ☐ |
| 5.2 | `/doc/<id>` 페이지 | doc_type 뱃지 = "이미지" | ☐ |
| 5.3 | 요약 또는 태그에 이미지 내 텍스트의 핵심 키워드 포함 | ☐ |
| 5.4 | (옵션) HEIC 업로드 → 정상 인제스트 (DE-17 검증) | ☐ |
| 5.5 | (메신저 캡처 시) `flags.third_party=true` 가 주의사항 카드에 노출 | ☐ |

---

## 6. 시나리오 6 — 스캔 PDF 재라우팅

W2 §3.A′ — PyMuPDF 텍스트 빈약 → ImageParser fallback + flags.scan=true.

| # | 액션 | 기대 결과 | 통과 |
|---|---|---|---|
| 6.1 | 텍스트 레이어 없는 스캔 PDF 1건 업로드 (또는 합성) | 인제스트 진행 | ☐ |
| 6.2 | `/doc/<id>` 헤더에 "스캔본" 뱃지 노출 | ☐ |
| 6.3 | 주의사항 섹션에 "스캔본 (Vision OCR 사용)" 항목 | ☐ |
| 6.4 | OCR 결과가 요약/청크에 반영 | ☐ |

---

## 7. 시나리오 7 — URL 인제스트

W2 §3.E 항목 E — POST /documents/url + SSRF 게이트.

| # | 액션 | 기대 결과 | 통과 |
|---|---|---|---|
| 7.1 | curl 또는 Swagger UI (http://localhost:8000/docs) 에서 `POST /documents/url` 호출, body `{"url": "https://en.wikipedia.org/wiki/Retrieval-augmented_generation"}` | 202 응답 + doc_id | ☐ |
| 7.2 | 인제스트 완료 (~20s) 후 `/doc/<id>` 접근 | doc_type "URL", title = 페이지 제목 | ☐ |
| 7.3 | 헤더에 "원본 페이지 열기" 외부 링크 (source_url) | ☐ |
| 7.4 | SSRF 차단 확인: `{"url": "http://localhost:3000"}` POST → 400 + "내부 호스트 차단" detail | ☐ |
| 7.5 | `{"url": "http://192.168.1.1"}` → 400 + "private IP 차단" | ☐ |

---

## 8. 시나리오 8 — PII / 워터마크 게이트

W2 §3.G 항목 G — content_gate.

| # | 액션 | 기대 결과 | 통과 |
|---|---|---|---|
| 8.1 | 주민번호 (예: 850123-1234567) 가 본문에 포함된 PDF/HWPX 1건 업로드 | 인제스트 정상, `/doc/<id>` 의 주의사항에 "개인정보 포함" 노출 | ☐ |
| 8.2 | "대외비" 텍스트 포함 doc | "워터마크: 대외비" 노출 | ☐ |
| 8.3 | 정상 doc (PII/워터마크 없음) | 주의사항 섹션 미노출 | ☐ |
| 8.4 | (전화번호 010-1234-5678 만 있는 doc) | has_pii=false 유지 (오탐 회피, DE-21 b 검증) | ☐ |

---

## 9. 시나리오 9 — SLO 측정

W2 §3.A — `/stats.slo_buckets`.

| # | 액션 | 기대 결과 | 통과 |
|---|---|---|---|
| 9.1 | 시나리오 1~8 종료 후 `GET /stats` (Swagger UI) | 응답에 `slo_buckets: {pdf_50p, image, pdf_scan, hwp, url}` 5종 키 모두 존재 | ☐ |
| 9.2 | 각 버킷의 sample_count 가 시나리오 결과와 일치 | ☐ |
| 9.3 | image 버킷의 p95_ms 가 < 2000 | ☐ |
| 9.4 | url 버킷의 p95_ms 가 < 2000 | ☐ |

---

## 10. 시나리오 10 — 폴러 batch 통합 (DE-51)

W2 Day 7 polish — useDocsBatchPolling.

| # | 액션 | 기대 결과 | 통과 |
|---|---|---|---|
| 10.1 | 브라우저 DevTools Network 탭 열기 | 준비됨 | ☐ |
| 10.2 | `/ingest` 에서 PDF 3건 동시 업로드 | 진행 중 | ☐ |
| 10.3 | Network 탭의 `/documents/...` 요청 관찰 | `batch-status?ids=A,B,C` 1회/1.5s, `documents/{id}/status` 호출은 0건 (또는 `/doc/<id>` 페이지 진입 시에만) | ☐ |
| 10.4 | 업로드 완료 후 호출 정지 | ☐ |
| 10.5 | retry 발생 시 (failed 상태에서 "재시도" 버튼 클릭) → batch-status 호출 재개 | ☐ |

---

## 11. (옵션) 모바일 viewport 검수

| # | 액션 | 기대 | 통과 |
|---|---|---|---|
| 11.1 | DevTools 디바이스 모드 (iPhone 13 Pro) | 헤더 hamburger 메뉴 노출 | ☐ |
| 11.2 | `/doc/<id>` 페이지 | 카드 적층 + Hero 검색 인풋 가독성 OK | ☐ |
| 11.3 | StageProgress 가 작은 화면에서 잘림 없이 표시 | ☐ |

---

## 12. KPI 정식 측정 (선행 액션 후)

본 체크리스트 후 별도 진행:
- [ ] HWPX 평가셋 10건 → KPI ≥ 0.9 측정 (S1 chunks≥1, S2 평균 길이 ≥200자, S3 section_title ≥30%)
- [ ] Vision 평가셋 20건 (메신저 10 + 문서 10) → 분류 정확도·OCR 정확도 측정
- [ ] URL 1~2건 추가 smoke
- [ ] 실제 HWP 5.x 1건 정식 smoke

---

## 13. 결과 보고 형식 (사용자 → 문서)

체크리스트 마감 후 W2 종합 정리 (`work-log/2026-04-28 W2 종합 정리.md`) §2.3 KPI 표에 실측값 기록:

```
| 인제스트 SLO 달성률 | 위반 (3.7s) | P95 < 2초 | ✅ 실측 P95 = 1.2s (45 sample) |
| HWP/HWPX 성공률 | 측정 불가 | ≥ 0.9 | ✅ 9/10 = 0.9 |
| Vision 캡셔닝 | 0% | ≥ 0.85 | ✅ 0.87 |
```

미통과 항목은 §3 "발견된 이슈" 또는 §4 "잔여 이슈" 에 추가 후 W3 진입점에 영향 평가.
