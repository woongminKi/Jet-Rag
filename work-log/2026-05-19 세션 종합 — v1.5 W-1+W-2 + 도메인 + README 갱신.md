# 2026-05-19 세션 종합 — v1.5 W-1+W-2 + 도메인 + README + 다이어그램

> 본 세션 누적 commit 6건 (`e57c3ec` 이전 push + `4396913` + `155653b` + `c2c4e26` + `c3815c1` + 본 commit)
> 시작 HEAD `6361894` → 마감 HEAD (본 commit)
> 단위 테스트 1206 → **1229 OK** (failures=3 기존 flaky 동일)
> Q1 의사결정 해소 / Q8 베타 후 진행 결정 / Q9·Q10·Q11 미응답

---

## 0. 핵심 요약 (~300 word)

본 세션은 배포 D 안 production 도달 직후 (2026-05-18 `6361894`) 의 후속 sprint 3건을 묶어서 ship 했다.

1. **v1.5 W-1 DeepInfra 어댑터 swap** — HF Inference Providers cold-start 위험 제거. ENV `JETRAG_EMBED_PROVIDER` 1줄 토글 (default `hf` / `deepinfra`). 호출 사이트 8건 무수정. 신규 어댑터 326 lines + 단위 테스트 13건 (W-1) + P2 보강 10건 (W-2) → 합계 23건 100% PASS. senior-developer 구현 + senior-qa 검증 PASS (P0/P1 결함 0).

2. **v1.5 W-2 production 측정** — production endpoint 12 query × 5 라운드 = N=60 warm 호출 latency 실측. P95 **1.705s** (KPI #10 운영 게이트 2.5s 의 32% 여유). golden_v2 in-process R@10 회귀 **0.0000** (115/115 row top-5 ordering 100% 일치 — W-0 cosine 0.999984 보증의 실증). **DECISION-14 등록** — HF Dedicated CPU $24/월 fallback 불필요 확정.

3. **도메인 부착 woong-s.com** — Q1 의사결정 해소. Cloudflare Registrar 에서 `woong-s.com` 구매 ($10/년). `jetrag.woong-s.com` (Vercel) + `jetrag-api.woong-s.com` (Railway) custom domain 부착. CORS 가 W-1 commit `e57c3ec` 시 이미 env 화 (`JETRAG_CORS_ORIGINS`) 덕에 **코드 변경 0** — dashboard + ENV 만으로 ship. smoke 전부 PASS (health 200, search 2.29s, CORS allow-origin 정확).

4. **README + CHANGELOG 갱신** — W21 (2026-05-03 MVP 마감) 이후 16일 진전 + 본 세션 4 sprint 누적 반영. README 에 Production (live) 섹션 + 포트폴리오 어필 포인트 5개 신설. CHANGELOG 는 W23~W30 + v1.5 sprint + Deploy 도메인 sprint 그룹 추가, DECISION-1~14 표 신설. senior-developer 자체평가 **8/10** (개선 여지: 데모 GIF + 아키텍처 다이어그램 + RLS ship).

**미응답 의사결정**: Q9 (공개 범위 + Auth provider, 멀티유저 D1~D3 블로커) / Q10 (공개 시점, Q9 답변 후) / Q11 (Vercel 라이선스, Q8 의존). **Q8 (결제 모델) = 베타 테스트 후 진행 (2026-05-19 사용자 명시 deferred)**.

총 작업 시간 ~3.5h (3 sprint + 종합 + 문서화 + 12 task), 총 5 commit, 코드 변경 791 insertions / 12 deletions + work-log 3건 신규 + README/CHANGELOG 갱신.

---

## 1. commit 별 진전

### 1.1 `4396913` — v1.5 W-1 DeepInfra 어댑터 swap

```
5 files changed, 941 insertions(+), 4 deletions(-)
```

- 신규: `api/app/adapters/impl/bgem3_deepinfra_embedding.py` (326 lines)
- 신규: `api/tests/test_bgem3_deepinfra_embedding.py` (305 lines, 13건)
- 수정: `api/app/adapters/impl/bgem3_hf_embedding.py` (+43/-8, factory 분기)
- 수정: `.env.example` (+12)
- 신규: `work-log/2026-05-19 v1.5 W-1 DeepInfra 어댑터 swap.md`

검증: 1206 → **1219 OK**, failures=3 기존 flaky 동일. senior-qa PASS, P0/P1 결함 0.

### 1.2 `155653b` — v1.5 W-2 production 측정 + DECISION-14 + P2 테스트

```
2 files changed, 335 insertions(+), 8 deletions(-)
```

- 수정: `api/tests/test_bgem3_deepinfra_embedding.py` (+223, P2 보강 10건)
- 수정: `work-log/2026-05-19 v1.5 W-1 DeepInfra 어댑터 swap.md` (+121, §10/§11/§12 신설)

senior-qa 측정 결과:
- P95 **1.705s** (운영 게이트 2.5s · 32% 여유 / production 게이트 3s · 43% 여유)
- R@10 회귀 **0.0000** (golden_v2 115/115 row top-5 ordering 100% 일치)
- 5xx/timeout 0/60 호출
- 단위 테스트 1219 → **1229 OK**

DECISION-14: HF Dedicated CPU $24/월 fallback 불필요, 32% 마진 안정적.

### 1.3 `c2c4e26` — 도메인 부착 woong-s.com

```
1 file changed, 185 insertions(+)
```

- 신규: `work-log/2026-05-19 도메인 부착 — woong-s.com.md`

코드 변경 0. dashboard + ENV 만 (Vercel domain 등록, Railway Custom Domain, Cloudflare DNS, CORS `JETRAG_CORS_ORIGINS` 갱신, Vercel `NEXT_PUBLIC_API_BASE_URL` 갱신).

production smoke PASS:
- `https://jetrag.woong-s.com` 200 OK
- `https://jetrag-api.woong-s.com/health` 200 OK
- search via custom domain 200 OK / 2.29s

### 1.4 `c3815c1` — README + CHANGELOG 갱신

```
2 files changed, 387 insertions(+), 138 deletions(-)
```

- 수정: `README.md` 264 → 334 lines (+70)
- 수정: `CHANGELOG.md` 192 → 369 lines (+177)

신규 섹션:
- README: Production (live) + 포트폴리오 어필 포인트 5개
- CHANGELOG: W23~W30 + v1.5 + Deploy sprint 그룹 + DECISION-1~14 표

발견된 불일치 (모두 README 갱신 시 정정):
- commit 누적 252 → **실측 548**
- 단위 테스트 275 → **1229**
- 마이그레이션 8 → **16** (013 결번 — 별도 트래킹 후보)

### 1.5 본 commit — README mermaid 아키텍처 다이어그램 + 종합 work-log

```
2 files changed (README.md + 본 work-log)
```

- 수정: `README.md` 335 → 379 lines (+44, 신규 `## 아키텍처` 섹션)
- 신규: `work-log/2026-05-19 세션 종합 — ...md` (본 문서)

mermaid `flowchart TB` 4-tier 다이어그램:
- 프론트엔드 (Vercel) / 백엔드 (Railway) / 데이터 (Supabase) / AI Provider 4 subgraph
- 어댑터 layer (`{{ }}` 육각형, classDef boundary) — HF↔DeepInfra 점선 swap path
- production (DeepInfra) classDef prod (진한 배경 + 파란 굵은 테두리)
- fallback (HF) classDef fallback (dashed border)
- subgraph 라벨에 layer · region · 비용 압축 ($5/월·$0/월·<$1/월)
- 화살표 라벨에 ENV/RPC 명 명시 (`NEXT_PUBLIC_API_BASE_URL`, `search_hybrid_rrf`)
- 캡션 1단락 — mermaid 미렌더 환경에서도 텍스트만으로 핵심 메시지 (4-tier + R@10 회귀 0 + 비용) 전달

senior-developer 자체평가: README 8/10 → **9/10** 도달 (남은 1점 = 데모 GIF + 멀티유저 RLS ship).

---

## 2. metric / KPI 변화

| 항목 | 본 세션 시작 | 본 세션 마감 | 변화 |
|---|---:|---:|---|
| HEAD | `6361894` | `c3815c1` | +5 commit |
| 단위 테스트 | 1206 OK | **1229 OK** | +23 (W-1 신규 13 + W-2 P2 10) |
| failures | 3 (기존 flaky) | 3 (동일) | 0 |
| 임베딩 provider | HF Inference Providers only | HF default + DeepInfra ENV swap | swap 가능 |
| production embed (Railway ENV) | HF | **DeepInfra** | swap 완료 |
| custom domain | 0 | **2 (frontend + backend)** | 부착 완료 |
| Cloudflare 도메인 | 0 | 1 (`woong-s.com` ~$10/년) | 신규 |
| KPI #10 P95 (production) | 미측정 | **1.705s** (게이트 2.5s 32% 여유) | PASS |
| R@10 회귀 (golden_v2 115 row) | baseline | **0.0000** (top-5 100% 일치) | 회귀 0 |
| DECISION 등록 | 1~13 | **1~14** | +1 (DeepInfra production) |
| 의사결정 해소 | Q1·Q8~Q11 미응답 | **Q1 해소 + Q8 deferred** | -2 미응답 |

---

## 3. 의사결정 진전

### 3.1 해소된 결정

#### Q1 도메인 보유 → **유료 도메인 `woong-s.com` 1개 구매**
- Cloudflare Registrar, ~$10/년 at-cost
- `woongs.com` 은 Sedo Parking 매물이라 회피 (대안 TLD 7개 가용성 확인 후 `.com` 변형 선택)
- 추후 다른 프로젝트 추가 시 서브도메인 무료 확장 (`proj2.woong-s.com` 등)

#### Q8 결제 모델 → **베타 테스트 후 진행** (deferred)
- 2026-05-19 사용자 명시 deferred
- 트리거: 멀티유저 D1~D3 ship + 베타 5~10명 1주 안정화 후 (Q10 답변 후)
- 그 시점에 무료 / cap / 구독 중 결정

### 3.2 신규 등록

#### DECISION-14 v1.5 W-2 DeepInfra production swap PASS (2026-05-19)
- provider: DeepInfra BGE-M3 (`api.deepinfra.com/v1/openai/embeddings`)
- 활성화: Railway `JETRAG_EMBED_PROVIDER=deepinfra`
- 비용: <$1/월 (페르소나 트래픽)
- 근거: P95 1.705s (32% 여유) + R@10 회귀 0 + 단위 테스트 회귀 0
- 사이드 이펙트: HF default 경로 보존 (회귀 안전판), 영구 캐시 entry 공유 안전

### 3.3 미응답

| Q | 항목 | 권장 | 블로커 |
|---|---|---|---|
| Q9 | 공개 범위 + Auth provider | 초대 코드 + Email/Google OAuth | 멀티유저 D1~D3 |
| Q10 | 공개 시점 | 1차 베타 5~10명 → 1주 안정화 → 공개 | Q8 결제 모델 결정 시점 |
| Q11 | Vercel 라이선스 | Hobby 유지 (수익 0 시) | Q8 의존 |

---

## 4. 다음 sprint 후보 (우선순위)

| 우선순위 | 항목 | 작업량 | 트리거 | 비고 |
|---|---|---|---|---|
| ~~1~~ | ~~README 아키텍처 다이어그램 (mermaid)~~ | ~~1h~~ | ✅ **본 세션 완료** (commit `c3815c1+1`) | 8→9/10 도달 |
| 1 | **woong-s.com root 포트폴리오 landing 페이지** | 4~8h | 즉시 | 이직 포트폴리오 가치 ★★★★ |
| 2 | **데모 GIF / 검색 결과 스크린샷** | 1~2h | 즉시 | 시각 자산 보강, 10/10 도달 |
| 4 | **멀티유저 D1 Auth 인프라 선행** (Supabase Auth client setup + JWT 미들웨어 스켈레톤) | 3~5h | Q9 부분 답변 가능 | provider 만 결정되면 즉시 ship |
| 5 | **멀티유저 D2 RLS audit** | 3~5h | Q9 무관 (모든 case 공통) | 마이그 017 신규 가능 |
| 6 | **멀티유저 D3 Per-user rate limit + 비용 cap** | 4~6h | Q9 무관 | 글로벌 cap → per-user 분리 |
| 7 | **W-2 P2 후속 1번** — 첫 query short-circuit 동작 점검 | 1~2h | 즉시 | M3 sprint 후보 |
| 8 | **마이그 013 결번 트래킹** | 10분 | 즉시 | trivial |

본 세션 직후 추천: **#1 (아키텍처 다이어그램)** — 본 세션 마지막 commit `c3815c1` 의 자체평가 8/10 → 9/10 도달, 1h 빠른 ship. 그 다음 sprint 에서 #4~#6 (D1~D3 Q9 무관 부분) 진행 가능.

---

## 5. 핸드오프 / 새 세션 진입

### 5.1 다른 컴퓨터 / 새 세션 시작 시

```bash
git clone https://github.com/woongminKi/Jet-Rag.git
cd Jet-Rag
git log --oneline -1
# 기대: c3815c1 docs(readme+changelog): W23~v1.5 + Deploy + 도메인 부착 누적 진전 갱신
```

production smoke (`.env` 불필요):
```bash
curl -I https://jetrag.woong-s.com           # 200
curl -I https://jetrag-api.woong-s.com/health # 200
```

로컬 dev 시:
```bash
cd api && uv sync --frozen
cd ../web && pnpm install
# .env 복원 (비밀 저장소 → .env / web/.env)
```

### 5.2 첫 명령 (Claude 에게) — 멀티유저 D1 Auth 진입

```
work-log/2026-05-19 세션 종합 §5.4 의 D1 Auth 사전 의사결정 확인하고
senior-planner 에게 D1 plan 작성 위임. 사용자 검토 후 senior-developer
구현 → senior-qa 검증 순서로 진행.
```

### 5.3 의사결정 상태 (2026-05-19 마감 기준)

| Q | 항목 | 결정 |
|---|---|---|
| Q1 | 도메인 보유 | ✅ 해소 — woong-s.com 구매 |
| Q8 | 결제 모델 | ⏸ deferred — 베타 테스트 후 (2026-05-19 사용자 명시) |
| Q9 | 공개 범위 + Auth provider | ✅ 해소 — 초대 코드 lockdown + Email/Google OAuth (베타 30명 → 공개) |
| Q10 | 공개 시점 | ⏸ Q8 의존 — 베타 5~10명 1주 안정화 후 결정 |
| Q11 | Vercel 라이선스 | ⏸ Q8 의존 — Hobby 유지 권장 (수익 0 시) |

### 5.4 D1 Auth 사전 의사결정 (2026-05-19 마감 기준)

다음 세션 D1 진입 시 plan 의 전제로 사용:

| 항목 | 결정 |
|---|---|
| **기존 단일 유저 데이터** | **본인 계정으로 이관** (D1 ship 직후 본인 가입 → SQL UPDATE 1회로 user_id 마이그레이션, 포트폴리오 demo 가치 유지) |
| **초대 코드 메커니즘** | **DB 테이블 신규** (`invite_codes` 테이블 + 마이그 017: code/issued_by/used_by/expires_at) |
| **Auth provider** | Email/Password + Google OAuth 2개 (Q9 결정) |
| **공개 범위** | 초대 코드 lockdown → 베타 30명 안정화 → 일반 공개 |
| **진입 시점** | 다음 세션 (본 세션 6 commit · 3.5h 누적, D1 6~10h 별도 sprint) |

### 5.5 다음 세션 작업 순서 (D1 sprint)

```
1. senior-planner 위임 — D1 plan 작성
   · backend JWT 미들웨어 설계
   · frontend 로그인/가입/로그아웃 + auth context 설계
   · Supabase Auth dashboard 설정 항목 정리
   · invite_codes 마이그 017 스키마 설계
   · 기존 데이터 user_id 마이그레이션 SQL 설계
   · 단위 테스트 계획
2. 사용자 plan 검토 + 승인
3. senior-developer 구현 (6~10h 추정)
4. senior-qa 검증 (회귀 + 보호 라우트 + JWT mock)
5. Q8 deferred — 베타 시작 시점에 재진입
6. D2 RLS audit → D3 per-user cap 으로 이어지는 sprint chain
```

---

## 6. 회고

### 6.1 ROI 가 가장 높은 결정

**W-0 결정성 시험 ($0.001 미만, 1h 작업)** — 본 세션 진입 직전 (2026-05-18 commit `e57c3ec`) 의 결정이 본 세션 3 sprint 전체의 안전판이었다:
- W-1: 캐시 entry 공유 안전 보증 → factory 분기만으로 ship
- W-2: R@10 회귀 0 이론 보증 → 실측 시 PASS 확정
- 도메인: DeepInfra production swap 안전성 검증 후 진입 → 도메인 ship 시점에 양쪽 PaaS 변경 동시 진행 가능

ROI 측정: $0.001 + 1h → 3 sprint × 평균 1~2h 작업 단축 + HF Dedicated CPU $24/월 fallback ($288/년) 회피 결정. 약 300배 ROI.

### 6.2 어댑터 layer 의 직교성 실증

backend stateless + ENV 1줄 swap (`JETRAG_EMBED_PROVIDER`) + CORS env 화 (`JETRAG_CORS_ORIGINS`) → 본 세션의 4 sprint 중 **2 sprint (W-2 + 도메인) 가 코드 변경 0** 으로 ship. 페르소나 A 의 "1인 운영 부담 0" 정합 + 발표 시 어댑터 내러티브 (§9.4) 강화.

### 6.3 dashboard 작업 효율

사용자가 카드 결제·dashboard 작업·검증을 빠르게 처리 (Cloudflare 도메인 구매 + DNS 등록 + Vercel/Railway custom domain 등록 + ENV 갱신 + redeploy 가 30분 안 처리). Claude 는 코드·문서 담당. 명확한 권한 분리가 sprint 속도 ↑.

### 6.4 발견된 작은 부채

- ~~마이그레이션 013 결번~~ → ✅ 본 commit 에서 해소 — git log 추적 결과 **단순 numbering skip** 확인 (012 → 014 jump). 실행 순서·idempotency 영향 0. `api/migrations/README.md` 표에 013 reserve 행 + 009~016 누락 row 보강 (W21 마감 이후 마이그 8건 표 갱신).
- README 의 시각 자산 0건 (다이어그램·GIF·스크린샷) — ✅ mermaid 다이어그램 추가 (`4276825`), 정적 스크린샷 / 데모 GIF 는 다음 sprint 후보 (10/10 도달).

### 6.5 W-2 P2 #1 첫 query short-circuit 진단 (senior-qa, 본 commit)

**결정적 원인 = H4** — `meta_filter_fast_path` (S3 D2, 2026-05-09 commit `dc47c38` ship). substring 매칭 (`api/app/services/meta_filter_fast_path.py:329` 의 `any(c.endswith(suf) or suf in c for suf in _DOC_SUFFIXES)`) 으로 "SK 사업보고서 매출" 의 `보고서` 가 doc-suffix 인식 → `X-Search-Path: meta_fast` 라우팅 → embedding/RPC/RRF 0회 → `documents` 테이블 ILIKE 1회로 150~200ms 응답.

**가설 우열**:
- ~~H1 (embed_query_cache hit)~~ — **거짓**. SK query 의 sha256 row 가 DB 에 없음 (embed_query_cache 409 row 중 부재).
- H2 (decomposition 미발화) — 참, 그러나 fast-path 가 line 681 에서 가로채 분기 도달 안 함. 무관.
- H3 (doc-scoped fast-path) — 동일 무관.
- **H4 (meta_filter_fast_path)** — **확정**.

**판정**: 의도된 동작 + 의도 외 부수 효과 2건.

**의도된 동작** (S3 D2 planner v0.1 §C 케이스 1~4):
- 명세 사인오프: `work-log/2026-05-09 S3 D5 정량 측정 + S3 종합 핸드오프.md`
- 응답 헤더 `X-Search-Path: meta_fast` + 본문 `meta.path="meta_fast"` 노출 중

**의도 외 부수 효과** (M3 sprint 후보, 본 세션 처리 X):

| # | 항목 | 작업량 | 영향 |
|---|---|---|---|
| BIAS-1 | W-2 latency 측정 셋의 path bias (16 query 중 5건 31% 가 meta_fast → P95 인위적 낮음. RAG path 만 P95 = 1.7s 가 진짜 baseline) | 1~2h | 측정 보강, KPI #10 결론 변함 없음 |
| UX-1 | "SK 사업보고서 매출" 의 사용자 의도 (콘텐츠 검색) vs fast-path 의도 (doc title 매칭) **mismatch** — fast-path 가 `title ILIKE '%SK 사업보고서 매출%'` 으로 0건 반환. doc-suffix + 콘텐츠 키워드 동반 시 RAG fallback 검토 | 2~4h | 검색 정확도 영향 가능, golden_v2 회귀 측정 필수 |

**senior-qa 권고 4건**:
- E.1 (P2, 30분) — 본 정정 (✅ 본 commit)
- E.2 (P1, 1~2h) — W-2 측정 셋 path 별 분리 (M3 sprint)
- E.3 (P1, 2~4h) — substring 매칭 false-positive 보강 (M3 sprint, senior-qa golden_v2 회귀 측정 필수)
- E.4 (P2, 30분) — `X-Embed-Cache` 응답 헤더 노출 (진단성 보강, senior-developer 사인오프 후)

**KPI #10 결론 변함 없음** — meta_fast (P95 ~200ms) + RAG (P95 1.7s) 둘 다 게이트 안. 측정 셋만 보강 필요.

---

## 7. 참조

### 7.1 본 세션 work-log

- `work-log/2026-05-19 v1.5 W-1 DeepInfra 어댑터 swap.md`
- `work-log/2026-05-19 도메인 부착 — woong-s.com.md`
- 본 문서

### 7.2 prior work-log

- `work-log/2026-05-15 HF self-host 검토 — v1.5 sprint 설계.md` — v1.5 권장 (DeepInfra)
- `work-log/2026-05-18 배포 방법 검토 — Railway + HuggingFace.md` — DECISION-13 (배포 D 안)

### 7.3 commit hash

| commit | 내용 |
|---|---|
| `e57c3ec` | Dockerfile + DeepInfra W-0 + Vercel CORS env (이전 push, 2026-05-18) |
| `4396913` | v1.5 W-1 DeepInfra 어댑터 swap |
| `155653b` | v1.5 W-2 production 측정 + DECISION-14 + P2 테스트 |
| `c2c4e26` | 도메인 부착 woong-s.com |
| `c3815c1` | README + CHANGELOG 갱신 |

### 7.4 메모리 (auto-load)

- `~/.claude/projects/-Users-kiwoongmin-Desktop-documents-test-repo-image2-piLab-project-Jet-Rag/memory/MEMORY.md` — 인덱스 (본 세션 진전 반영됨)

### 7.5 production URLs (live)

- https://jetrag.woong-s.com — frontend custom
- https://jetrag-api.woong-s.com — backend custom (DeepInfra embedding)
- https://jetrag.vercel.app — frontend fallback
- https://jet-rag-production.up.railway.app — backend fallback
