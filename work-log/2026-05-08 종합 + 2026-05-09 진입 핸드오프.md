# 2026-05-08 종합 + 2026-05-09 다른 PC 진입 핸드오프

> 프로젝트: Jet-Rag
> 작성: 2026-05-08 마감 시점 (S2 D4-pre ship 직후)
> 목적: **다른 PC** 에서 본 1장만 보고 5분 안 컨텍스트 회복 + S2 sprint 이어 진입 가능. 본 PC (사용자 데스크탑) 의 11 docs 자료 위치 + Supabase 적재 상태 + 어제까지 누적 통합.

---

## 0. 한 줄 요약

> **2026-05-07 ~ 2026-05-08 — 10 commit push 완료** (`62aafcf` ~ S2 D2 hash). **Master plan §6 — S0/S1/S1.5 100% 완전 마감 + S2 D1 운영 hook + D4-pre 회귀 검증 + D2 page cap ship (~40%)**. 단위 테스트 528 → **632 / skipped 1 / 회귀 0**. 다른 PC 진입 시 1순위 권고 = **S2 D3 (운영 모드 UI 토글)** — D2 backend 완료, web 통합 1.5일 ETA. **Q-S2-1 (운영 모드 default) 사용자 결정 필요**.

---

## 1. 2일 누적 ship (9 commit, 모두 origin push 완료)

### 2026-05-07 (전 마감 + 본 마감 7 commit)

| # | commit | sprint | 핵심 변경 | 단위 테스트 |
|---|---|---|---|---:|
| 1 | `62aafcf` | **S0 D2-B** | vision_page_cache lookup/upsert 통합 — `services/vision_cache.py` + `ImageParser.parse(sha256=)` cache hit fast path | 531→542 (+11) |
| 2 | `318bbda` | **S0 D3** | vision_usage_log 기반 budget 초기값 — `services/budget_calculator.py` + `scripts/compute_budget.py` + config ENV 3종. 잠정값 doc $0.10 / daily $0.50 | 542→556 (+14) |
| 3 | `f90fdc8` | **S0 D4** | vision 비용 cap 가드 — `services/budget_guard.py` + extract/incremental 사전+N=5 재검사 + `VisionBudgetExceededCard` UI | 556→567 (+11) |
| 4 | `5b653b8` | **S0 D5** | 24h sliding cap + Usage 대시보드 cross-check — `check_24h_sliding_budget` + `check_combined` 우선순위 | 567→577 (+10) |
| 5 | `a6fedc2` | **S1.5 D1** | vision_need_score PoC — 6 신호 hand-tuned 가중치 | 577→588 (+11) |
| 6 | `c143102` | **S1.5 D2** | 분포 분석 + 임계 조정안 — composite max 0.46 → OR rule 채택, table 0.5→0.3, entity deprecated | 588 (회귀 0) |
| 7 | `619e425` | **S1.5 D3** | OR rule + 6 신호 전수 + table v2 single-span fallback. needs_vision_or 64.3%, recall 5/6 = 83.3% | 588→612 (+24) |

### 2026-05-08 (오늘 3 commit)

| # | commit | sprint | 핵심 변경 | 단위 테스트 |
|---|---|---|---|---:|
| 8 | `b4a56c7` | **S2 D1** | vision_need_score 운영 hook — `_enrich_pdf_with_vision` + `_vision_pages_with_sweep` 페이지 루프 hook + ENV `JETRAG_VISION_NEED_SCORE_ENABLED` 회복 토글. needs_vision False = sweep retry 대상 X. compute_page_signals raise 시 보수적 True fallback | 612→619 (+7) |
| 9 | `6378591` | **S2 D4-pre** | 회귀 측정 (Hybrid mock + DB cross-check + baseline retrieval) — `evals/run_s2_d4_pre_regression.py` 신규. **비용 $0.00**. recall 표면 회귀 1건 (G-A-008 데이터센터 p.40) but baseline R@10=1.000 → 실 회귀 위험 0 | 619 (회귀 0) |
| 10 | (신규) | **S2 D2** | page cap + cost budget 병행 — `check_doc_page_cap()` 신규 (in-memory 카운터, DB 미접근), `BudgetScope` 'page_cap' 추가, `_mark_page_cap_exceeded_flag()` (cost cap flag 와 직교). ENV `JETRAG_VISION_PAGE_CAP_PER_DOC` (default 50, 0=무한). `_vision_pages_with_sweep` 시그니처 +1 (page_cap_status). needs_vision skip 페이지는 카운터 증가 X (cap 도달 지연 정합) | 619→632 (+13) |

**누적 단위 테스트**: 528 → **632 / skipped 1 / 회귀 0** (+104)
**web tsc + lint**: 0 error (S0 D4 의 `VisionBudgetExceededCard` 만 web 변경, lint 통과)

---

## 2. Master plan §6 sprint 진척률 (2026-05-08 마감 시점)

| Sprint | 목표 | 진척률 | 마감일 | 비고 |
|---|---|---:|---|---|
| **S0** 비용 계측 / 캐시 / cap | 1주 | **100% ✅** | 2026-05-07 | D1 마이그 014 / D2 마이그 015+통합 / D3 budget 초기값 / D4 cost cap / D5 24h sliding |
| **S1** 골든셋 + 실 query 로그 | 1주 | **100% ✅** | 2026-05-07 (D3 마감) | D1~D5 골든셋 v1 (157 row) + admin 대시보드 2건 + baseline 측정 |
| **S1.5** vision_need_score PoC | 3일 | **100% ✅** | 2026-05-07 | D1 PoC / D2 분포 / D3 OR rule 안정화 |
| **S2** Vision 선별 + budget 본 ship | 1주 | **D1 + D4-pre + D2 ship (~40%)** | 진행 중 | D1 운영 hook ship (`b4a56c7`) / D4-pre 회귀 검증 (`6378591`) / **D2 page cap ship** (단위 테스트 +13). 잔여: D3 운영 모드 UI / D4 실 reingest 본격 측정 (선택) / D5 효과 측정 |
| **S3** Cheap router + decomposition + reranker | 1주 | 0% | — | S2 마감 후 |
| **S4** 인제스트 보조 인덱스 | 1~2주 | 0% | — | |
| **S5** /answer UX | 1주 | 0% | — | |

### S0 효과 (master plan §6 정합 검증 완료)
- vision worst case **450 → 50 호출** (sweep 3→2 + retry 3→1)
- reingest cache **0 호출** (vision_page_cache, S0 D2-B)
- 폭주 **3중 차단** (doc + daily + 24h sliding)
- 빌링 cross-check 가이드 (Google AI Studio Usage)

### S1.5 효과
- 본 PC 5 PDF / 115 페이지 측정 — needs_vision_or **64.3%** (vision call 약 36% 회피 가설)
- 골든셋 v1 vision_diagram + table_lookup recall **5/6 = 83.3%**

### S2 D1 + D4-pre 효과
- 운영 hook 활성 (default true) — needs_vision False 페이지 paid call skip
- 본 PC 5 PDF mock 시뮬레이션 — **115p → 41 skip (35.7%, 가설 36% 정확 매칭)**
- per doc skip 분포: 데이터센터 87.8% / sonata 10.3% / 포트폴리오 0% / 보건의료 0% / law sample3 50%
- 골든셋 회귀 **0** (baseline R@10=1.000, 정답 chunk top-3 retrieve)

### 알려진 한계
- 데이터센터 PDF p.40 single-line table false negative (S1.5 v3 multi-line 휴리스틱 후속 후보)
- 데이터센터 87.8% skip 의문 — 표·그림 위주 자료지만 PyMuPDF text 만으로 retrieval 충분 (실 reingest 필요 시 옵션 A 진입)

---

## 3. 다른 PC 진입 절차 (5분)

### 3.1 코드 + work-log 동기화

```bash
# 본 repo clone (처음이면) 또는 pull
git clone https://github.com/woongminKi/Jet-Rag.git    # 첫 진입
# 또는
cd Jet-Rag && git pull origin main                     # 이미 있으면

cd Jet-Rag
git log --oneline -3
# 기대: 6378591 / b4a56c7 / 21edff6 (현재 origin head)

# 의존성
cd api && uv sync
cd ../web && pnpm install

# 단위 테스트 baseline
cd ../api && uv run python -m unittest discover tests
# 기대: Ran 619 tests, OK (skipped=1)

# web 검증
cd ../web && pnpm exec tsc --noEmit && pnpm lint
# 기대: 0 error
```

### 3.2 환경 변수 (`api/.env`)

본 PC 와 동일 값 사용:
```bash
SUPABASE_URL=https://mpmtydudhojpukuuadrd.supabase.co
SUPABASE_KEY=<anon>
SUPABASE_SERVICE_ROLE_KEY=<service_role>
GEMINI_API_KEY=<gemini>
HF_API_TOKEN=<hf>

# W25 D14 ship
JETRAG_PDF_VISION_ENRICH=true

# S0 ship
JETRAG_GEMINI_RETRY=3                  # 503 회복용
JETRAG_VISION_MAX_SWEEPS=2             # default 3 → 2 (S0 D3)
# JETRAG_DOC_BUDGET_USD=0.10           # S0 D3 잠정값 (default config 에 0.10)
# JETRAG_DAILY_BUDGET_USD=0.50         # S0 D3 잠정값
# JETRAG_24H_BUDGET_USD=0.50           # S0 D5 (default = daily)

# S2 D1 ship
# JETRAG_VISION_NEED_SCORE_ENABLED=true  # default true (회복 토글, false 시 모든 페이지 호출)
```

`web/.env` — 변경 없음 (이전 값 유지).

### 3.3 본 PC 의 11 docs 자료 (다른 PC 에는 없음)

본 PC `<repo>/` 루트 직속에 사용자 자료 11건:
- PDF 5: `(붙임2) 2025년 데이터센터 산업 활성화 지원 사업 통합_안내서.pdf` / `law sample3.pdf` / `sonata-the-edge_catalog.pdf` / `보건의료_빅데이터_플랫폼_시범사업_추진계획(안).pdf` / `포트폴리오_이한주 - na Lu.pdf`
- HWP 1: `law sample2.hwp`
- HWPX 2: `직제_규정(2024.4.30.개정).hwpx` / `한마음생활체육관_운영_내규(2024.4.30.개정).hwpx`
- DOCX 2: `승인글 템플릿1.docx` / `승인글 템플릿3.docx`
- PPTX 1: `브랜딩_스튜디오앤드오어.pptx`

`.gitignore` 의 `/*.{pdf,hwp,hwpx,docx,pptx}` 로 git 추적 X. 다른 PC 동기화 옵션:
- **A. USB / 외장 디스크** → `<repo>/` 루트 또는 `<repo>/assets/` 직속 복사 (자동 인식, ENV 0)
- **B. iCloud 등 사용자 클라우드** → 자동 동기화
- **C. Supabase 만 활용** — chunks 이미 적재 → 실 reingest 안 하고 retrieval 만 측정 (S2 D2/D3 진행 가능)

`assets/public/` 8건 (PDF 4 + HWPX 2 + HWP 1 + sample-report) 은 git 추적 → `git pull` 만으로 자동 회귀.

### 3.4 dev 시작

```bash
# Terminal 1
cd api && uv run uvicorn app.main:app --reload

# Terminal 2
cd web && pnpm dev
```

---

## 4. 다음 후보 우선순위

### 4.1 1순위 — S2 D3 (운영 모드 UI 토글)

**왜?** S2 D2 backend (page cap + cost budget 병행) 완료 → master plan sprint 순서 정합. 1.5일 ETA. **Q-S2-1 (운영 모드 default) 사용자 결정 필수**.

**작업**: 운영 모드 3개 UI 토글 (`<select>`) — 기본 / 빠른 / 정밀.

| 항목 | 설계 결정 |
|---|---|
| 모드별 page cap | 빠른=10 / 기본=50 / 정밀=0 (무한) — 사용자 결정 의존 |
| UI 위치 | upload form + doc 카드 (재처리 버튼 옆) |
| API | POST /documents 에 mode 인자 추가 + ENV override 가능 |
| 카드 UI | `vision_page_cap_exceeded` flag 표시 + "정밀 모드로 재처리" 버튼 |
| 단위 테스트 | mode → page cap 매핑 + UI 회귀 보호 |

산출물: `routers/documents.py` 갱신 + web upload UI + `VisionPageCapExceededCard` 신규 + 단위 테스트

### 4.2 2~5순위

| # | 후보 | 작업량 | 진입 조건 | 권고도 |
|---|---|---|---|---|
| 2 | **S1.5 v3 multi-line table 휴리스틱** | 0.5~1일 | 데이터센터 87.8% skip 의문 해소 | ★★ |
| 3 | **S2 D4 옵션 A 실 reingest 본격 측정** | 0.5~1일 | 비용 ~$0.32 + DB chunks 갱신 → 사용자 명시 승인 필요 | ★ |
| 4 | **E1 진단 + 1차 ship 잔여 (A2/A3/A4/A5)** | 1.5~3일 | PDF reingest 필요 — 자료 보유 PC 시점 | ★★ |
| 5 | **S1.1 reliable 100+ sample 모델 회귀 재측정** | 0.5일 | KST 02~05시 503 회복 시간대 + RAGAS judge timeout | ★ |

### 4.3 후속 (S2 마감 후 순차)

S3 (1주, cheap router + decomposition + reranker) → S4 (1~2주, 보조 인덱스) → S5 (1주, /answer UX) — master plan §6 정합.

---

## 5. 활성 한계 (다른 PC 진입 시 점검)

| # | 한계 | 영향 | 회복 절차 |
|---|---|---|---|
| 1 | **데이터센터 p.40 single-line table false negative** | 표 자료 vision call 누락 가능 (단 baseline R@10=1.000 으로 실 회귀 위험 0) | S1.5 v3 multi-line 휴리스틱 후속. 또는 옵션 A 실 reingest 로 표 안 내용 정확성 spot 검증 |
| 2 | **데이터센터 87.8% skip 의문** | 표·그림 위주 자료 vs 5 신호 모두 0 trigger | (a) PyMuPDF text 추출 충분 가설 (실 reingest spot 검증) (b) S1.5 v3 multi-line 휴리스틱 catch 검증 |
| 3 | **본 PC 5 PDF 통계 신뢰도 한계** | needs_vision_or 64.3% 의 신뢰 구간 명시 X | S2 D5 효과 측정 시 11 docs 전수 + 골든셋 v1 cross-check |
| 4 | **entity 신호 deprecated** | composite 가중 0, OR 항목 제거 | 한국어 추가 패턴 (`< 표 N >`, `(그림 N)`) v2 시점 |
| 5 | **S1 D5 모델 회귀 신뢰도 (n=12)** | 2.5-flash vs lite factory 결정 보류 | S1.1 reliable 100+ 재측정 |
| 6 | **E1 진단 미완** | ETA 정밀화 (A2/A3/A4/A5) 진입 신호 부재 | PDF reingest 가능 PC 진입 시 E1 plan §10 채움 |
| 7 | **RAGAS Context Precision 47점 사례** | 베타 시점 사용자 만족도 cross-check 필요 | 베타 5~10명 누적 후 6 옵션 진입 |

---

## 6. 사용자 결정 보류 항목

| # | 항목 | 권고 default | 결정 시점 |
|---|---|---|---|
| Q-S2-1 | 운영 모드 default | **기본** (5~10p, master plan §3) | S2 D3 진입 시 |
| Q-S2-2 | 11 docs 재인제스트 시점 | S2 D5 효과 측정과 함께 1회만 | S2 D5 진입 시 |
| Q-S2-3 | budget 초기값 (잠정 doc $0.10 / daily $0.50) | 1주 추가 누적 후 자동 산정 (≥ 2026-05-14) | 누적 데이터 확보 후 |
| ~~Q-S2-4~~ | ~~page cap default 값~~ | **50 채택 → S2 D2 ship 완료** (default Settings + ENV 회복 토글) | ~~S2 D2 진입 시~~ ✅ |
| Q-S1.1 | 2.5-flash vs lite factory | S1.1 100+ 재측정 후 | 별도 sprint |
| Q-E1 | 정확도 vs latency 우선순위 | (a) 정확도 P0 | E1 1차 ship 진입 시 |

---

## 7. 참고 문서 우선순위 (5분 회복)

| # | 문서 | 5분 행동 |
|---|---|---|
| 1 | **본 문서 §0 / §3 / §4** | 마감 상태 + 진입 절차 + 다음 후보 |
| 2 | [`2026-05-08 S2 D2 page cap + cost budget 병행.md`](./2026-05-08%20S2%20D2%20page%20cap%20+%20cost%20budget%20%EB%B3%91%ED%96%89.md) | S2 D2 ship 본문 — 설계 결정 6건 + hook 흐름 + 단위 테스트 +13 |
| 3 | [`2026-05-08 S2 D4-pre 회귀 측정.md`](./2026-05-08%20S2%20D4-pre%20%ED%9A%8C%EA%B7%80%20%EC%B8%A1%EC%A0%95.md) | 본 PC 5 PDF mock 측정 결과 + 데이터센터 87.8% skip 분석 |
| 4 | [`2026-05-08 S2 D1 vision_need_score 운영 hook ship.md`](./2026-05-08%20S2%20D1%20vision_need_score%20%EC%9A%B4%EC%98%81%20hook%20ship.md) | S2 D1 hook 흐름 + 설계 결정 6건 |
| 4 | [`2026-05-07 종합 + 2026-05-08 진입 핸드오프.md`](./2026-05-07%20%EC%A2%85%ED%95%A9%20+%202026-05-08%20%EC%A7%84%EC%9E%85%20%ED%95%B8%EB%93%9C%EC%98%A4%ED%94%84.md) | 어제 마감 (S0+S1.5 100%) |
| 5 | [`2026-05-07 S1.5 D3 ship — 임계·가중 정정 + S2 진입 결정.md`](./2026-05-07%20S1.5%20D3%20ship%20%E2%80%94%20%EC%9E%84%EA%B3%84%C2%B7%EA%B0%80%EC%A4%91%20%EC%A0%95%EC%A0%95%20+%20S2%20%EC%A7%84%EC%9E%85%20%EA%B2%B0%EC%A0%95.md) | S1.5 sprint 마감 본문 — OR rule 정의 |
| 6 | [`2026-05-06 무료유료 모델 전략 통합 plan + 다른 컴퓨터 핸드오프.md`](./2026-05-06%20%EB%AC%B4%EB%A3%8C%EC%9C%A0%EB%A3%8C%20%EB%AA%A8%EB%8D%B8%20%EC%A0%84%EB%9E%B5%20%ED%86%B5%ED%95%A9%20plan%20+%20%EB%8B%A4%EB%A5%B8%20%EC%BB%B4%ED%93%A8%ED%84%B0%20%ED%95%B8%EB%93%9C%EC%98%A4%ED%94%84.md) | Master plan §6 의 6 sprint 전체 흐름 |
| 7 | [`2026-04-22 개인 지식 에이전트 기획서 v0.1.md`](./2026-04-22%20%EA%B0%9C%EC%9D%B8%20%EC%A7%80%EC%8B%9D%20%EC%97%90%EC%9D%B4%EC%A0%84%ED%8A%B8%20%EA%B8%B0%ED%9A%8D%EC%84%9C%20v0.1.md) | 페르소나 A · KPI · DoD 마스터 |

---

## 8. 핵심 변경 파일 목록 (2일 누적 9 commit)

### 신규 (services/scripts/tests)
- `api/app/services/vision_cache.py` (S0 D2-B)
- `api/app/services/budget_calculator.py` (S0 D3)
- `api/app/services/budget_guard.py` (S0 D4 + S2 D2 — `check_doc_page_cap`)
- `api/app/services/vision_need_score.py` (S1.5 D3 — D1 의 `ingest/stages/vision_score.py` 이동·rename)
- `api/scripts/compute_budget.py` (S0 D3)
- `api/scripts/poc_vision_need_score.py` (S1.5 D1 + D3 v2)
- `api/scripts/analyze_vision_need_score.py` (S1.5 D2)
- `evals/run_s2_d4_pre_regression.py` (S2 D4-pre, ~526 LOC)
- `api/tests/test_vision_cache.py` / `test_budget_calculator.py` / `test_budget_guard.py` / `test_vision_need_score.py` / `test_extract_pdf_vision_enrich.py` 신규 클래스 / `test_incremental_vision.py` (총 +104 단위 테스트)
- work-log 9건 (S0 D3~D5 / S1.5 D1~D3 / S2 D1 / S2 D4-pre / S2 D2 / 본 문서)

### 수정
- `api/app/config.py` — S0 D3 의 doc/daily/krw + S0 D5 의 sliding_24h + S2 D1 의 vision_need_score_enabled + **S2 D2 의 vision_page_cap_per_doc**
- `api/app/adapters/impl/image_parser.py` — S0 D2-B 의 `parse(sha256=)` cache hit fast path
- `api/app/ingest/stages/extract.py` — S0 D2-B vision_cache + S0 D4 budget_guard 사전+N=5 + S2 D1 needs_vision hook + **S2 D2 page cap hook + `_mark_page_cap_exceeded_flag`**
- `api/app/ingest/incremental.py` — 동일 패턴 + **S2 D2 시그니처 +1 (page_cap_status 반환)**
- `web/src/app/doc/[id]/page.tsx` — S0 D4 `VisionBudgetExceededCard`
- `web/src/lib/api/index.ts` + `types.ts` — S0 D4 reingestMissingVision + 타입
- `.env.example` — S0 D3 + S2 D1 + **S2 D2** ENV 매트릭스
- `api/scripts/README.md` — S0 D3 매트릭스

---

## 9. 다른 PC 진입 후 첫 명령 (즉시 실행)

```bash
# 1) 동기화 + baseline (3분)
cd Jet-Rag && git pull origin main
cd api && uv sync && uv run python -m unittest discover tests
# 기대: Ran 632 tests, OK (skipped=1)

# 2) S2 D3 진입 (master plan §6 default, 1.5일 ETA)
# senior-developer 에 의뢰 — 운영 모드 UI 토글
# - Q-S2-1 사용자 결정 필수 (운영 모드 default — 기본 / 빠른 / 정밀)
# - mode → page cap 매핑 (빠른=10, 기본=50, 정밀=0=무한)
# - upload form + doc 카드 UI (web 변경 동반)
# - VisionPageCapExceededCard 신규 (cost cap card 와 직교)
# - 단위 테스트 + work-log
```

---

## 10. 한 문장 마감

> **2026-05-08 마감 — 10 commit push 누적**. **S0 + S1 + S1.5 100% 완전 마감 + S2 D1 + D4-pre + D2 page cap ship (~40%)**. 단위 테스트 528 → **632** / skipped 1 / 회귀 0. 본 PC 5 PDF mock 측정 35.7% skip (가설 정확 매칭) + 골든셋 회귀 0 (R@10=1.000). S2 D2 ship — `JETRAG_VISION_PAGE_CAP_PER_DOC` (default 50) 추가, in-memory 카운터로 cost cap 과 직교, needs_vision skip 페이지는 카운터 증가 X (사용자 가치 페이지만 차감). 다른 PC 진입 시 §3 절차 5분 + §4.1 의 **S2 D3 운영 모드 UI** (1순위, Q-S2-1 사용자 결정 필요, 1.5일 ETA). 사용자 자료 11 docs 동기화는 §3.3 옵션 A/B/C 중 선택.
