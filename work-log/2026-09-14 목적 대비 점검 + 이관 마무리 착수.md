# 2026-09-14 목적 대비 점검 + 이관 마무리 착수

> **범위**: ① 프로젝트 목적(자동 수집 → 회의 전 요약 → 기억 검색) 대비 현재 상태 점검
> ② 9/8 핸드오프 §7 의 이관 마무리 항목 처리.
> **다음 세션 재진입**: [§4.1 Railway 서비스 삭제](#41-railway-서비스-삭제-핸드오프-71) — 사용자 승인 완료(9/14),
> 대시보드 삭제만 남음. 삭제 확인되면 §5 의 후속 3줄로 이관이 닫힌다. spike 는 같은 날 삭제 완료(§4.2).

---

## 0. 한눈에 보기

| 항목 | 상태 |
|---|---|
| 목적 대비 점검 (코드 + 운영 DB 실측) | ✅ §1 |
| 7.2 GitHub repo variable `JET_RAG_API_BASE` | ✅ **0건 확인** — SLO 실행 5건 로그 전부 프록시 주소 |
| 7.6 Railway 전용 파일 정리 | ✅ `2be7307` — Dockerfile·.dockerignore·documents 대조 하네스 삭제 |
| 7.1 Railway 서비스 삭제 | ⬜ **사용자 결정** — 9/14 현재 `server: railway-hikari` 200, 아직 살아 있음 |
| 7.7 `spike` 함수 | ✅ **삭제** `4162720` — 배포 delete(라이브 404) + 디렉터리·config.toml·deno.json exclude 정리. deno test 246 passed |
| 라이브 스모크 (정리 커밋 후) | ✅ /health 200 · /search 200 · /stats 200 · /documents 200 · /me/plan 401 |

---

## 1. 목적 대비 점검 결과

사용자가 서술한 목적: PC·모바일에서 hwp·pdf·캡처 이미지를 **자동으로** 읽어 두고,
① "회의 전에 파일이 너무 많다, 요점만" ② "예전에 그거 어디 있지, 무슨 내용이었지" 에 답하는 에이전트.

| 목적 | 상태 | 근거 |
|---|---|---|
| 자동 수집 | **없음.** 웹 업로드·안드로이드 공유시트·이메일 3채널 모두 사용자가 직접 밀어넣음. Drive·폴더 감시·네이티브 앱은 "후속 sprint" 문구만 | `docs/superpowers/specs/2026-07-05-monetization-sprint-design.md:48` |
| ① 다건 브리핑 | **부분.** 문서 1건 3줄 요약·태그는 인제스트 때 생성·표시. 여러 문서 묶는 기능은 기획서 US-10(multi-doc map-reduce)이 v2 유예 후 미착수. `/answer` 는 상위 10청크·5문장 상한이라 요약 용도에 구조적으로 부적합 | 기획서 v0.1 `:92,123,144` / `_shared/answer/prompt.ts:13,24` |
| ② 기억 검색 | **완성.** 하이브리드 RRF + 페이지·하이라이트 + 결과에 요약 노출 + 인용 답변. 골든셋 top-1 ≈0.80 | `_shared/search/assemble.ts`, `result-card.tsx` |
| 검색 필터 UI | **자리표시자.** API 는 날짜·타입·태그 필터를 받지만 웹은 `q,limit,offset,doc_id,mode` 만 보냄 | `filter-sidebar.tsx:17-20`, `web/src/lib/api/index.ts:57-67` |

### 실사용 실측 (운영 DB, service role 로 직접 조회)

| 항목 | 값 |
|---|---|
| auth 사용자 | **1명** (본인, 2026-05-20 가입, 최근 로그인 09-06) |
| 문서 | 14건 — 5월 12 / 7월 1 / 9월 1 |
| `search_metrics_log` | 17,938건 중 5월 17,028건(평가 실행). 7/20 이후 902건 중 골든셋 질의 491건, 나머지는 "a"·"test"·"가가가…" 스모크 |
| `payment_history` / `subscriptions` | 0건 / 본인 수동 등록 1건 |
| 베타 피드백 | 1건(7월, 갤럭시) — "업로드 자체가 귀찮다, 알아서 읽어줬으면" = 자동 수집 요구 |

**판정**: 목적 ② 축만 완성. 8/24~9/8 이관 174커밋은 사용자 가치 0 (비용 모순은 9/8 핸드오프 §7.8 과
메모리에 이미 기록, 재론 안 함). **사용자 결정: 이관을 먼저 닫고, 그 다음 자동 수집(A) → 다건 브리핑(B).**
상세는 메모리 `jetrag_2026_09_14_goal_gap_assessment.md`.

### 점검 중 확인한 사실 (오판 방지용)

- `web/src/app/admin/*` 페이지는 프론트 인증이 없지만 **백엔드 `/admin/*` 는 `requireAdmin` 게이트**.
  무인증 라이브 호출 `/admin/queries/stats` → **403**. 프론트만 보고 "관리자 화면 노출" 로 판정하면 틀린다.
- 대조 하네스 개수: `verify_*.py` **63** (그중 `*_parity.py` 59). 핸드오프의 "63종" 은 전자 기준.

---

## 2. 7.2 GitHub variable — 0건

```
gh api repos/woongminKi/Jet-Rag/actions/variables → total_count 0
gh run view 34743940615 --log | grep API_BASE → JET_RAG_API_BASE: https://jetrag-api.woong-s.com
```

9/9·9/10·9/11·9/12·9/13 `monitor-search-slo` 5건 전부 success, 프록시 주소. **Railway 를 지워도 CI 는 안 깨진다.**
(gh 는 로그인이 안 돼 있어 keychain 의 git credential 을 `GH_TOKEN` 으로 넘겨 조회했다.)

---

## 3. 7.6 Railway 전용 파일 정리 — `2be7307`

| 삭제 | 이유 | 의존자 |
|---|---|---|
| `api/Dockerfile` | RAILPACK·`PORT` 주입 전제의 Railway 빌드 파일 | 0 (Makefile·CI·문서 참조 없음) |
| `api/.dockerignore` | 위 파일 짝 | 0 |
| `api/scripts/verify_documents_read_parity.py` | Railway 응답과 **직접** 대조. 9/8 컷오버로 역할 종료 | CI 미포함. 422 fixture(`fixtures/documents_422_measured.json`)는 남김 |

수정: `rehearse_railway_shutdown.ts` 안내문(variable 0건·하네스 삭제 반영), `read.ts:98` 주석.
`deno check read.ts` 통과. `rehearse_railway_shutdown.ts` 의 fmt 경고는 HEAD 에서도 동일하고 CI 범위(`supabase/functions`) 밖.

---

## 4. 사용자 결정 2건 (9/14 둘 다 승인)

### 4.1 Railway 서비스 삭제 (핸드오프 §7.1)

- 9/14 실측: `https://jet-rag-production.up.railway.app/health` → **200, `server: railway-hikari`**. 6일째 프록시 미경유, 요금 계속.
- 관찰 조건 충족: 9/8 컷오버 이후 SLO 모니터 5일 연속 success, 앱 라우트 스모크 정상.
- 로컬에 Railway 토큰·CLI 없음 → **대시보드에서 직접**: Railway → 프로젝트 `feisty-hope` → 서비스 `Jet-Rag` → Settings → Delete Service.
- 되돌리기: `workers/api-proxy/wrangler.toml` 의 `LEGACY_ORIGIN` 은 이미 비어 있어 코드 쪽 되돌릴 것 없음. 서비스 삭제는 비가역.

### 4.2 `spike` Edge 함수 삭제 (핸드오프 §7.7) — ✅ 완료 `4162720`

- `supabase functions list` → `spike` **ACTIVE v8, verify_jwt=false**.
- 라이브 실측: `GET /functions/v1/spike` 무인증 200. `?kind=burn&ms=300` → `cpuMs 300`. 즉 누구나 요청당 최대 2s CPU 를 태울 수 있다(요금 변수는 호출 수, Pro 200만 건/월).
- `?kind=env` 는 Deno 버전·SharedArrayBuffer 유무 등 **런타임 능력 조사** — 시크릿 노출 없음(코드·응답 확인).
- 실행: `supabase functions delete spike` → 라이브 404. `supabase/functions/spike/` 4파일 +
  `config.toml` [functions.spike] 블록 + `deno.json` exclude 제거. deno fmt 148 · lint 147 · **test 246 passed**.
  `api/scripts/spike_*.py` 기준선 스크립트는 보존.

---

## 5. 결정 후 후속 (코드 3줄 규모)

1. Railway 삭제 확인: `curl -sI https://jet-rag-production.up.railway.app/health` 가 404/연결 실패로 바뀌는지.
2. README `:73-76` 비용 문단 갱신("Railway $5 는 서비스를 삭제해야 빠진다" → 삭제 완료).
3. 메모리 `jetrag_railway_removal_goal.md` 를 "종료" 로 갱신, 핸드오프 §7 체크박스 반영.

---

## 6. 다음 후보 (이관 종료 후)

- **A. 자동 수집** (권고) — Google Drive 지정 폴더 동기화(Edge + pg_cron). 목적의 첫 문장이자 베타 피드백 1순위.
- **B. 다건 브리핑** — 기획서 US-10 부활. 문서 14건인 지금은 데이터가 없어 A 뒤.
- **C. 검색 필터 UI 결선** — 사이드바를 API 필터 4종에 연결. 반나절.
